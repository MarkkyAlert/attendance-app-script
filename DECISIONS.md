# DECISIONS — ทำไมถึงเป็นแบบนี้

บันทึกการตัดสินใจที่ **มองไม่เห็นจากการอ่านโค้ด** พร้อมทางที่พิจารณาแล้วไม่เลือก

ช่อง **"ไม่เลือกทางไหน"** คือหัวใจของเอกสารนี้ — มันมีไว้กันไม่ให้คนมาแก้ต่อ "ปรับปรุง" ย้อนกลับ
ไปเป็นทางที่เคยลองแล้วรู้ว่าไม่ได้ผล

กลไกอยู่ที่ [ARCHITECTURE.md](ARCHITECTURE.md) · กติกาและจุดห้ามแตะอยู่ที่ [CLAUDE.md](CLAUDE.md)

---

## หลักการตั้งต้น 3 ข้อ

ก่อนอ่านรายข้อ — เกือบทุกการตัดสินใจในระบบนี้สืบกลับไปที่ 3 ข้อนี้:

1. **ตัวเลขในระบบขึ้นเอกสารราชการ (ปพ.6)** ที่ครูต้องส่งโรงเรียนจริง ความถูกต้องมาก่อนความสวย
   และมาก่อนความสะดวกในการเขียนโค้ด
2. **ขายแบบ copy ต่อครู 1 คน** อัปเดตโค้ดให้ลูกค้าเก่าไม่ได้ ดู log ไม่ได้ แก้ให้ทีหลังไม่ได้
   → ต้องเลือกทางที่ **พังแล้วกู้ได้เอง** มากกว่าทางที่เร็วกว่า
3. **ผู้ใช้คือครูประจำชั้นที่ยุ่ง ไม่ใช่ผู้ดูแลระบบ** ระบบต้องไม่ขวางงาน และไม่ให้ตัวเลขที่ตีความได้หลายแบบ

---

## ความปลอดภัย

### `ANYONE_ANONYMOUS` และรหัสครูเป็นปราการเดียว

- **บริบท** — หน้าผู้ปกครองต้องเปิดได้จากลิงก์ที่ครูส่งให้ทาง LINE โดยผู้ปกครองไม่ต้องมีบัญชี Google
- **เลือก** — ทั้ง deployment เป็น `ANYONE_ANONYMOUS` แล้วสร้างชั้นสิทธิ์เองทั้งหมดในระดับแอปพลิเคชัน
- **ไม่เลือกทางไหน** — บังคับล็อกอิน Google (`ANYONE`/`DOMAIN`) เพราะหน้าผู้ปกครองอยู่ใน deployment
  เดียวกันและจะเปิดไม่ได้ · แยกเป็น 2 deployment ก็ไม่ได้เพราะครูต้องกด deploy เองอยู่แล้ว
  การเพิ่มเป็น 2 ครั้งทำให้ขั้นตอนติดตั้งพังง่ายขึ้นเท่าตัว
- **ผลที่ตามมา** — ฟังก์ชัน global ที่ไม่มี `_` ต่อท้ายเปิดรับจากอินเทอร์เน็ตทันที และ**รหัสครูเป็นสิ่งเดียวที่กันจริง**
  (ด่านตรวจอีเมลเจ้าของที่มีอยู่ในโค้ดไม่ทำงาน — ดู ARCHITECTURE ข้อ 3)
- **ทบทวนเมื่อ** — ถ้าเลิกทำหน้าผู้ปกครอง หรือย้ายหน้าผู้ปกครองไปคนละ deployment

### โควตาอุปกรณ์ 5 เครื่อง แบบปฏิเสธเครื่องที่ 6

- **บริบท** — โค้ดเดิม `slice(0, 5)` เบียดเครื่องเก่าออกเงียบๆ เมื่อรายการเต็ม
- **เลือก** — `isTrustedTeacherDeviceQuotaFull_` ปฏิเสธเครื่องใหม่ (fail-closed) พร้อมบอกวิธีรีเซ็ตจากเมนูในชีต
- **ไม่เลือกทางไหน** — เบียดเครื่องเก่าออก เพราะ**คนที่รู้รหัสครูจะแทรกตัวเข้ามาได้เสมอ**ไม่ว่ารายการจะเต็มแค่ไหน ·
  บังคับเครื่องเดียวก็ไม่เลือก เพราะขัดกับ `MAX_TRUSTED_TEACHER_DEVICES` ที่ตั้งไว้ 5 และครูใช้ทั้งมือถือกับคอมจริง
- **ผลที่ตามมา** — คนที่รู้รหัสครูยิง 5 ครั้งด้วย device id สุ่ม จะล็อกครูออกจากระบบถาวร
  **ยอมรับผลนี้** เพราะกู้ได้ทางเมนูใน Google Sheets ซึ่งเป็นช่องทางที่คนจากอินเทอร์เน็ตเข้าไม่ถึง
  — เสียเวลาดีกว่าถูกแทรกตัวแบบไม่รู้ตัว
- **ทบทวนเมื่อ** — ถ้ามีวิธียืนยันตัวตนครูที่แข็งกว่ารหัสผ่านตัวเดียว

### Session ทุกชนิดอยู่บน CacheService

- **เลือก** — CacheService ล้วน ไม่มีที่เก็บถาวร
- **ไม่เลือกทางไหน** — Script Properties (โควตาจำกัด และเขียนบ่อยจะช้า) · ชีตแยก (ทุก request ต้องแตะ Sheets)
- **ผลที่ตามมา** — **ครูหลุดออกจากระบบแบบสุ่มได้ และหลุดทุกครั้งที่ deploy version ใหม่**
  เป็นพฤติกรรมที่ยอมรับแล้ว ไม่ใช่บั๊ก · rate limit ก็อยู่ที่เดียวกันจึงเป็น best-effort ไม่ใช่การรับประกัน
- **ทบทวนเมื่อ** — ถ้ามี bug report เรื่องหลุดบ่อยจนครูรำคาญ ทางแก้คือย้าย session (ไม่ใช่ rate limit) ไปที่ถาวร

### Print token ใช้ครั้งเดียว + scope ล็อกฝั่ง server

- **บริบท** — หน้าพิมพ์เปิดเป็นแท็บใหม่ซึ่งเข้าไม่ถึง `sessionStorage` ของแท็บเดิม
- **เลือก** — token exchange อายุ 120 วิ ใช้ครั้งเดียว แลกเป็น print session ที่ล็อก
  "รายงาน 1 ชนิด ช่วงวัน 1 ช่วง" ไว้ตั้งแต่ฝั่ง server (`enforcePrintRequestScope_`)
- **ไม่เลือกทางไหน** — ส่ง session ครูไปใน URL เพราะ token สิทธิ์เต็มจะไปโผล่ใน address bar / history / referrer
- **ผลที่ตามมา** — ต่อให้ print token หลุด เอาไปดึงรายงานอื่นหรือช่วงเวลาอื่นไม่ได้ ·
  ต้องมี fallback ผ่าน `localStorage` เพราะบาง browser ตัด query param ตอนเปิดแท็บใหม่จากใน iframe

### ด่าน local context สำหรับเครื่องมือ dev

- **บริบท** — ช่องเลือกฟังก์ชันของ Apps Script editor **ไม่แสดงฟังก์ชันที่ลงท้ายด้วย `_`**
  ประตูเข้าของเครื่องมือ dev จึงต้องเป็น public ซึ่งแปลว่าเปิดรับจากอินเทอร์เน็ต
- **เลือก** — กันด้วย `Session.getActiveUser().getEmail()` ซึ่ง**ว่างเสมอเมื่อถูกเรียกผ่าน `google.script.run`
  บน deployment แบบ anonymous** แต่มีค่าเมื่อรันจาก editor หรือเมนูในชีต
- **ไม่เลือกทางไหน** — `SpreadsheetApp.getUi()` เพราะทดสอบแล้ว **throw ตอนรันจาก editor ด้วย** ใช้เป็นด่านไม่ได้
- **ผลที่ตามมา** — เป็นแพทเทิร์นที่ควรลอกไปใช้กับเครื่องมือ dev ทุกตัวในอนาคต

### ความลับอยู่ใน Script Properties เท่านั้น ห้ามลงชีต

- **บริบท** — เวอร์ชันเก่าเคยเก็บ `teacher_key`, `parent_secret_key`, `teacher_auth_emails` ไว้ในชีต `ตั้งค่า`
- **เลือก** — ย้ายไป Script Properties ทั้งหมด และให้ `ensureSecurityMigration_` **ไล่ลบของเก่าออกจากชีต**
- **เหตุผล** — ชีตแชร์ได้ export ได้ เห็นได้จาก Drive และติดไปกับ backup ที่ครูส่งต่อได้
- **ผลที่ตามมา** — Script Properties **ไม่ติดไปกับการ copy** ครูต้องกดเมนูติดตั้งเองทุกครั้ง ซึ่งเป็นข้อดีในแง่นี้

### PIN คือ UI lock ไม่ใช่ security boundary

- **เลือก** — `verifyPin` อยู่ **ข้างใน** `runAsTeacher_` คือต้องมี session ครูที่ใช้ได้อยู่แล้วถึงจะเรียกได้
- **เหตุผล** — PIN แก้ปัญหาคนละข้อกับรหัสครู: รหัสครูกันคนนอกที่เข้าถึง URL, PIN กันคนที่หยิบ
  **เครื่องที่ล็อกอินค้างอยู่**ไปใช้ (เช่นวางมือถือไว้บนโต๊ะครู) การให้ PIN 4-6 หลักเป็นด่านจริง
  จะอ่อนกว่ารหัสครู 12 ตัวที่มีอยู่แล้ว
- **ผลที่ตามมา** — **ตอนที่ UI ขึ้นหน้าล็อก PIN ทุก endpoint ครูยังเรียกได้ปกติด้วย session เดิม**
  → ห้ามเอา PIN ไปกันข้อมูลอ่อนไหวใดๆ · lockout เก็บใน Script Properties จึงถาวรและ global ทั้งระบบ

---

## ข้อมูล

### กันสูตรด้วยการใส่ `'` นำหน้า ไม่ใช่ปฏิเสธค่า

- **บริบท** — Sheets ตีความค่าที่เขียนผ่าน `setValues` เหมือนผู้ใช้พิมพ์เอง ชื่อที่ขึ้นต้นด้วย `=` กลายเป็น `#ERROR!`
  **ชื่อเด็กหายทั้งจากระบบและจากเอกสาร ปพ.6**
- **เลือก** — `sanitizeSheetText_` เติม `'` นำหน้าเมื่อเจอ `= + - @` หรือ `\t \r`
- **ไม่เลือกทางไหน** — ปฏิเสธค่าที่ขึ้นต้นด้วยอักขระเหล่านี้ เพราะจะกลายเป็นการบังคับให้ครูแก้ข้อมูลตัวเอง
  เพื่อเอาใจข้อจำกัดของ Sheets · `setNumberFormat('@')` **ทดสอบบนชีตจริงแล้วกันไม่อยู่**
- **พิสูจน์แล้วบนชีตจริง** — `'` ถูกกลืนตอนเขียนและไม่ติดกลับมาตอนอ่าน ค่ากลับมาครบทั้งชุด
  (มี regression test ใน `Diagnostics.js`)

### ลาป่วย/ลากิจหักออกจากตัวหาร ไม่นับเป็นขาด

- **เลือก** — `basis_days = school_days − (sick_leave + personal_leave)`
- **เหตุผล** — เด็กที่ลาป่วยทั้งเดือนโดยมีใบรับรองแพทย์ ไม่ควรได้ 0% เพราะไม่ใช่การขาดเรียน
  ตรงกับวิธีที่ครูคิดจริงและกับเกณฑ์ ปพ.6
- **ผลที่ตามมา** — เด็กที่ลาป่วยทั้งช่วงจะได้ 100% ไม่ใช่ 0% · `late` นับเป็น "มา" อยู่ในตัวเศษ
- **⚠️ ห้ามแตะโดยไม่ถาม** — ตัวเลขนี้ขึ้นเอกสารทางการ

### `percent` คืน `null` ไม่ใช่ `0` เมื่อ `basis_days <= 0`

- **เหตุผล** — "ยังวัดไม่ได้" กับ "มาเรียน 0%" เป็นคนละเรื่องกันโดยสิ้นเชิงในสายตาครู
  ถ้าคืน `0` เด็กที่เพิ่งย้ายเข้าจะขึ้นแดงทันทีทั้งที่ยังไม่มีวันเรียนให้วัด
- **ผลที่ตามมา** — **ฝั่ง client ต้องแยกสองกรณีนี้ทุกที่ที่แสดงผล**

### ไม่นับวันที่ครูยังไม่ยืนยันเป็นตัวหาร

- **เลือก** — `measurement_day_dates` = วัน `school_day` **∩** วันที่ confirmed แล้ว
  วันเรียนที่ครูยังไม่ยืนยันไปโผล่เป็น `missing_confirmed_dates` เตือนบนหน้าจอแทน
- **เหตุผล** — **ไม่ลงโทษเด็กเพราะครูยังเช็คไม่เสร็จ** ถ้านับวันที่ยังไม่ยืนยันเป็นตัวหาร
  เปอร์เซ็นต์ของทั้งห้องจะร่วงทุกครั้งที่ครูค้างงาน
- **ผลที่ตามมา** — ตัวหารเป็นสิ่งที่ครูควบคุมได้ผ่านการยืนยันวัน ซึ่งเป็นเจตนา และเป็นเหตุผลว่าทำไม
  ต้องมีบรรทัดบอกขอบเขตบนเอกสาร ปพ.6 (ดูหมวดถัดไป)

### student identity: `student_id` เป็นหลัก `num:` เป็น fallback

- **บริบท** — ข้อมูลรุ่นเก่ามีแค่ "เลขที่" ซึ่ง**ไม่เสถียร** — ครูสลับเลขที่กันได้ นักเรียนออกแล้วมีคนใหม่
  ใช้เลขที่เดิม และเลขที่ซ้ำได้ระหว่าง active/inactive
- **เลือก** — เพิ่มคอลัมน์ `student_id` (immutable) เป็น key หลัก คงเส้นทาง `num:` ไว้เป็น fallback
  พร้อม `pickBestStudentCandidate_` แก้ความกำกวม
- **ไม่เลือกทางไหน** — บังคับให้ทุกแถวต้องมี `student_id` แล้วทิ้งแถวที่ backfill ไม่สำเร็จ
  เพราะนั่นคือการทำข้อมูลเช็คชื่อของครูหายจริง
- **ผลที่ตามมา** — ต้องมี migration + สองเส้นทางตลอดไป แลกกับการไม่ทำข้อมูลใครหาย

### roster bounds เป็น half-open `[start, end)`

- **เลือก** — `inactive_at` เป็น **exclusive** — วันที่ระบุไว้ถือว่าไม่อยู่ในห้องแล้ว
- **เหตุผล** — ครูกรอก "พ้นจากห้อง 1 ก.ย." หมายถึง 31 ส.ค. เป็นวันสุดท้ายที่ยังนับ ตรงกับความรู้สึกของครู
  และทำให้ย้ายเข้า-ออกวันเดียวกันไม่นับซ้อน
- **ผลที่ตามมา** — เวลาแปลงกลับเป็น inclusive ต้อง `shiftDate_(endDate, -1)` — จุดที่ผิดพลาดได้ง่าย

### archive ภาคเรียนเก่า ไม่ลบ

- **เลือก** — ย้ายไปชีตซ่อน `_att_archive_<id>` แล้ว **copy ก่อน ลบทีหลัง** (idempotent)
  และให้ระบบยังอ่านได้ปกติผ่าน `sourceInfo` routing
- **ไม่เลือกทางไหน** — ลบทิ้งจริง เพราะข้อมูลเช็คชื่อเป็นหลักฐานประกอบเอกสารที่ส่งไปแล้ว
- **ผลที่ตามมา** — **ไม่มีฟังก์ชัน unarchive** แต่ไม่จำเป็นเพราะสลับภาคเรียนแล้วอ่านได้เลย ·
  `deleteSemester` ปฏิเสธถ้ายังมีข้อมูล (นับ archive ด้วย) = ลบภาคเรียนที่มีข้อมูลไม่ได้เลย โดยตั้งใจ

### ตัดชีตประวัติแก้ไขที่ 5,000 แถวแบบมี hysteresis

- **บริบท** — เดิมโตปีละ 10,000-20,000 แถวโดยไม่มีการตัดทิ้งเลย
- **เลือก** — เพดาน 5,000 + slack 500 คือยอมให้ล้นถึง 5,500 แล้วค่อยตัดทีเดียว
- **ไม่เลือกทางไหน** — `deleteRows` ทุกครั้งที่เช็คชื่อ เพราะจะจ่ายค่า write ทุกการกดปุ่มของครู
- **ผลที่ตามมา** — ตัดจากบน (เก่าสุดออกก่อน) เสมอ จึงใช้ `getNextChangeLogId_` ที่อ่านแค่แถวล่างสุดได้

### ลบ `AttendanceSummaryService.js` ทิ้งทั้งไฟล์

- **บริบท** — ไม่มีไฟล์ไหนเรียกใช้เลยสักฟังก์ชัน แต่เขียนตรรกะนับและคิดเปอร์เซ็นต์ซ้ำกับของจริง
- **เลือก** — ลบทิ้ง (~1,100 บรรทัดรวมกับ dead code อื่น)
- **เหตุผล** — ตรรกะนับ 2 ชุดที่ไม่มีใครเรียกชุดหนึ่ง คือระเบิดเวลา วันหนึ่งมีคนเรียกผิดชุด
  แล้วตัวเลขบนเอกสารจะไม่ตรงกันโดยไม่มีใครรู้
- **หมายเหตุ** — ฟังก์ชัน public ที่ไม่มีใครเรียกอีก 11 ตัวก็ถูกลบพร้อมกัน เพราะเปิดรับจากอินเทอร์เน็ตฟรีๆ
  (ถ้าจะทำหน้าจัดการลิงก์ผู้ปกครองทีหลัง กู้ 3 ตัวกลางคืนได้จาก commit `d98e708`)

---

## ปฏิทินวันเรียน

### วันนอกปฏิทิน: เตือน + เพิ่มเข้าปฏิทินได้ทันที ไม่บล็อก

- **บริบท** — ครูเช็คชื่อในวันเสาร์ที่มีกิจกรรมจริง แต่วันนั้นไม่อยู่ในปฏิทินวันเรียน
- **เลือก** — เช็คชื่อและยืนยันได้ตามปกติ ขึ้นแถบเตือนพร้อมปุ่ม "นับเป็นวันเรียน" ที่กดแล้วเพิ่มเข้าปฏิทินทันที
- **ไม่เลือกทางไหน** — บล็อกไม่ให้เช็คชื่อ (ขวางงานครูในวันที่มีกิจกรรมจริง) ·
  เช็คได้เงียบๆ โดยไม่เตือน (ครูจะไม่รู้ว่าวันนั้นไม่เข้ารายงาน จนกระทั่งพิมพ์ ปพ.6 แล้วตัวเลขไม่ตรง)
- **ผลที่ตามมา** — ไม่ขวางงาน ไม่ทำข้อมูลสูญ และครูรู้ตัวตั้งแต่ตอนเช็คไม่ใช่ตอนพิมพ์

### กันการแก้ปฏิทินย้อนหลังแบบ **แยกทิศ**

- **บริบท** — ถ้ากันตายตัวว่า "ห้ามแก้ปฏิทินของวันที่ยืนยันแล้ว" ครูจะแก้ความผิดพลาดของตัวเองไม่ได้เลย
- **เลือก** — `shouldBlockSchoolCalendarChange_` กัน**เฉพาะทิศที่ทำข้อมูลหาย** คือเปลี่ยนวันเรียน → วันหยุด
  ในวันที่มีการเช็คชื่อที่ยืนยันแล้ว ส่วนทิศตรงข้าม (เพิ่มเป็นวันเรียน) ผ่านได้เสมอ
- **เหตุผล** — สองทิศนี้ไม่สมมาตร: ทิศหนึ่งทำให้ข้อมูลที่มีอยู่หลุดออกจากรายงาน อีกทิศแค่เพิ่มข้อมูลกลับเข้ามา
- **ผลที่ตามมา** — กติกาขึ้นกับสถานะจริง ไม่ใช่บล็อกตายตัว — ยกเลิกการยืนยันก่อนแล้วลบได้ทันที ·
  ฟังก์ชันถูกแยกออกมาเพื่อให้ทดสอบผ่าน `Diagnostics.js` ได้ (ขับ `<select>` ใน cross-origin iframe ไม่ได้)

---

## ตัวเลขที่ระบบชูให้ครูเห็น

### "ต้องติดตาม" = มาเรียนต่ำกว่า 80% เท่านั้น

- **บริบท** — เดิมมี **3 นิยามที่ไม่ตรงกันเลย** อยู่คนละที่: หน้าแรกใช้ (ขาด+สาย) ≥ ค่าที่ครูตั้ง ใน 30 วันล่าสุด ·
  ชิปหน้ารายงานใช้ ต่ำกว่า 80% **หรือ ขาด > 0** หรือ สาย > 1 · หน้าพิมพ์ใช้ ขาด ≥ ค่าที่ครูตั้ง หรือต่ำกว่า 80%
  → เงื่อนไข `ขาด > 0` ตัวเดียวทำให้ **37 จาก 40 คน** ติดร่างแห
- **เลือก** — รวมเหลือนิยามเดียวคือ **ต่ำกว่า 80%** ซึ่งเป็นเกณฑ์เดียวกับ ปพ.6 ที่ครูต้องส่งอยู่แล้ว
  บวกเงื่อนไขว่าต้องมีวันวัดผล ≥ 10 วันก่อน · และเปลี่ยนชื่อรายการ 30 วันบนหน้าแรกเป็น
  **"เริ่มขาด/สายบ่อยช่วงนี้"** เพราะมันตอบคำถามคนละข้อและมีประโยชน์จริง
- **ไม่เลือกทางไหน** — แค่ปรับค่าเกณฑ์ให้เข้มขึ้น เพราะปัญหาไม่ใช่ "เกณฑ์หลวมไป" แต่คือ
  **คำเดียวกันหมายถึงคนละอย่างใน 3 ที่** ครูที่เปิด 3 หน้าจะได้รายชื่อ 3 ชุด
- **ผลที่ตามมา** — ชิปลดจาก 37 เหลือ 1 และตรงกับที่ไฮไลต์บน ปพ.6 พอดี ·
  ครูไม่ต้องจำสองมาตรฐาน · เงื่อนไขขั้นต่ำ 10 วันกันไม่ให้ต้นเทอม (ตัวหารน้อย) ทำให้ทั้งห้องติดร่างแห
- **⚠️ แก้เฉพาะตัวกรองฝั่ง client** — `is_below_threshold` ฝั่ง server ไม่ถูกแตะ เพราะคุมไฮไลต์แดงบน ปพ.6 ·
  ค่าที่ครูตั้งไว้เดิม (`attention_threshold_days`) ไม่ถูกแตะ แค่หดขอบเขตให้เหลือเฉพาะรายการ 30 วัน

### ตัวเลขใหญ่บนหน้าแรกนับเฉพาะ "ขาด"

- **บริบท** — วงแหวน "เช็คแล้ว 100%" มีความหมายเฉพาะช่วงที่ยังเช็คไม่เสร็จ พอเสร็จแล้วมันค้างอยู่อย่างนั้น
  ทั้งวันโดยไม่บอกอะไรอีกเลย
- **เลือก** — ปรับตามสถานการณ์: ยังเช็คไม่ครบ → วงแหวนความคืบหน้าเหมือนเดิม ·
  เช็คครบแล้วมีคนขาด → **"N คนขาดวันนี้"** พร้อมบรรทัดรอง สาย/ลาป่วย/ลากิจ · ไม่มีใครขาด → "มาครบทุกคน"
- **ไม่เลือกทางไหน** — นับรวมลาป่วย/ลากิจในตัวเลขใหญ่ เพราะ **"ขาด" คือสิ่งเดียวที่ครูต้องลงมือทำต่อวันนี้**
  (โทรหาผู้ปกครอง) ส่วนเด็กที่ลา ครูรู้อยู่แล้วและไม่ต้องทำอะไร — แต่ยังแสดงในบรรทัดรองเพื่อไม่ให้ตกหล่น

### เอกสาร ปพ.6 ไม่มีคำเตือนสีแดง แต่มีบรรทัดขอบเขต + บล็อกก่อนพิมพ์

- **บริบท** — เดิมพิมพ์กรอบเตือนสีแดงลงบนตัวเอกสารเมื่อข้อมูลยังไม่ครบ
- **เลือก** — เอาคำเตือนออกจากตัวเอกสาร ใส่**บรรทัดบอกขอบเขตด้วยสีปกติ**แทน
  ("คำนวณจากวันเรียนที่ยืนยันแล้ว N วัน จากทั้งหมด M วัน") และย้ายการเตือนไปเป็น
  **การบล็อกตั้งแต่ก่อนกดพิมพ์** เมื่อมีวันที่ยืนยันแล้วแต่หลุดปฏิทิน
- **เหตุผล** — เอกสารนี้ส่งโรงเรียน คำเตือนสีแดงบนเอกสารราชการทำให้ครูเสียหน้าและตีความได้ว่าข้อมูลผิด
  แต่การลบทิ้งเฉยๆ ก็ทำให้คนอ่านไม่รู้ว่าตัวเลขคำนวณจากฐานไหน — บรรทัดขอบเขตให้ข้อเท็จจริงโดยไม่ตัดสิน
- **ผลที่ตามมา** — ครูแก้ปัญหาได้ก่อนกระดาษออกจากเครื่องพิมพ์ แทนที่จะพิมพ์เสร็จแล้วต้องพิมพ์ใหม่

---

## การส่งมอบ

### `restoreBackup` เป็นช่องทางอัปเดตโค้ดช่องทางเดียว

- **บริบท** — ครูที่ซื้อไปแล้วมีสำเนาของตัวเอง push โค้ดให้ไม่ได้
- **ผลที่ตามมา** — **ห้ามทำ `restoreBackup` พัง** และมันต้องเรียก `ensureSystemSheets_` ไม่ใช่ `setupSystem_`
  (ตัวหลังใช้ `getUi()` ซึ่ง throw เมื่อรันจาก Web App) · rollback snapshot ต้องอยู่ใน `try` ให้ครอบทั้งเส้นทาง
- **ยังไม่ได้ทดสอบทั้งเส้น** — เป็นงานค้างที่ระบุไว้ใน `TODO.md`

### เครื่องมือ dev ต้องถอดก่อนส่งมอบทุกครั้ง

- **เลือก** — เก็บ `Diagnostics.js` และ `SeedTestData.js` ไว้ใน repo แต่ลบก่อนส่งมอบ
  (มี comment กำกับทั้งในไฟล์และใน `onOpen`)
- **ไม่เลือกทางไหน** — ใส่ flag เปิด/ปิดในโค้ด เพราะ flag ที่ลืมปิดอันตรายกว่าไฟล์ที่ลืมลบ
  (ไฟล์ที่ลืมลบยังมีด่าน local context กันอยู่ แต่ยังไม่ควรอยู่ในมือลูกค้า)
- **ผลที่ตามมา** — `SeedTestData.js` มีด่านชั้นที่สำคัญที่สุดคือ `SEED_MAX_EXISTING_ATTENDANCE_ROWS = 500`
  ที่ปฏิเสธการรันถ้าชีตมีข้อมูลเกิน 500 แถว — กันไม่ให้เผลอรันทับสำเนาลูกค้าจริง

---

## UI

### `table-layout: fixed` ในตารางรายวัน — บันทึก 3 ทางที่ลองแล้วไม่ได้ผล

- **อาการ** — ตารางรายวันมีคอลัมน์ตรึง 2 คอลัมน์ แล้วมีช่องว่างเหลื่อมระหว่างคอลัมน์
- **ทางที่ลองแล้วไม่ได้ผล** — (1) แก้ `z-index` ของ `thead` · (2) เปลี่ยน `border-collapse: collapse`
  เป็น `separate` · (3) ล็อกความกว้างคอลัมน์แรกให้เท่ากับ `left` ของคอลัมน์ที่สองด้วยตัวแปร CSS
- **ต้นเหตุจริง** — **`table-layout: auto` ทำให้ `width` / `max-width` บนเซลล์เป็นแค่ข้อเสนอ**
  ค่าที่ตั้งไว้ไม่มีผลจริง จึงล็อกความกว้างไม่ได้ และ `left` ของ sticky column ก็เชื่อถือไม่ได้ตามไปด้วย
- **ทางที่ได้ผล** — เปลี่ยนเป็น `table-layout: fixed` **ก่อน** แล้วค่อยคุมความกว้างด้วยตัวแปรเดียว
- **ของแถม** — ชื่อนักเรียนเลิกถูกตัดด้วย `...`

### render แบบ full `innerHTML` replace

- **เลือก** — ยอมรับว่าเป็นแบบนี้ ไม่แปลงเป็น framework หรือ virtual DOM
- **เหตุผล** — ไม่มี build step ให้ใช้ และการเขียนใหม่ทั้งระบบมีความเสี่ยงสูงกว่าประโยชน์
- **ผลที่ตามมา** — focus / scroll / ค่าใน `<input>` ที่ยังไม่ commit หายทุกครั้งที่ render
  จึงต้องมี **partial render 3 จุดเป็นข้อยกเว้น** (`refreshAttendanceList_`, `recalcSummary_`,
  `refreshSchoolCalendarList_`) ซึ่งเกิดจากเจอปัญหาจริง ไม่ใช่การ optimize ล่วงหน้า
- **ทบทวนเมื่อ** — ถ้าจำนวนจุด partial render โตเกิน ~6 จุด แปลว่าถึงเวลาคิดใหม่

### 3 หน้าไม่แชร์โค้ดกัน

- **เลือก** — หน้าครู / ผู้ปกครอง / พิมพ์ เป็นแอปแยกกันสิ้นเชิง
- **เหตุผล** — คนละ auth model · ไม่ควรส่งโค้ดครูทั้งชุดไปให้ผู้ปกครองบน deployment ที่เป็น anonymous ·
  และ 2 หน้าหลังไม่ผ่าน template engine จึงใช้ `include_` ไม่ได้โดยบังคับ
- **ราคาที่จ่ายและยอมรับแล้ว** — `esc()` มี 9 ชุด, icon renderer หลายชุด, `formatPercentLabel` 4 ชุด,
  การหาวันที่ Bangkok 3 ชุด (รายละเอียดใน ARCHITECTURE ข้อ 10)
- **ทบทวนเมื่อ** — ถ้าจะรวม `esc()` ให้ใช้เวอร์ชันที่เข้มที่สุด (`JavaScript.html`) เท่านั้น

---

## ภาคผนวก — ข้อที่รู้แล้วแต่ยังไม่ตัดสินใจ

รายการนี้คือของที่เจอแล้ว ยืนยันกับโค้ดแล้ว แต่**ยังไม่ได้เลือกทาง** อย่าถือว่าเป็นการตัดสินใจ

| เรื่อง | สภาพปัจจุบัน |
|---|---|
| โค้ดโมดูล client ถูก cache 900 วิ โดยไม่มีทางล้าง | หลัง push ต้องรอถึง 15 นาทีถึงเห็นของใหม่ (ARCHITECTURE ข้อ 8) |
| CSS ถูกโหลดซ้ำสองรอบ | inline ครบตั้งแต่ `doGet` แล้วยังถูก lazy fetch ซ้ำ ต้องเลือกทางใดทางหนึ่ง |
| `esc()` 9 ชุด 2 พฤติกรรม | ยังไม่เป็นช่องโหว่จริงเพราะ `normalizeLimitedText_` กันที่ชั้น validation |
| `saveSetting_` ไม่ bump derived version | ขณะที่ `saveSettings` bump — ไม่สม่ำเสมอ |
| `.clasp.json` ถูก track ใน git และไม่มี `.gitignore` | scriptId อยู่ใน repo |
| `appsscript.json` ไม่ประกาศ `oauthScopes` | หน้าจอขอสิทธิ์ที่ครูเห็นกว้างเกินจำเป็น (อยู่ใน `TODO.md`) |

---

## 25. archive ล้มกลางคัน — ไม่ย้อนชีต archive ออก

**บริบท** — `archiveSemesterAttendance` คัดลอกไปชีตซ่อนก่อน แล้วค่อยลบจากชีตหลัก
ถ้าการลบ throw กลางคัน จะเหลือสถานะที่ข้อมูลอยู่ทั้งสองที่

**เลือก** — จับ error แล้วรายงานว่าข้อมูลยังครบและกดซ้ำได้ · **เก็บแถวในชีต archive ไว้เสมอ**

**ทางที่ไม่เลือกและทำไม** — TODO เดิมเสนอให้ "ครอบด้วย rollback" คือย้อนแถวที่ copy ไปแล้วออก
**อันตรายกว่าปัญหาที่จะแก้**: `deleteSheetRowsByIndexes_` ลบเป็นบล็อกไล่จากล่างขึ้นบน
ถ้าล้มกลางคันแปลว่าชีตหลักถูกลบไปแล้ว**บางส่วน** การย้อนชีต archive ออกตอนนั้น
จะทำให้แถวที่ลบไปแล้ว**หายทั้งคู่** — ชีต archive คือตาข่ายรองของข้อมูลที่ไม่มีที่อื่นแล้ว
กติกาคือ **ยอมให้มีข้อมูลซ้ำสองที่ ดีกว่าเสี่ยงให้ข้อมูลหาย**

**ผลที่ตามมา** — ระหว่างที่ค้าง ข้อมูลจะอยู่ทั้งชีตหลักและชีต archive
ทุกเส้นทางที่ผ่าน `getUniqueLatestRecords_` จะยุบให้เหลือแถวเดียวอยู่แล้ว (กติกาแถวหลังชนะ)
แต่การนับแถวดิบจะเห็นซ้ำ · แก้ได้ด้วยการกดปุ่มเก็บข้อมูลซ้ำจนสำเร็จ

**เงื่อนไขที่ควรทบทวน** — ถ้าวันหนึ่งทำ transaction จริงได้ (เช่น เขียนชีตชั่วคราวแล้วสลับชื่อ)
ก็ไม่ต้องยอมรับสถานะซ้ำอีก

## 26. `isSemesterAttendanceArchived_` แยกเป็นสองตัว

**บริบท** — ฟังก์ชันเดียวถูกใช้ตอบ 3 คำถาม: ต้องอ่านชีต archive ไหม (routing) ·
เก็บถาวรเสร็จหรือยัง (ป้ายและปุ่มบนหน้าจอ) · ยังมีข้อมูลอยู่ไหม (guard ตอนลบภาคเรียน)
คำตอบเดิมคือ "ชีต archive ไม่ว่าง"

**เลือก** — เพิ่ม `isSemesterAttendanceFullyArchived_` สำหรับหน้าจอโดยเฉพาะ
= archive มีของ **และ** ชีตหลักไม่เหลือของภาคเรียนนี้แล้ว

**ทางที่ไม่เลือกและทำไม** — เปลี่ยนนิยามของตัวเดิมให้เป็น "ชีตหลักหมดแล้ว" ทั้งหมด
จะพัง routing ทันที เพราะระหว่าง archive ค้างครึ่งทาง ระบบจะเลิกอ่านชีต archive
ทั้งที่ข้อมูลบางส่วนอยู่ในนั้นแล้ว · และ guard ตอนลบภาคเรียนจะยอมให้ลบภาคเรียน
ที่ข้อมูลอยู่ในชีต archive ได้

**ผลที่ตามมา** — หน้าจอต้องอ่านคอลัมน์วันที่ของชีตหลักเพิ่ม `getSemesterRows_`
จึงอ่านครั้งเดียวแล้วส่งต่อให้ทุกภาคเรียนใช้ร่วมกัน

**เงื่อนไขที่ควรทบทวน** — ถ้าเพิ่มคำถามที่ 4 ให้ตั้งฟังก์ชันใหม่ อย่าเอาตัวเดิมไปตอบอีก

---

## 27. CSS ส่งทางเดียว — เลือก inline ทิ้ง lazy

**บริบท** — `StyleReport` / `StylePhase3` / `StylePhase4` ถูกส่ง **สองทางพร้อมกัน**:
`include_` ตอน `doGet` และ `getClientStyleContent` แบบ lazy ที่ cache 900 วินาที

**เลือก** — เหลือ `include_` ทางเดียว ลบ `getClientStyleContent` / `extractClientStyleContent_` /
`CLIENT_STYLE_REGISTRY` / `PAGE_STYLE_MAP` ทิ้ง

**ทางที่ไม่เลือกและทำไม** — ตัด `include_` ออกแล้วเหลือ lazy อย่างเดียว จะได้หน้าแรกเบาลง
แต่ **เสี่ยง FOUC** เพราะ CSS มาช้ากว่า HTML และ `StyleReport`/`Phase3`/`Phase4` ใช้ `var(--...)`
โดยไม่นิยามเอง ต้องพึ่ง `Stylesheet` ที่ต้องมาก่อนเสมอ — ลำดับที่พึ่ง network จึงเปราะ
ที่สำคัญกว่านั้น **ไม่ได้ประหยัดอะไรเลยในทางปฏิบัติ** เพราะเดิมส่งทั้งสองทางอยู่แล้ว
การตัด lazy ออกจึงลดงานลงล้วนๆ โดยขนาดหน้าแรกเท่าเดิม

**ผลที่ตามมา** — **แก้ CSS แล้วเห็นผลทันที ไม่ติด cache 15 นาทีอีกต่อไป**
เหลือเฉพาะ `Js*` 6 ตัวที่ยังติด · ฟังก์ชัน public ลดไป 1 ตัว

**เงื่อนไขที่ควรทบทวน** — ถ้าวันหนึ่ง CSS รวมกันใหญ่จนหน้าแรกช้าจนครูรู้สึกได้
ค่อยกลับมาทำ lazy **แต่ต้องเอา `include_` ออกพร้อมกัน** ไม่งั้นกลับไปมีสองทางเหมือนเดิม

## 28. `esc()` รวมเป็นพฤติกรรมเดียว

**บริบท** — มี `esc()` อยู่ 9 ไฟล์ แยกเป็น 3 พฤติกรรม: บางตัวใช้ `if (!str) return ''`
(ทำให้ **เลข `0` กลายเป็นสตริงว่าง**) บางตัว escape `"` `'` บางตัวไม่ escape

**เลือก** — ให้ทุกตัวเหมือน `JavaScript.html` คือเช็ค `null`/`undefined` เท่านั้น และ escape `"` `'` ด้วย

**ทางที่ไม่เลือกและทำไม** — ทำเป็นฟังก์ชันกลางตัวเดียวแล้วให้ทุกโมดูลเรียก
`ParentView.html` / `PrintReport.html` เป็นหน้า standalone ที่ไม่โหลด `JavaScript.html`
จึงแชร์ไม่ได้จริง และการบังคับผ่าน `AppShared` จะเพิ่มการพึ่งพาให้โมดูลที่ตั้งใจให้แยกกันได้

**ผลที่ตามมา** — `esc(0)` คืน `'0'` แทน `''` (ถูกขึ้น ไม่มีทางแย่ลง) และค่าที่ยัดลง attribute
ปลอดภัยขึ้นทุกไฟล์ · ยังต้องคัดลอกโค้ดซ้ำ 9 ที่ ถ้าแก้ต้องแก้ให้ครบ

## 29. แยกประเภท error ด้วย code หรือธง — และเรียงลำดับการแก้จากปลายทางก่อน

**ปัญหา**: หลายจุดฝั่งหน้าจอแยกประเภท error ด้วยการค้นคำไทยในข้อความ
(`indexOf('โมดูล')` · `indexOf('หมดอายุ')` · `indexOf('วันหยุดในปฏิทินวันเรียน')`)
ทั้งที่ protocol `CODE|ข้อความ` มีอยู่แล้ว ผลคือ**แก้ถ้อยคำ error ฝั่ง server
แล้วตรรกะควบคุมพังโดยไม่มี error ให้เห็น**

**ทางที่เลือก** — แยกเป็น 3 ชั้นตามที่มาของ error

1. **error ที่เกิดฝั่ง client เอง** → ติดธงที่จุดโยน ไม่ใช่ให้ปลายทางเดา
   `moduleLoadError_` ติด `__source = 'deps'` ให้ทุกจุด แล้ว `loadPage` อ่านธงเดียว
2. **error ที่มี code จาก server อยู่แล้ว** → อ่าน code ตรงๆ
   `PRINT_ERROR_KIND_BY_CODE_` และ `describeParentAuthError_` แปลง code → ชนิดที่หน้าจอใช้
3. **error ที่ยังไม่มี code** → คงการค้นคำไทยไว้ **แต่ต้องเขียนกำกับว่าผูกกับข้อความไหน
   ที่ไฟล์ไหนบรรทัดไหน** และครอบด้วย `!parsed.code` ให้ code ชนะเสมอเมื่อวันหนึ่งมันมา

**ทางที่ทิ้ง — เติม `CODE|` ที่ฝั่ง server ก่อน**
ดูเหมือนตรงจุดกว่า แต่**อันตราย** เพราะจุดแสดงผลบางจุดเอาข้อความดิบไปโชว์
เติม code ก่อนจะทำให้ผู้ใช้เห็น `DATE_IS_HOLIDAY|...` บนจอทันที
→ **ลำดับที่ถูกคือปลายทางก่อน ต้นทางทีหลัง** ทำปลายทางเสร็จแล้วการเติม code
ที่ฝั่ง server กลายเป็นงานที่ปลอดภัยและทำทีละข้อความได้

**ทางที่ทิ้ง — ทำ error class / ตัวกลางร่วมกัน 3 หน้า**
`Index.html` · `ParentView.html` · `PrintReport.html` ไม่แชร์โค้ดกันโดยเจตนา (ดูข้อ "3 หน้าไม่แชร์โค้ดกัน")
การทำตัวกลางต้องเจาะกำแพงนั้น แลกกับการลดโค้ดซ้ำ ~15 บรรทัด **ไม่คุ้ม**
เลือกเขียนตัว parse สั้นๆ ในแต่ละหน้าแทน แบบเดียวกับที่ทำกับ `esc()` (ข้อ 28)

**ผลพลอยได้ที่เป็นบั๊กจริง**: `ParentView` ยิง `google.script.run` ตรงๆ ไม่ผ่าน `serverCall`
`err.message` จึงเป็นค่าดิบ และหน้านั้นเอาไปแสดงทั้งก้อน
→ **ผู้ปกครองที่ลิงก์หมดอายุเห็น `PARENT_AUTH_EXPIRED|ลิงก์สำหรับเปิดข้อมูลหมดอายุ...` บนจอ**
เป็นหน้าเดียวที่เปิดสาธารณะแบบ anonymous จึงเป็นจุดที่แย่ที่สุดที่จะหลุด

### ★ แก้ข้อสรุปข้างบน — "ปลายทาง" กว้างกว่าที่เขียนไว้ตอนแรก (17 ส.ค. 2569 รอบสอง)

ตอนเขียนข้อนี้ครั้งแรกสรุปว่า *"จุดแสดงผลถอด code ออกได้ครบแล้ว จึงเติม code ฝั่ง server ได้เลย"*
**ไม่จริง** เพราะนับแต่เส้นทางของ error ที่ถูก `throw` ออกไป

ยังมีเส้นทางที่สองที่มองข้ามไป: **ฝั่ง server จับ error เองแล้วก๊อป `e.message`
ใส่ payload กลับมา** ในรูป `{ success: false, message: e.message }` **27 จุด**
ฝั่ง client เอา `data.message` ไป toast ตรงๆ ไม่ได้ผ่าน `parseServerError_`
(ตัวนั้นทำงานกับ error ที่ throw เท่านั้น) → เติม code แล้วโค้ดหลุดขึ้นจอทาง payload ทันที

**สิ่งที่ทำเพิ่ม**: `splitErrorCode_` + `buildFailurePayload_` [Utils.js]
แทนที่ `{ success: false, message: e.message }` ทั้ง 27 จุด
payload ได้ `code` แยกฟิลด์มาด้วย หน้าจอจึงแยกประเภทได้โดยไม่ต้องแตะข้อความ

`splitErrorCode_` ยอมรับเป็น code เฉพาะ `^[A-Z][A-Z0-9_]*$` **ไม่ใช่แค่เจอ `|` แล้วตัด**
เพราะข้อความปกติอาจมี `|` อยู่ตามธรรมชาติ แล้วจะถูกตัดหัวหายโดยไม่ได้ตั้งใจ

**บทเรียนที่ใช้ซ้ำได้**: ก่อนเปลี่ยนรูปแบบข้อความที่ข้ามพรมแดน server → client
ต้องไล่ให้ครบ **ทั้งเส้นทาง throw และเส้นทาง payload** การนับแค่เส้นทางเดียวทำให้
มั่นใจผิดว่าปลอดภัยแล้ว

## 30. ยุบ error handling ของ RPC เฉพาะในไฟล์เดียว ไม่ยุบข้ามหน้า

**ปัญหา**: `serverCall` · `serverCallQuiet` · `fetchPageInitialData_` (ทั้งสามอยู่ใน
`JavaScript.html`) กับ `handlePrintFailure` (`PrintReport.html`) ก๊อปตรรกะจัดการ error
ของ RPC ไว้คนละชุด **ท่อนแปลงข้อความ 4 สาขาเหมือนกันเป๊ะ 3 ที่**

**ทางที่เลือก — ยุบ 3 ตัวในไฟล์เดียวกัน ปล่อยตัวที่สี่ไว้**
แยกเป็น `isTeacherSessionExpiredCode_` · `handleTeacherSessionExpired_` ·
`humanizeServerError_` · `invokeFailureHandler_` แล้ว**คงหางที่ต่างกันจริงไว้ที่เดิม**
(toast / เงียบ / reject promise) เพราะความต่างนั้นคือเหตุผลที่มี 3 ตัวแต่แรก
→ ลดรวม 157 บรรทัดเหลือ 103

**ทางที่ทิ้ง — ยุบ `handlePrintFailure` เข้ามาด้วย**
`Index.html` · `ParentView.html` · `PrintReport.html` ไม่แชร์โค้ดกันโดยเจตนา
(ดูข้อ "3 หน้าไม่แชร์โค้ดกัน") การทำตัวกลางต้องเจาะกำแพงนั้น แลกกับการลดโค้ดซ้ำไม่กี่บรรทัด
**ไม่คุ้ม** และหน้าพิมพ์มีความต้องการต่างจริง: มันแปลง code → **ชุดข้อความทั้งชุด**
(หัวข้อ + คำอธิบาย + สิ่งที่ควรทำต่อ) ไม่ใช่แปลงเป็นข้อความเดียวแบบฝั่งครู
ตรวจแล้วว่าไม่มีช่องข้อความดิบหลุด เพราะ `showError` ไม่เอา message ไปแสดงเลย
เป็นแนวเดียวกับที่ทำกับ `esc()` (ข้อ 28) และตัว parse error (ข้อ 29)

**สิ่งที่ตั้งใจไม่ทำ — ไม่ยุบ 3 สาขาที่ค้นคำอังกฤษ (permission / Timeout / quota)
ให้ไปเทียบ code** เพราะเป็นข้อความของ Apps Script และ Google เอง **ไม่ใช่ของเรา
จึงไม่มี `CODE|` ให้เทียบ** เขียนกำกับไว้ใน `humanizeServerError_` แล้วว่าอย่า "แก้"

## 31. เช็ค `expires_at` ฝั่ง client เฉพาะตอนอ่าน session กลับ ไม่กั้นทุก request

**ปัญหา**: `expires_at` ถูกส่งลงหน้าครูและเก็บใน `sessionStorage` **แต่ไม่มีใครเทียบเลย**
เปิดหน้าใหม่หลังทิ้งไว้นาน ครูเห็นจอกำลังโหลดค้างหลายวินาทีแล้วค่อยเด้งไปหน้าเข้าสู่ระบบ
เพราะต้องยิง request ให้ server ตอบ `AUTH_EXPIRED` ก่อนถึงจะรู้
(`ParentView.html` เช็คตั้งแต่ตอนอ่านกลับอยู่แล้ว = มีแบบอย่างในโปรเจกต์)

**ทางที่เลือก — เช็คจุดเดียวคือ `initializeTeacherAuth_`**
ทิ้ง session ที่หมดอายุแล้วทันที แล้วขึ้นหน้าเข้าสู่ระบบพร้อมบอกเหตุผล
*"เซสชันหมดอายุแล้ว กรุณาใส่รหัสครูเพื่อเข้าใช้งานอีกครั้ง"* ไม่ใช่โยนฟอร์มเปล่าให้เดา

**ทางที่ทิ้ง — กั้นทุก request ใน `serverCall`**
ดูรอบคอบกว่า แต่**แลกความเสี่ยงผิดทาง**: การเทียบใช้ `Date.now()` ของเครื่องผู้ใช้
ถ้านาฬิกาเครื่องครูเพี้ยนไปข้างหน้าเป็นชั่วโมง ทุก request จะถูกกั้นตั้งแต่ฝั่ง client
= **ใช้งานไม่ได้เลย** ขณะที่การเช็คแค่ตอนอ่านกลับ อาการแย่สุดคือต้องล็อกอินใหม่ครั้งเดียว
แล้วการล็อกอินก็เขียน `expires_at` ชุดใหม่ทับให้เอง
ตัวตัดสินจริงยังเป็น `AUTH_EXPIRED` จาก server ซึ่งเชื่อถือได้กว่านาฬิกาเครื่อง
และ `handleTeacherSessionExpired_` (ข้อ 30) รับกรณีหมดอายุระหว่างใช้งานไว้อยู่แล้ว

**ทางที่ทิ้ง — ตั้ง timer เด้งออกเองเมื่อถึงเวลา**
session ครูเป็น sliding (`touchTeacherSession_` ต่ออายุทุกครั้งที่ผ่านด่าน)
`expires_at` ที่ client ถืออยู่จึงล้าสมัยได้ตลอดเวลา timer จะเด้งครูออกกลางงาน
ทั้งที่ฝั่ง server ต่ออายุไปแล้ว

**รายละเอียดที่ต้องไม่ลดรูป**: **ไม่มี `expires_at` ให้ถือว่ายังไม่หมดอายุ**
session ที่เก็บไว้จากโค้ดเวอร์ชันก่อนไม่มีฟิลด์นี้ ถ้าตีเป็นหมดอายุ
ครูทุกคนจะถูกเตะออกหนึ่งครั้งตอนอัปเดตโค้ดโดยไม่มีเหตุผล

## 32. แก้ความช้าหน้าเช็คชื่อด้วยการเลิก cache ที่ผูกช่วงวันที่ ไม่ใช่เลิก cache

**ที่วัดได้** (`perf:attendance_vs_report` [Diagnostics.js] 17 ส.ค. 2569 · ครั้งแรกที่ cache เย็น)
`raw_records_ms` **3,738** · `pf_calendar_prime_ms` **808** · `students_ms` 665 ·
`records_day_status_ms` 367 · `alerts_ms` 338 · `pf_settings_ms` 279 · **`rows_ms` 1**
→ `rows_ms=1` บอกชัดว่า**ไม่ใช่ปัญหาการประมวลผล เป็นการดึงข้อมูลล้วนๆ**

**สองต้นเหตุที่แก้ และทั้งคู่เป็นเรื่องเดียวกัน: cache ที่ถูกทำให้ใช้ไม่ได้**

1. **ปฏิทิน** — `getSchoolCalendarEntriesForRange_` cache ด้วยคีย์ `[from, to]`
   แต่ผู้เรียกหลักส่งช่วง `date-35` ถึง `date` → **เปลี่ยนวันที่ = คีย์ใหม่ = miss 100%**
   และ builder อ่านทั้งชีตอยู่แล้ว **cache ที่ผูกช่วงจึงไม่ได้ประหยัดอะไรเลย**
   → แยกเป็น `getAllSchoolCalendarEntries_` (คีย์ไม่ผูกช่วง) แล้วกรองในหน่วยความจำ
2. **เรคอร์ดรายวัน** — สแกนคอลัมน์วันที่ทั้งชีต **ต่อหนึ่งวัน** ต้นทุนโตตามขนาด archive
   ขณะที่ `attendance_date_buckets` อ่านทั้งชีตครั้งเดียวแล้วใช้ได้ทุกวัน
   → ให้เส้นทางรายวันเสิร์ฟจากถัง

**ทางที่ทิ้ง — แคบช่วง prime ปฏิทินให้เล็กลง**
เช่น prime แค่วันเดียวแทน 35 วัน จะลดเวลาอ่านได้บ้าง แต่ **ไม่แก้ต้นเหตุ**
ยังคง miss ทุกครั้งที่เปลี่ยนวันที่ และ 35 วันนั้นจำเป็นสำหรับ alert ขาดติดกัน

**ทางที่ทิ้ง — เพิ่ม TTL ให้ยาวขึ้น**
ไม่ช่วยเลย เพราะปัญหาไม่ใช่ของหมดอายุ แต่เป็น**คีย์ไม่เคยซ้ำ**

**★ ราคาที่ยอมจ่าย และต้องรู้ไว้**
หน้าเช็คชื่อซึ่งครูใช้ทุกเช้า **ไปผูกกับ `attendance_date_buckets` ซึ่งเป็น cache ก้อนใหญ่สุด**
(10,000 แถว ≈ 1.5 MB ≈ 19 chunk ต้องอยู่ครบถึงจะ hit) ถ้าข้อมูลโตจน chunk หลุดบ่อย
**อาการช้าจะกลับมาที่หน้านี้ก่อนหน้าอื่น** ยอมจ่ายเพราะ (ก) หน้ารายงานพึ่งถังนี้อยู่แล้ว
ระบบจึงไม่ได้มีจุดเปราะใหม่ (ข) archive ภาคเรียนเป็นทางระบายที่มีอยู่แล้ว
(ค) `readAttendanceRecordsByDate_` ยังอยู่ครบ เปลี่ยนกลับได้ที่จุดเดียว

**สิ่งที่ตรวจก่อนเปลี่ยน ไม่ได้เดา**
ลำดับแถวตรงกัน (ทั้งสองใช้ `getAttendanceReadSheets_` archive ก่อนชีตหลัก) ·
`sheet_name`/`row_index` มีครบในถัง · ไม่มีใครพึ่ง `student_key` จริง
(`getRecordStudentKey_` ปั้นจาก `student_id`/`student_number`) ·
`invalidateSchoolCalendarCaches_` และ `invalidateAttendanceCaches_` เรียก
`bumpDerivedDataCacheVersion_` ซึ่งล้าง derived cache **ทุกคีย์** รวมคีย์ใหม่

## 33. สำรองชีต `ลิงก์ผู้ปกครอง` ด้วย — แต่ล้างคอลัมน์ token ทิ้งก่อนเสมอ

**ปัญหา**: `buildBackupSnapshot_` สำรอง 7 ชีตแต่**ไม่มี `ลิงก์ผู้ปกครอง`**
ซึ่งสำคัญเพราะ `restoreBackup` คือช่องทางเดียวที่ครูซึ่งซื้อไปแล้วจะได้โค้ดเวอร์ชันใหม่
= ครู copy Spreadsheet+Script ชุดใหม่แล้วกู้คืนข้อมูลเข้าไป
→ **สำเนาใหม่ไม่มีลิงก์ผู้ปกครองเลย ลิงก์ทุกใบที่แจกไปแล้วตายทันที**
และครูต้องออกใหม่ทีละคนโดยไม่มีอะไรบอกว่าต้องทำ

**ทางที่เลือก — สำรองด้วย แต่ล้างคอลัมน์ `token` ทิ้งก่อนใส่ไฟล์**
ปกติคอลัมน์นั้นว่างอยู่แล้ว (ระบบเก็บแค่ `token_hash` ส่วน token จริงโชว์ครั้งเดียวตอนออกลิงก์)
แต่**ไฟล์สำรองเป็นของที่ครูดาวน์โหลดไปเก็บและอาจส่งต่อ** ถ้าวันหนึ่งมี token หลงเหลือในชีต
มันจะกลายเป็นกุญแจเปิดข้อมูลนักเรียนที่อยู่ในไฟล์เดียวกัน จึงล้างซ้ำอีกชั้น
การกู้คืนไม่ต้องใช้คอลัมน์นี้เพราะตรวจด้วย hash

**ทางที่ทิ้ง — ไม่สำรอง แล้วเขียนเตือนบนหน้าจอว่าต้องออกลิงก์ใหม่**
ปลอดภัยกว่าในทางทฤษฎี แต่โยนงานที่เครื่องทำได้ให้ครูทำมือทีละคน
และ**ครูจะรู้ตัวก็ต่อเมื่อผู้ปกครองทักมาว่าลิงก์เปิดไม่ได้**

**★ ไฟล์สำรองรุ่นเก่า (v3) ต้องข้ามชีตนี้ ห้ามเขียนทับ**
`restoreBackupSheet_` เขียนทับด้วยข้อมูลว่างถ้า snapshot ไม่มีคีย์นั้น
→ กู้คืนไฟล์เก่าจะ**ล้างลิงก์ที่ใช้งานอยู่ทิ้งทั้งหมด** จึงครอบด้วย `if (sheets.parent_links)`
และ `BACKUP_VERSION` ขึ้นเป็น 4 เพื่อให้แยกรุ่นได้ (ยังกู้คืน v3 ได้ตามปกติ)

**ของแถมที่เจอระหว่างทาง — คำสัญญาบนหน้าจอมี 2 ชุด**
ฝั่ง client เขียนประโยค "จะเขียนทับอะไรบ้าง" ของตัวเอง **ไม่ได้ใช้ `data.note` ที่ server ส่งมาเลย**
= ต้องแก้ 2 ที่ทุกครั้งและตกหล่นมาแล้วครั้งหนึ่ง → เปลี่ยนให้ client แสดง `data.note`
ซึ่งทำให้ข้อความต่างกันตามรุ่นไฟล์ได้ด้วย (v3 บอกว่าลิงก์ที่ใช้อยู่จะไม่ถูกแตะ)

## 34. Chart.js — คง CDN ไว้ แต่ใส่ SRI แทนการเอาไฟล์มาไว้ในโปรเจกต์

**สภาพ**: `chart.umd.min.js` จาก cdnjs เป็น **dependency ภายนอกตัวเดียวของทั้งโปรเจกต์**
ถ้าเครือข่ายโรงเรียนบล็อก กราฟบนหน้าแรกหายไป

**ทางที่เลือก — คง CDN + ปักเวอร์ชัน + ใส่ `integrity` กับ `crossOrigin`**
เพราะ **ทางล้มเหลวเดิมเขียนไว้ดีอยู่แล้ว** — `renderCharts` เปลี่ยนกราฟเป็นการ์ดที่บอกตรงๆ ว่า
*"เครือข่ายของโรงเรียนอาจบล็อกการโหลดกราฟ ลองรีโหลดอีกครั้งหรือดูสรุปตัวเลขด้านบนแทน"*
= ครูยังทำงานได้ครบ แค่ไม่มีกราฟ ไม่ใช่หน้าพัง
สิ่งที่**ยังขาดจริง**คือความปลอดภัย: ไฟล์นี้ถูกโหลดเข้าหน้าที่แสดงข้อมูลนักเรียน
ถ้า CDN ถูกแทรกโค้ด เดิมไม่มีอะไรกันเลย → ใส่ SRI แล้ว hash ไม่ตรงจะไม่ถูกรัน
และตกไปที่ทางล้มเหลวเดิมซึ่งปลอดภัยกว่ารันโค้ดที่ไม่รู้ว่าใครแก้

**ทางที่ทิ้ง — เอา Chart.js มาไว้ในโปรเจกต์**
ตัดเน็ตภายนอกได้จริง แต่แลกด้วย
- ไฟล์ **200,807 ไบต์** ที่ต้องดูแลและอัปเดตเอง กลายเป็นโค้ดของเราในสายตาคนอ่านรอบหน้า
- ต้องเสิร์ฟผ่าน Apps Script ซึ่งช้ากว่า CDN และ **ติด cache 900 วินาทีแบบ `Js*`**
- ทุกวันนี้โหลดแบบ lazy เฉพาะตอนเปิดหน้าแรก การรวมเข้าโปรเจกต์ทำให้จ่ายค่าขนส่งกับ
  ทุกสำเนาของครูทุกคน เพื่อแก้ปัญหาที่เกิดเฉพาะโรงเรียนที่บล็อก CDN
**ถ้าวันหนึ่งมีครูรายงานว่ากราฟไม่ขึ้นจริงๆ ให้กลับมาชั่งใหม่** — ตอนนี้ยังไม่มีข้อมูลว่าเกิดขึ้นจริง

**กติกาที่ต้องรักษา**: `CHART_JS_URL` กับ `CHART_JS_SRI` **ต้องเปลี่ยนคู่กันเสมอ**
เปลี่ยน URL อย่างเดียว = เบราว์เซอร์ปฏิเสธไฟล์ กราฟหายทั้งที่เน็ตปกติ

## 35. ดาวน์โหลดถูกบล็อก — ให้ลิงก์กดเอง ไม่ใช่บอกให้ครูไปตั้งค่าเบราว์เซอร์

**ปัญหา** (เจอจริง 18 ส.ค. 2569): กด "สร้างไฟล์สำรอง" → ฝั่ง server สำเร็จ
(ป้าย "สำรองล่าสุด" ขยับ) แต่**ไฟล์ไม่ถึงเครื่องเลย** — ไม่มีในประวัติดาวน์โหลดของ Chrome
ไม่มีไฟล์ในเครื่อง และไม่มีแท็บใหม่
เพราะ `window.open` ถูกเรียก**หลังจบ round-trip ของ `serverCall`** จึงเสีย
transient user activation → เบราว์เซอร์บล็อก **เป็นชนิดเดียวกับบั๊ก `execCommand('copy')`
ของลิงก์ผู้ปกครอง** (ข้อ 1 ของรอบ UAT)

**ทางที่เลือก — แก้ที่ `openDownloadWindow` ตัวเดียว ให้เปิดกล่องที่มีลิงก์กดเอง**
การกดลิงก์คือ user gesture จริง จึงไม่ถูกบล็อก ครูได้ไฟล์ในคลิกเดียว
**ได้ทั้ง 3 ที่ที่ใช้ฟังก์ชันนี้ร่วมกัน** — ไฟล์สำรอง · CSV หน้ารายงาน · แม่แบบหน้านำเข้า

**ทางที่ทิ้ง — คงข้อความ "กรุณาอนุญาต pop-up แล้วลองใหม่" ไว้เฉยๆ**
เป็นสิ่งที่โค้ดเดิมทำ และมัน**โยนงานกลับให้ครูไปตั้งค่าเบราว์เซอร์**
แย่กว่านั้นสำหรับไฟล์สำรอง: ไฟล์บน Drive ถูกลบทิ้งใน 60 วินาที
ครูที่ไปหาวิธีอนุญาต pop-up แล้วกลับมาจะพบว่าต้องกดสร้างใหม่อยู่ดี

**ทางที่ทิ้ง — ทำ modal เฉพาะของหน้าสำรองข้อมูล**
แก้ได้จุดเดียว ขณะที่อีก 2 จุดยังพังเหมือนเดิม และเพิ่มโค้ดซ้ำ

**ผลข้างเคียงที่ต้องแก้ตาม**: ยืดตัวจับเวลาลบไฟล์สำรองจาก **60 วินาที เป็น 5 นาที**
เพราะครูต้องอ่านกล่องแล้วกดลิงก์เอง ของเดิมไฟล์หายก่อนกดทัน
(`registerTempDriveFile_` ลงทะเบียน 1800 วินาที จึงยังอยู่ในขอบเขต)
และแก้ข้อความบนหน้าตั้งค่าจาก "ดาวน์โหลดลงเครื่องนี้ทันที" เป็น "ดาวน์โหลดลงเครื่องนี้"
เพราะคำว่า "ทันที" ไม่จริงเสมอไปแล้ว

---

## 36. เลิกอ่านของเดิมซ้ำด้วย memo ระดับ execution ไม่ใช่ยืดอายุ cache

**บริบท** — ผลวัดจาก `_timing_log` 594 แถว (18 ส.ค. 2569) ชี้ว่าคอขวดไม่ใช่อัลกอริทึม
(`rows_ms = 1` — ปั้น HTML นักเรียน 39 คนใช้ 1 มิลลิวินาที) แต่เป็นการอ่านของเดิมซ้ำ
4 จุดที่ยืนยันกับโค้ดแล้ว

1. `getInitialData` [Code.js:101-102] อ่านชีต `ตั้งค่า` **เต็มใบ 2 ครั้ง** ผ่าน `getTeacherSessionUser_`
   และ `getPinStateData_` ซึ่งเรียก `getSettings_()` ตรงๆ ทั้งที่ `getCachedSettings_()` มีอยู่แล้ว
   และทั้งคู่ไม่ populate คีย์ `'st'` → ตัวถัดไปอาจอ่านเป็นครั้งที่ 3 (วัดได้ `pf_settings_ms=151` ตอน cold)
2. `getStudentList` [StudentService.js] อ่านชีตนักเรียนเต็มทุกครั้ง ไม่ผ่าน `getCachedStudentList_`
3. `getDerivedDataCacheVersion_` อ่าน ScriptProperties **ทุกครั้งที่สร้างคีย์ cache**
   หน้าเช็คชื่อ 1 ครั้งสร้าง ~10–15 คีย์
4. `getCurrentAttendanceSourceInfo_` เปิดชีต 2 ใบ + `getLastRow()` 2 ครั้งทุกครั้ง มี call site **37 จุด**
   เพื่อได้สตริงเดิม

**ทางที่เลือก** — memo ระดับ execution ทุกตัว + เปลี่ยน 3 จุดให้ใช้ตัวที่ cache อยู่แล้ว

**ทางที่ทิ้ง**

- **ยืดอายุ CacheService ให้นานขึ้น** — ไม่แก้ปัญหา เพราะต้นทุนที่วัดได้คือการเรียกซ้ำ**ภายใน execution เดียว**
  ซึ่งจ่ายค่า CacheService/PropertiesService round-trip อยู่ดี และการยืดอายุเพิ่มความเสี่ยงข้อมูลเก่า
- **memo `getCachedSettings_` โดยไม่แตะจุดที่ล้าง `'st'`** — จะได้ของเก่าค้างทั้ง execution
  จึงรวมการล้างเป็น `invalidateSettingsCache_()` แล้วไล่แก้ทั้ง 6 จุดที่เคย `remove('st')` เอง
- **memo `getCurrentAttendanceSourceInfo_` แล้วล้างเองรายจุด** — มี invalidate 4 ทาง เสี่ยงลืม
  จึงล้างที่ `bumpDerivedDataCacheVersion_` จุดเดียว ซึ่งทุก mutation ผ่านอยู่แล้ว

**ที่ยอมแลก** — `DERIVED_CACHE_VERSION_MEMO_` ทำให้ execution หนึ่งไม่เห็น bump จาก execution อื่น
ที่เกิดระหว่างทาง รับได้เพราะ cache ปลายทางอายุ 180–300 วินาทีอยู่แล้ว และ mutation ทุกตัวผ่าน
`withAttendanceMutationLock_` · ถ้าเจออาการ "แก้แล้วไม่อัปเดตในคำขอเดียวกัน" ให้สงสัยข้อนี้ก่อน

**ตรวจก่อนแก้ว่าไม่มีใครแก้ object ที่จะ memo** — grep `sourceInfo.<field> =` และ `settings.<field> =`
เจอแค่ 2 บรรทัดใน `getSettings_()` เอง (normalize ก่อนคืนค่า) ไม่ใช่การแก้จากภายนอก
และ `sanitizeStudentListForClient_` สร้าง object ใหม่ทั้งหมด ไม่กลายพันธุ์ของที่ memo ไว้

**ผลข้างเคียงที่ตรวจแล้วว่าไม่กระทบ** — `getCachedStudentList_` ผ่าน JSON ทำให้ `created_at`
เปลี่ยนจาก `Date` เป็นสตริง ISO · grep แล้ว client **ไม่ใช้ `student.created_at` เลย**
(ที่เจอเป็น metadata ของ backup ใน localStorage คนละเรื่อง)

---

## 37. ให้ข้อมูลที่เก็บถาวรแล้วมีเวอร์ชัน cache ของตัวเอง แทนการเลิก invalidate

**บริบท** — `_timing_log` 594 แถว (18 ส.ค. 2569) ชี้ตัวเดียวซ้ำๆ ใน 10 รายการล่าสุด:
`readAllAttendanceRecords_` อ่าน 2,586 แถว ใช้เวลา 2,269 / 3,968 / 4,070 / 4,735 / 16,903 / 18,245 / 18,620 ms
และอธิบายทั้ง `attendance_load_ms` (ค่ากลาง 6,246) · `dashboard_build_ms` (4,226) · `report_build_ms` (4,165)

ไล่ครบวงจรแล้ว: `markStatus` → `invalidateAttendanceCaches_` → `bumpDerivedDataCacheVersion_`
→ คีย์ derived cache **ทุกตัว**เปลี่ยน รวม `attendance_all_records` และ `attendance_date_buckets`
→ ครั้งถัดไปสแกนชีตใหม่ทั้งหมด

**ประเด็นคือ 2,586 แถวนั้นอยู่ในชีต `_att_archive_1` ทั้งหมด (ชีตหลักมี 0 แถว)
และชีต archive ไม่เปลี่ยนเลยเมื่อครูเช็คชื่อ** — เช้าหนึ่งเช็ค 40 คน = ทิ้ง cache 40 รอบ
ครูไม่รู้สึกตอนแตะเพราะ optimistic UI แต่จ่ายเต็มตอนเปิดหน้าแรกหรือรายงาน

**ทางที่เลือก** — แยก `getAllAttendanceRecords_` เป็น 2 cache แล้ว `concat` ตามลำดับเดิม
ส่วน archive ใช้ `attendance_archive_version` (Script Properties) ซึ่ง bump เฉพาะตอนชีต archive เปลี่ยนจริง
อายุ 6 ชั่วโมง · ส่วนชีตหลักยังใช้ `derived_cache_version` อายุ 180 วินาทีตามเดิม

**ทางที่ทิ้ง**

- **เลิก bump ตอน `markStatus` แล้วล้างเป็นรายคีย์แทน** — ระบบไม่มี invalidate รายคีย์เลย
  ต้องเขียนใหม่ทั้งชุดและต้องไล่ให้ครบทุกคีย์ที่ derived จากข้อมูลเช็คชื่อ พลาดจุดเดียว = ครูเห็นตัวเลขเก่า
  บนเอกสาร ปพ.6 ซึ่งเป็นความเสียหายที่หนักกว่าความช้ามาก
- **ยืดอายุ cache รวมให้ยาวขึ้นเฉยๆ** — ไม่ได้ผล เพราะปัญหาคือถูก**ทิ้ง**ไม่ใช่**หมดอายุ**
- **แก้ `attendance_date_buckets` ให้ patch เฉพาะวันที่เปลี่ยน แทนการทิ้งทั้งก้อน** — แก้อาการปลายทาง
  แต่ต้นทางยังสแกนใหม่อยู่ดีเมื่อ bucket หลุด cache และการ patch ผิดจะทำให้ตัวเลขเพี้ยนแบบเงียบๆ
- **ย้ายข้อมูลเก่าออกจาก Sheets ไปที่อื่น** — ผิดข้อจำกัดของสินค้า ครูต้องเปิดชีตดูข้อมูลตัวเองได้

**ที่ตั้งใจไม่แตะ** — ลำดับ `archive.concat(main)` เหมือนเดิมเป๊ะ และไม่แตะ `getUniqueLatestRecords_`
(`CLAUDE.md` ระบุเป็นจุดห้ามแตะโดยไม่ถาม จึงถามก่อนลงมือ)
สำหรับภาคเรียนที่**ยังไม่เก็บถาวร** ส่วน archive จะว่าง พฤติกรรมเท่าเดิมทุกประการ ไม่มีทางแย่ลง

**ด่านที่สอง** — คีย์ชั้น C2 ใส่ `<ชื่อชีต>:<getLastRow()>` ไว้ด้วย เผื่อมีเส้นทางเขียนชีต archive
แล้วลืม bump เวอร์ชัน (ตรวจแล้วว่าทุกเส้นทางผ่าน `invalidateSemesterCaches_` ซึ่ง bump ให้)

**พิสูจน์ยังไง** — `perf:archive_cache_survives_mark` ใน `Diagnostics.js` เทียบ `readAllAttendanceRecords_`
(สแกนตรง) กับ `getAllAttendanceRecords_` (เส้นทางแยก cache) ทั้งจำนวนและลายเซ็นรายแถวตามลำดับ
แล้ว bump เวอร์ชันจำลองการแตะสถานะ วัดซ้ำ — `after_simulated_mark_ms` ต้องต่ำกว่า `direct_scan_ms` มาก

---

## 38. เลิกอุ่น cache ตอนยืนยันวัน แทนการย้ายออกนอกล็อก

**บริบท** — `derived_cache_warm_ms` วัดจากการใช้งานจริงได้ค่ากลาง **7,510 ms** สูงสุด **12,243 ms**
ตรงกับ "ยืนยันวัน ~16 วิ" ที่จับเวลาด้วยตาในรอบ UAT

หลังแยก cache ชีต archive (ข้อ 37) วัดซ้ำด้วย `perf:confirm_day_warm_cost` ได้
`cold_warm_ms` **5,896** · `hot_warm_ms` **128** → **การแยก cache ไม่ได้แก้ข้อนี้ให้**
เพราะ `confirmDay` เรียก `invalidateAttendanceCaches_` **ก่อน** แล้วค่อยอุ่น จึงต้องสร้างใหม่จากศูนย์เสมอ
(dashboard + summary table + daily grid ทั้งเดือน) ทั้งหมดนี้อยู่ใน `withAttendanceMutationLock_`

**ข้อเท็จจริงที่ตัดสิน** — ไล่ดูฝั่ง client แล้ว `confirmDay` เมื่อสำเร็จ **ไม่ยิงคำขอใดๆ ต่อ**
แค่เซ็ต `state.attendance.day_status.status = 'confirmed'` แล้ว `syncAttendanceViewAfterStateChange_()`
ซึ่งวาดจอฝั่ง client ล้วน → ครูยังอยู่หน้าเช็คชื่อ ไม่ได้เปิด dashboard หรือรายงาน
**การอุ่นจึงเป็นการเดาว่าครูจะเปิดหน้าอื่นต่อ และครูจ่ายค่านั้นแน่นอน 100% ทุกครั้ง**

หลักฐานเสริม: `unconfirmDay` ใช้ mode `draft` ซึ่งอยู่ในรายการยกเว้นอยู่แล้ว = ไม่เคยอุ่น และไม่มีใครบ่น
ความไม่สมมาตรนี้เองบอกว่าการอุ่นไม่ใช่สิ่งจำเป็น

**ทางที่เลือก** — ตัดการอุ่นออกจาก `confirmDay` ทิ้ง

**ทางที่ทิ้ง**

- **ย้ายออกไปนอกล็อกแต่ยังอุ่นอยู่** — ครูยังรอเท่าเดิม เพราะ Apps Script ไม่มีงานเบื้องหลัง
  `google.script.run` คือคำขอเดียวจบ ไม่มีทาง "สั่งแล้วปล่อย" ได้เลย ได้แค่เลิกบล็อกคำขออื่น
  ซึ่งระบบนี้มีครูคนเดียวต่อสเปรดชีต จึงแทบไม่มีประโยชน์
- **อุ่นเฉพาะ dashboard** — ยังเป็นตัวที่แพงที่สุดในสามตัว (`dashboard_build_ms` 3,091 วัดพร้อมกัน)
  ตัดไปแค่ครึ่งเดียวแต่ยังจ่ายส่วนใหญ่
- **อุ่นก่อน invalidate** — ผลลัพธ์คือ cache ที่อุ่นไว้ถูกทิ้งทันทีหลังจากนั้น ไม่ได้อะไรเลย
- **ไม่ต้อง invalidate เลยตอนยืนยัน** — ผิด สถานะวันเปลี่ยนจริง รายงานต้องเห็นของใหม่

**ผลที่ยอมรับ** — ครูที่กดยืนยันแล้วเปิดหน้าแรกต่อทันที จะเจอการสร้าง cache ครั้งแรกที่นั่นแทน
แต่เป็นต้นทุนเดียวกับที่ต้องจ่ายอยู่แล้วเมื่อ cache หมดอายุ และจ่ายเฉพาะหน้าที่เปิดจริง

**ยังค้างอยู่** — mode `clear_holiday` ยังอุ่นในล็อกเหมือนเดิม (`AttendanceService.js`)
ยังไม่ได้วัดว่าแพงแค่ไหนและครูทำบ่อยแค่ไหน จึงยังไม่แตะ อยู่ใน `TODO.md`

---

## 39. ปุ่มเลื่อนวันใช้ load token + ฐานคำนวณจากวันที่ที่ขอไปแล้ว

**บริบท** — `setLoading(show)` คือ `state.loading = show;` **เฉยๆ ไม่วาดอะไรเลย**
ครูกด ◀ แล้วจอค้างวันเดิมตลอด round-trip (3–8 วินาที) จึงกดซ้ำเพราะคิดว่ากดไม่ติด

และ `loadAttendance` **ไม่มี in-flight guard และไม่มี load token** แบบที่ `loadPage` มี (`isActivePageLoad_`)
→ กด ◀ รัวๆ 3 ครั้ง = ยิง 3 คำขอ และ **คำตอบที่มาถึงช้าที่สุดเป็นตัวชนะ ไม่ใช่วันที่ครูกดล่าสุด**
เป็นบั๊กความถูกต้อง ไม่ใช่แค่ความช้า

**เรื่องที่สามที่เจอตอนแก้** — `shiftDate` คำนวณจาก `state.currentDate` ซึ่งอัปเดตเมื่อโหลดสำเร็จเท่านั้น
กด ◀ สามครั้งเร็วๆ จึงได้ "เมื่อวานของวันเดิม" สามรอบ = ถอยหลังได้แค่วันเดียว

**ทางที่เลือก**

1. token แบบเดียวกับ `loadPage` — คำตอบของคำขอที่ถูกแทนที่แล้วถูกทิ้ง
2. `pendingAttendanceDate` แยกจาก `state.currentDate` เป็นฐานให้ปุ่ม ◀ ▶ และตัวกันเลือกวันซ้ำ
3. `setAttendanceDateLoading_` ทำสถานะโหลดที่**มองเห็นจริง** — หรี่รายชื่อ ปิดปุ่ม ◀ ▶
   ต่อท้ายวันที่ด้วย " · กำลังโหลด..." และเด้งค่าใน `input[type=date]` ไปวันใหม่ทันที

**ทางที่ทิ้ง**

- **debounce ปุ่ม ◀ ▶ แบบเดียวกับช่องพิมพ์วันที่ (800ms)** — ผิดกับพฤติกรรมปุ่ม ครูกดเพื่อไล่ทีละวัน
  การหน่วงทำให้รู้สึกหนืดกว่าเดิม และไม่แก้ปัญหา race ที่ต้นเหตุ
- **ปิดปุ่มทั้งหมดจนกว่าจะโหลดเสร็จ** — ครูไล่วันเร็วๆ ไม่ได้ ทั้งที่ token แก้ปัญหาได้โดยไม่ต้องห้าม
  (ปิดเฉพาะระหว่างรอ แต่ยังยิงคำขอใหม่ทับได้เมื่อกลับมากดได้)
- **แสดง skeleton ทั้งหน้าแบบ `loadPage`** — กระพริบแรงเกินไปสำหรับการเลื่อนทีละวัน
  ครูเสียบริบทว่ากำลังดูอะไรอยู่ การหรี่ของเดิมไว้อ่านง่ายกว่า

**⚠️ ห้าม disable ช่องวันที่** — เส้นทางพิมพ์วันที่มี debounce 800ms ถ้า disable กลางคัน
โฟกัสจะหลุดแล้วครูพิมพ์ต่อไม่ได้ · ปิดเฉพาะปุ่ม ◀ ▶ เท่านั้น

**ที่ระวังไว้** — ล้าง `pendingAttendanceDate` ทุกครั้งที่คำขอล่าสุดจบ **แม้ครูจะเปลี่ยนหน้าไปแล้ว**
(เช่น session หมดอายุกลางคัน) ไม่งั้นค่าจะค้าง แล้วปุ่ม ◀ ▶ คำนวณจากวันที่เก่าเมื่อกลับมาหน้าเช็คชื่อ

---

## 40. แตะสถานะแล้ววาดเฉพาะแถวนั้น โดยมีทางถอยกลับไปวาดทั้งลิสต์เสมอ

**บริบท** — `syncAttendanceViewAfterStateChange_` → `refreshAttendanceList_` →
`list.innerHTML = renderAttendanceListContent_(...)` **ทั้งลิสต์**
แต่ละแถวมีปุ่มสถานะ 5 ปุ่ม แต่ละปุ่มฝัง SVG เต็มตัวจาก `renderUiIcon` และมี label 2 ชุด (mobile/desktop)
→ ห้องละ 40 คน = ทิ้งและสร้าง DOM ~200 ปุ่มใหม่ **เพื่อเปลี่ยนสถานะคนเดียว**
`id="srow-<เลขที่>"` มีอยู่แล้วตั้งแต่แรกแต่ไม่เคยถูกใช้

**ทางที่เลือก** — `refreshAttendanceStudentRow_(studentNumber)` แทนแถวเดียวด้วย `replaceChild`
และ **คืน `false` เมื่อทำทางลัดไม่ได้ ให้ผู้เรียกวาดทั้งลิสต์แทน**
แยกผู้เรียกเป็น 2 กลุ่มชัดเจน: `markStatus` / `undoStatus` (4 จุด) ใช้ทางลัด ·
`bulkMarkPresent` / `undoBulkPresent` / `confirmDay` / `unconfirmDay` (5 จุด) วาดทั้งลิสต์เหมือนเดิม

**เงื่อนไขที่ต้องถอยกลับไปวาดทั้งลิสต์ — ต้องครบทุกข้อ ไม่งั้นจะเหลือแถวค้างแบบเงียบๆ**

| กรณี | ตรวจด้วย |
|---|---|
| โหมดรูป (PhotoGrid) โครงสร้างคนละแบบ + ต้อง hydrate รูป | `state.viewMode === 'grid'` |
| แถวนั้นถูกกรองออกจากลิสต์อยู่แล้ว (เช่นกรอง "ยังไม่เช็ค" แล้วยกเลิกสถานะ) | หา `srow-<เลขที่>` ไม่เจอ |
| แถวนั้นต้องหายไปหลังเปลี่ยน (เช่นกรอง "ยังไม่เช็ค" แล้วเพิ่งเช็ค) | ไม่อยู่ใน `getFilteredAttendanceStudents_` |
| จำนวนแถวที่ควรแสดง ไม่ตรงกับที่มีอยู่จริง | เทียบ `filtered.length` กับจำนวน `.attendance-student-row` |

**ทางที่ทิ้ง**

- **แก้ทีละ property บนแถวเดิม** (เปลี่ยน class + textContent ของ badge) — เร็วกว่าเล็กน้อย
  แต่ต้องรู้โครงสร้างแถวทั้งหมดในสองที่ ถ้า `renderStudentRows` เปลี่ยนแล้วลืมแก้อีกที่
  จะเพี้ยนแบบไม่มีอะไรเตือน · การวาดแถวเดียวจากฟังก์ชันเดิมใช้แหล่งความจริงเดียวกัน
- **เอา virtual DOM / diff library มาใช้** — ผิดกับข้อจำกัดของโปรเจกต์ (ES5 ไม่มี build step)
- **ทำทางลัดให้ครอบคลุม bulk ด้วย** — ตอน bulk ทุกแถวเปลี่ยนอยู่แล้ว วาดทั้งลิสต์ถูกกว่าและง่ายกว่า

**หลักการที่ยึด** — ทางลัดต้อง**ตรวจว่าตัวเองใช้ได้ไหมก่อนเสมอ** และเมื่อไม่แน่ใจให้ถอยไปทางเดิม
ผลลัพธ์บนจอต้องเหมือนเดิมทุกกรณี ไม่ใช่ "เหมือนเดิมในกรณีที่คิดถึง"

---

## 41. หั่น cache ตามไบต์จริง ไม่ใช่จำนวนตัวอักษร

**บริบท** — `ARCHITECTURE.md` บันทึกกับดักนี้ไว้นานแล้วว่า `LARGE_CACHE_CHUNK_SIZE = 80000`
นับเป็นตัวอักษรแต่เพดาน CacheService นับเป็นไบต์ **แต่ไม่เคยมีใครแก้และไม่เคยมีใครวัด**

วัดจริงได้ (สคริปต์หลังถอด `<script>` แล้ว `JSON.stringify`)

| โมดูล | ตัวอักษร | อักษรไทย | ไบต์ | เกิน 100 KB |
|---|---:|---:|---:|:--:|
| `JsReports` | 89,600 | 11,220 | **115,010** | ★ ใช่ |
| `JsDashboard` | 76,898 | 8,886 | 97,895 | ไม่ (เหลือ ~4.5 KB) |
| `JsAnalytics` | 57,692 | 6,613 | 72,712 | ไม่ |
| `JsImport` | 32,719 | 4,897 | 43,954 | ไม่ |
| `JsProfile` | 12,777 | 791 | 15,102 | ไม่ |
| `JsPhotoGrid` | 11,575 | 140 | 12,513 | ไม่ |

`getCachedClientAssetContent_` ใช้ `putCachedJsonByKey_` (คีย์เดียว ไม่หั่น) ซึ่งกลืน error ทิ้ง
→ **`JsReports` เขียน cache ไม่ติดเลย เปิดหน้ารายงานทุกครั้ง server ต้องอ่านไฟล์ + รัน regex ใหม่**
โดยไม่มีอะไรเตือน

**ทางที่เลือก**

1. `utf8ByteLength_` + `splitStringByUtf8Bytes_` หั่นตามไบต์จริง เพดาน 90,000 ไบต์/ชิ้น
   (เหลือที่ให้ชื่อคีย์และ overhead)
2. `getCachedClientAssetContent_` เปลี่ยนไปใช้ `putLargeCachedJsonByKey_` / `getLargeCachedJsonByKey_`

**ทางที่ทิ้ง**

- **ลดเพดานตัวอักษรลงเป็น 24,000 (100 KB ÷ 4 ไบต์ กรณีแย่สุด)** — ปลอดภัยแต่หยาบ
  เนื้อหาที่เป็น ASCII ล้วนจะถูกหั่นเป็นชิ้นเล็กเกินจำเป็น 4 เท่า เพิ่มจำนวนคีย์โดยไม่ได้อะไร
- **ใช้ `Utilities.newBlob(str).getBytes().length` วัดไบต์** — ถูกต้องแต่ช้ามากเมื่อเรียกกับสตริง
  หลักล้านตัวอักษร จึงนับเองด้วยลูปเดียว (ทดสอบแล้วตรงกับ `Buffer.byteLength` ทุกเคส)
- **ย่อไฟล์ `JsReports` ให้เล็กลง** — แก้อาการไม่แก้เหตุ ไฟล์อื่นจะโตข้ามเพดานตามมาเรื่อยๆ
  และ `JsDashboard` เหลือที่แค่ 4.5 KB อยู่แล้ว
- **เลิก cache โมดูล client ไปเลย** — เสียประโยชน์ทั้งหมดเพื่อแก้ปัญหาของไฟล์เดียว

**⚠️ ห้ามตัดกลาง surrogate pair** — ตัดกลางคู่แล้วต่อกลับจะได้อักขระเสีย และ `JSON.parse` พังทั้งก้อน
`splitStringByUtf8Bytes_` จึงก้าวทีละ 2 เมื่อเจอ high surrogate
ทดสอบในเครื่อง 9 เคสรวมไทยล้วน · emoji ล้วน · emoji ชิดขอบพอดี · ไฟล์จริง — ต่อกลับตรงต้นฉบับทุกเคส

**★ ผลข้างเคียงที่ต้องรู้** — เดิม `JsReports` เขียน cache ไม่ติด จึง **แก้แล้วเห็นผลทันทีมาตลอด**
หลังแก้รอบนี้มันจะค้างได้ถึง 15 นาทีเหมือนโมดูลอื่น (คีย์ `client_module_asset|X` ล้างไม่ได้)
บันทึกไว้ใน `CLAUDE.md` · `ARCHITECTURE.md` · `UAT.md` แล้ว เพราะเป็นกับดักที่ทำให้เสียรอบทดสอบทิ้ง

**ยังค้างอยู่** — `putCachedJsonByKey_` (ตัวเล็ก) ยังไม่เช็คขนาด ถ้า `daily_grid` / `summary_table`
โตเกิน 100 KB จะล้มเงียบแบบเดียวกัน อยู่ใน `TODO.md`

### ⚠️ แก้ไขข้อ 41 — สมมติฐานหลักผิด (วัดบนของจริง 19 ส.ค. 2569)

**ที่เขียนไว้ข้างบนว่า `JsReports` "เขียน cache ไม่ติดเลย" — ไม่จริง**

`cache:client_modules_fit` รันบน Apps Script จริงแล้วได้

```
JsReports   stringify_bytes: 107,972   over_100kb: true   single_key_cached: true
broken_with_single_key: []          ← ไม่มีโมดูลไหนเขียนไม่ติดเลยสักตัว
```

**CacheService รับค่า 107,972 ไบต์ด้วยคีย์เดียวและอ่านกลับได้ครบตรงต้นฉบับ**
เพดาน "100 KB ต่อคีย์" ที่เอกสาร Google ระบุ **ไม่ถูกบังคับตามนั้น** อย่างน้อยก็ไม่ที่ขนาดนี้

**ผมผิดตรงไหน** — คำนวณขนาดในเครื่องแล้วสรุปว่าเกินเพดาน = เขียนไม่ติด
โดยไม่ได้วัดบนแพลตฟอร์มจริงก่อน ทั้งที่ `putCachedJsonByKey_` กลืน error ทิ้งจึงไม่มีทาง
รู้ได้จากการอ่านโค้ดอย่างเดียว **ต้องวัดก่อนสรุปเสมอ ไม่ใช่คำนวณแล้วเชื่อ**
ตัวเลขที่วัดในเครื่องเองก็คลาด (115,010 เทียบของจริง 107,972 ไบต์) เพราะ `include_`
ผ่าน `HtmlService` ซึ่งประมวลผลเนื้อหาไม่เหมือนอ่านไฟล์ตรงๆ

**แล้วยังเก็บการแก้ไว้ทำไม**

- ตัวหั่น chunk เดิม (80,000 **ตัวอักษร**) ยังผิดหน่วยจริง ไทยล้วนได้ chunk 240 KB
  ยังไม่รู้ว่าเพดานจริงอยู่ที่เท่าไร แต่ 240 KB ห่างจาก 108 KB ที่พิสูจน์แล้วว่าผ่านอยู่มาก
- `JsDashboard` 92,807 ไบต์ และโตขึ้นเรื่อยๆ วันหนึ่งจะชนเพดานจริงไม่ว่ามันอยู่ตรงไหน
- ถ้าพลาดจริงขึ้นมา จะเงียบสนิทเพราะ error ถูกกลืน

**แต่ต้องเรียกให้ถูก: นี่คือการกันไว้ล่วงหน้า ไม่ใช่การแก้บั๊กที่เกิดขึ้นจริง**
และมีข้อเสียที่ยอมรับไว้ — **หั่นหลาย chunk แล้ว chunk เดียวถูก evict = พลาดทั้งก้อน**
ขณะที่คีย์เดียวอยู่หรือหลุดเป็นหน่วยเดียว

**สิ่งที่ยังจริงจากข้อ 41** — `utf8ByteLength_` / `splitStringByUtf8Bytes_` ถูกต้องตามที่ทดสอบ
(9 เคสในเครื่อง ตรงกับ `Buffer.byteLength` ทุกเคส ต่อกลับตรงต้นฉบับ ไม่ตัดกลาง surrogate pair)
และ `check` ตัวใหม่ยังมีประโยชน์ เพราะรายงาน `single_key_cached` ไว้เป็นหลักฐานว่าเพดานไม่ถูกบังคับ

**สิ่งที่ไม่จริงและถูกลบออกจาก `CLAUDE.md` / `ARCHITECTURE.md` / `UAT.md` แล้ว** —
ข้อความที่บอกว่า `JsReports` เคยแก้แล้วเห็นผลทันทีเพราะ cache ไม่ติด
ความจริงคือมันติด cache มาตลอด และค้างได้ 15 นาทีเหมือนอีก 5 ตัวมาตั้งแต่ต้น

---

## 42. โหลดซ้ำหน้าที่เปิดอยู่ ให้หรี่ของเดิมไว้ ไม่ใช่ล้างจอ — แก้ที่ `loadPage` จุดเดียว

**บริบท** — มี **22 จุด** เรียก `loadPage(...)` หลังบันทึกค่าเดียว (ตั้งค่า 16 · นักเรียน 5 · เช็คชื่อ 1)
ทุกจุดล้างจอ 2 รอบ: `renderPagePanelLoading` ตอนเริ่ม แล้ว `renderPagePanelSkeleton` หลังโมดูลพร้อม
ครูที่เพิ่งกดติดดาวนักเรียนคนเดียวจึงเห็น **เนื้อหา → จอเปล่า → จอเปล่า → เนื้อหา**

**ทางที่เลือก — แก้ที่ `loadPage` จุดเดียว ไม่แตะ 22 จุด**

จำสถานะ panel รายหน้าไว้ (`pagePanelState`) แล้วแยกทางเดิน **3 ทาง ไม่ใช่ 2**

| สถานะ panel | หมายถึง | ทำอะไร |
|---|---|---|
| `'content'` | โหลดซ้ำหน้าที่เปิดอยู่ | หรี่ของเดิมไว้ ไม่ล้างจอ ไม่วาด skeleton |
| `'loading'` | `navigateTo` วาด shell ไปแล้ว | ไม่วาดซ้ำ (เดิมวาด 2 รอบด้วย state เดียวกัน) |
| อื่นๆ | error / ยังไม่เคยเปิด | วาด shell ตามปกติ |

**ทางที่ทิ้ง**

- **แก้ทีละจุดให้อัปเดต state เองแล้ววาดเฉพาะส่วนที่เปลี่ยน (22 จุด)** — ได้ผลดีที่สุดในทางทฤษฎี
  แต่ต้องรู้ว่าแต่ละ mutation กระทบ state ตัวไหนบ้าง ซึ่ง server หลายตัวคืนแค่ `{success, message}`
  ทำให้ต้องเดา · พลาดจุดเดียว = ครูเห็นข้อมูลเก่าค้างโดยไม่มีอะไรเตือน ซึ่งแย่กว่าช้า
  แก้ที่ `loadPage` ได้ประโยชน์กับทั้ง 22 จุดพร้อมกันโดยยังดึงข้อมูลจริงจาก server เสมอ
- **ไม่แสดงอะไรเลยระหว่างโหลดซ้ำ** — ครูไม่รู้ว่าระบบรับคำสั่งแล้ว จะกดซ้ำ (บทเรียนเดียวกับข้อ 39)
- **ใช้ `pointer-events: none` ไม่ได้เพราะจะบล็อก modal** — ตรวจแล้วว่า `#modal-overlay` อยู่นอก
  `.page-panel` ใน `Index.html` จึงไม่โดนบล็อก และการกันคลิกระหว่างโหลดซ้ำยังกันกดซ้ำให้ด้วย

**บั๊กที่เจอตอนทำ** — ถ้าครูเปลี่ยนหน้าระหว่าง soft reload คำขอเดิมจะถูกทิ้งที่ `isActivePageLoad_`
แล้ว**ไม่มีใครล้างคลาสหรี่ให้ = หน้านั้นค้างหรี่และกดไม่ได้ตลอดไป**
แก้ด้วย `clearAllPageSoftReloading_()` ใน `beginPageLoad_` ซึ่งทุกการโหลดหน้าผ่านอยู่แล้ว

**เก็บของแถมอีก 2 อย่างที่พิสูจน์ได้ว่าเปล่าประโยชน์**

1. `loadSettingsDetails_` มีผู้เรียกตัวเดียว (`applyPageDataAndRender_`) ซึ่งเพิ่งตั้ง
   `setPageDataStatus_('settings','details','loading')` แล้ว `renderSettingsPage()` ในบรรทัดก่อนหน้า
   → การวาดซ้ำข้างในใช้ state เดิมเป๊ะ = วาดหน้า ~480 บรรทัดทิ้งเปล่า **1 รอบต่อการเปิดหน้า 1 ครั้ง**
   (3 รอบ → 2) · เทียบสถานะก่อนวาด เผื่อวันหน้ามีผู้เรียกใหม่ที่ยังไม่ได้ตั้ง
2. `addCurrentDateToCalendar` เปลี่ยนจาก `loadPage('attendance')` เป็น `loadAttendance(state.currentDate)`
   เพราะ `getAttendanceDaily` คืน `calendar_state` มาให้อยู่แล้ว [AttendanceService.js:213]
   จึงพอสำหรับลบกล่องเตือน ไม่ต้องโหลด bootstrap ของทั้งหน้าใหม่

---

## 43. หน้าแรกยิง 2 คำขอขนานกัน · หน้าติดตามใส่ load token ก่อน แล้วค่อยใส่ debounce

### 43.1 หน้าแรก — ตัวที่สองไม่ได้พึ่งตัวแรก จึงไม่ต้องต่อแถว

`loadPage('dashboard')` เรียก `getInitialData` → `getDashboardBootstrapData_` แล้ว **รอผลตอบก่อน**
จึงค่อยเรียก `getDashboardData` ใน `loadDashboardFullData_`
ทั้งสองอ่านชีตชุดเดียวกัน (`getCachedSettings_` · `getCachedStudentList_` · ปฏิทิน ·
`getUniqueLatestRecordsByDate_(today)`) และตัวที่สอง**ไม่ได้ใช้ผลของตัวแรกเลย**

**ทางที่เลือก** — `prefetchDashboardFullData_` ยิงพร้อม `fetchPageInitialData_` ตั้งแต่ต้น
แล้ว `loadDashboardFullData_` มารับผลที่ยิงไปแล้วถ้าเดือนตรงกัน

**ความเสี่ยงที่ยอมรับ** — ฝั่ง client เดาเดือนเอง (เดือนปัจจุบัน) แต่ server ใช้
`getEffectiveMonthRange_` ซึ่งเลื่อนเดือนได้ถ้าวันนี้อยู่นอกภาคเรียน
ถ้าไม่ตรงจะทิ้งผลแล้วยิงใหม่ = **แย่สุดเท่าเดิม บวกคำขอที่เสียไป 1 ครั้ง**
โควตา `get_dashboard_data` คือ 120 ครั้ง/นาที จึงรับได้ · กรณีนอกภาคเรียนไม่ใช่กรณีปกติ

**ทางที่ทิ้ง**

- **ให้ `getInitialData` คืนข้อมูล dashboard เต็มไปเลยในคำขอเดียว** — จะทำให้หน้าแรกไม่มีอะไรขึ้น
  จนกว่างานหนักจะเสร็จ ซึ่งเป็นเหตุผลที่แยก bootstrap ออกมาแต่แรก (ขึ้นโครงก่อน เติมตัวเลขทีหลัง)
- **ให้ server บอกเดือนที่ถูกมาก่อนแล้วค่อยยิง** — นั่นคือการต่อแถวเหมือนเดิมทุกประการ

### 43.2 หน้าติดตาม — เป็นบั๊กก่อน แล้วค่อยเป็นเรื่องช้า

`shiftMonth` เรียก `loadAll()` ทันทีทุกครั้งที่กด และ `loadAll` ยิง **4 คำขอ** พร้อมกัน
โดย**ไม่มีอะไรกันคำขอชุดเก่าเลย** → เลื่อน 3 เดือนเร็วๆ = **12 คำขอ**
และร้ายกว่านั้น callback ของชุดเก่าจะเขียนลง `analyticsData` ที่ชุดใหม่เพิ่งล้าง
พร้อมกับลด `pending` ของชุดใหม่ → **ข้อมูลของหลายเดือนปนกันบนจอ และวาดก่อนโหลดครบ**

**ลำดับที่แก้ — token ก่อน debounce**

1. `analyticsLoadId` ทิ้งคำตอบของชุดที่ถูกแทนที่แล้ว (แก้ความถูกต้อง)
2. `scheduleLoadAll_` หน่วง 400ms ให้กดรัวๆ ได้แล้วยิงชุดเดียว (ลดคำขอ)

**ทำไมต้อง token ด้วยทั้งที่มี debounce แล้ว** — debounce ลดโอกาส แต่ไม่ได้กัน
ครูกดเลื่อนเดือนแล้วรอเกิน 400ms แล้วกดอีก ยังทำให้ 2 ชุดซ้อนกันได้อยู่ดี
**debounce เป็นการประหยัด ไม่ใช่การกันบั๊ก**

**ที่ระวังไว้** — `loadAll` ยกเลิก timer ที่ค้างอยู่เสมอตอนเริ่ม ไม่งั้น `render()` ซึ่งเรียก
`loadAll()` ตรงๆ (โหลดครั้งแรก ไม่ควรหน่วง) จะโดน timer เก่าจากการเลื่อนเดือนยิงซ้ำทีหลัง

---

## 44. ไม่ทำ lazy CSS — วัดแล้วได้ 17 มิลลิวินาที ปิดเงื่อนไขของข้อ 27

**บริบท** — ข้อ 27 ตัด lazy CSS ทิ้งแล้วเขียนเงื่อนไขไว้ว่า *"ถ้าวันหนึ่ง CSS รวมกันใหญ่จน
หน้าแรกช้าจนครูรู้สึกได้ ค่อยกลับมาทำ"* · แผนความเร็วรอบนี้ยกมาเป็นงานหนึ่ง โดยอ้างว่า
"CSS 106 KB ที่หน้าส่วนใหญ่ไม่ใช้" **แต่ไม่เคยมีใครวัดว่ามันช้าจริงไหม**

**วัดแล้ว 2 ด้าน**

ด้านไบต์ (วัดในเครื่อง) — ตัวเลข 106 KB ในแผนคือ**ขนาดดิบ** ไม่ใช่สิ่งที่วิ่งบนสาย

| | ดิบ | gzip |
|---|---:|---:|
| CSS ทั้ง 5 ไฟล์ | 229,668 | 36,603 |
| เฉพาะ 3 ไฟล์ที่หน้าส่วนใหญ่ไม่ใช้ | 100,469 | **16,800** |
| ทั้งหน้า | ~599,505 | ~106,810 |

→ lazy CSS ประหยัดได้ **~16 KB จาก ~107 KB (15%)** = ต่ำกว่า 0.1 วินาทีบนเน็ตทั่วไป

ด้านเวลาฝั่ง server (`perf:inline_css_cost` บน Apps Script จริง) — **นี่คือตัวชี้ขาด
เพราะคอขวดของหน้านี้คือเวลาประมวลผล ไม่ใช่ไบต์**

```
css_total_ms: 17      (รอบสอง 14)     shell_ms: 21
Stylesheet 4 · StyleReport 5 · StylePhase3 4 · StylePhase4 2 · StylePin 2
```

**17 มิลลิวินาที** เทียบกับ `attendance_load_ms` ค่ากลาง 6,246 ms และหน้าครูที่ใช้ 40–60 วินาที

**ตัดสิน — ไม่ทำ** และตั้งเกณฑ์ไว้ล่วงหน้าก่อนเห็นตัวเลข: <300 ms = ไม่ทำ ·
300–1500 ms = cache ฝั่ง server ไม่ใช่ lazy · >1500 ms = ค่อยพิจารณา lazy
ผลออกมา 17 ms จึงต่ำกว่าเกณฑ์ล่างเกือบ 20 เท่า

**สิ่งที่แลกไปถ้าทำ** (จากข้อ 27 ซึ่งเจอมาแล้วจริง) — FOUC เพราะ CSS มาช้ากว่า HTML ·
`StyleReport`/`Phase3`/`Phase4` ใช้ `var(--...)` โดยไม่นิยามเอง ต้องพึ่ง `Stylesheet`
มาก่อนเสมอ **ลำดับที่พึ่ง network จึงเปราะ** · และต้องเอา `include_` ออกพร้อมกัน
ไม่งั้นกลับไปมีสองทางแล้วชน cascade เหมือนเดิม

**เงื่อนไขที่จะทำให้กลับมาทบทวน** — `perf:inline_css_cost` รายงาน `css_total_ms` เกิน 1,500 ms
หรือ CSS โตจนเกิน 3 เท่าของวันนี้ · **ห้ามกลับมาทำเพราะ "ขนาดไฟล์ดูใหญ่" อย่างเดียว**

---

## 45. เครื่องมือวัดต้องไม่ทำให้ตัวเองตาย และต้องวัดถูก

**เจอระหว่างปิดข้อ 44 — 2 เรื่องในเครื่องมือที่ใช้ตัดสินใจมาทั้งวัน**

### 45.1 `duration_ms` ของทุก check เป็น 0 มาตลอด

```javascript
return {
  name: ..., ok: true,
  duration_ms: new Date().getTime() - startedAt,   // ← ประเมินตรงนี้
  detail: fn()                                     // ← งานจริงเพิ่งเริ่มทำตรงนี้
};
```
object literal ประเมินตามลำดับคีย์ **เวลาจึงถูกจับก่อนงานเริ่ม** ทุก check รายงาน 0
โชคดีที่ทุก check ที่ใช้ตัดสินใจรายงานเวลาไว้ใน `detail` ของตัวเองด้วย จึงไม่มีข้อสรุปไหนผิดเพราะเรื่องนี้
แต่ตัวเลข `duration_ms` ที่เห็นตลอดวันเป็นของปลอม · แก้ให้เรียก `fn()` ให้เสร็จก่อนแล้วค่อยจับเวลา

### 45.2 ตัวควบคุมที่ผมเพิ่มเอง เกือบทำให้ทั้งรอบตาย

`perf:archive_cache_survives_mark` เรียก `readAllAttendanceRecords_` (สแกนตรง ไม่ผ่าน cache)
เป็นตัวควบคุมเพื่อพิสูจน์ว่าข้อมูลตรงกัน · รอบ 19 ส.ค. 14:54 Apps Script ช้าทั้งกระดาน
(`attendance_pass1_ms` 16,309 เทียบปกติ ~1,500) และ **การสแกนตรงใช้ 136,459 ms**
ทำให้ทั้งรอบใช้ **344 วินาที จากเพดาน 360** — อีกนิดเดียวจะตายและไม่ได้ผลอะไรเลยสักตัว

**แก้** — ข้ามการสแกนตรงถ้าเดินมาถึง check นั้นช้ากว่า `P0_DIAGNOSTIC_CONTROL_SCAN_BUDGET_MS`
แล้วรายงาน `direct_scan_skipped: true` · การเทียบก่อน/หลัง bump ยังทำได้เสมอเพราะไม่ต้องพึ่งการสแกนตรง

**⚠️ แก้ตัวเลขหลังวัดจริง** — ตอนตั้งเกณฑ์ผมเขียนว่า "รอบปกติถึงภายในไม่กี่วินาที" **ซึ่งผิด**
รอบแข็งแรง 20 ส.ค. 2569 รายงาน `elapsed_before_ms: 18,848` เกณฑ์ 30,000 จึงเหลือระยะห่างแค่ 11 วินาที
ถ้าเพิ่ม check ก่อนหน้าอีกนิดเดียว รอบที่แข็งแรงจะเริ่มถูกข้ามเงียบๆ แล้วเสียตัวควบคุมไปโดยไม่รู้ตัว
→ ปรับเป็น **45,000** ให้เหลือระยะห่างราว 26 วินาทีจากค่าที่วัดได้
ยังไม่รู้ว่ารอบที่ช้าใช้ไปเท่าไรก่อนถึง check นี้ เพราะตอนนั้นยังไม่มีฟิลด์ `elapsed_before_ms`
**ให้ดูค่านั้นทุกรอบ ถ้าเข้าใกล้ 45,000 ให้ย้าย check ไปไว้ต้นๆ แทนการเพิ่มเกณฑ์ไปเรื่อยๆ**

**บทเรียน** — ตัวควบคุมที่แพงกว่าสิ่งที่มันตรวจ ต้องมีทางถอย ไม่งั้นวันที่ระบบมีปัญหาที่สุด
คือวันที่เครื่องมือวัดใช้ไม่ได้พอดี

**ของแถมที่ได้จากรอบช้านี้** — เป็นหลักฐานที่ดีที่สุดของการแยก cache ชีต archive (ข้อ 37)
ขณะที่การสแกนตรงใช้ **136 วินาที** เส้นทางที่มี cache ใช้ **343 ms** และหลังจำลองการแตะสถานะ
ใช้ **310 ms** — ต่างกัน 400 เท่า และเป็นความต่างระหว่าง "ใช้ได้" กับ "ชนเพดาน 6 นาที"

---

## 46. ตัดการอุ่น cache ตอนล้างข้อมูลวันหยุด — ปิดเส้นทางนี้ทั้งหมด

**บริบท** — หลังตัด mode `confirm` (ข้อ 38) เหลือ `clearHolidayAttendance` เป็นผู้เรียก
`warmLikelyDerivedCachesForDate_` ตัวสุดท้าย · วัดได้ `cold_warm_ms` **4,528 / 5,343 / 5,569 ms**
ทั้งหมดอยู่ใน `withAttendanceMutationLock_` ขณะครูรอ

**ตอนแรกดูเหมือนต่างจาก `confirmDay`** — client ที่นี่**ยิงคำขอต่อทันที**
(`loadSummary` หรือ `loadDailyGrid` ใน [JsReports.html]) ไม่ได้อยู่เฉยๆ แบบหน้าเช็คชื่อ
การอุ่นจึงดูเหมือนไม่ใช่การเดา

**แต่ไล่ดูจริงแล้วไม่มีสถานการณ์ไหนที่มันช่วย**

| | อุ่นอะไร | client ขออะไรต่อ |
|---|---|---|
| ช่วงวันที่ | เดือนของวันที่ล้าง (`getEffectiveMonthRange_`) | **ช่วงที่เลือกในหน้ารายงาน** (ช่อง `rpt-start`/`rpt-end`) |
| หน้าแรก | อุ่น `dashboard_data` ให้ด้วย | ครูอยู่หน้ารายงาน ไม่ได้ไปหน้าแรก |

- **ช่วงไม่ตรง** (กรณีปกติ เช่นครูเลือกทั้งภาคเรียน) → อุ่นทิ้งเปล่า **และงานจริงยังต้องทำอยู่ดี**
- **ช่วงตรงพอดี** → ก็ยังไม่ได้อะไร เพราะ `getSummaryTable` เรียก `getCachedSummaryTableDataForRange_`
  ซึ่ง**cache ให้เองอยู่แล้ว** การอุ่นก่อนจึงเป็นการย้ายงานเดิมเข้าไปทำ**ในล็อก**ให้ครูรอเฉยๆ

**ทางที่เลือก** — ตัดการอุ่นออก แล้วเมื่อเหลือผู้เรียก 0 ตัว **ลบ `warmLikelyDerivedCachesForDate_`
ทิ้งทั้งฟังก์ชัน** พร้อม check `perf:in_lock_cache_warm_cost` ที่วัดมันโดยเฉพาะ

**ทางที่ทิ้ง**

- **ส่งช่วงวันที่ของหน้ารายงานเข้ามาให้ server อุ่นให้ถูก** — ผูก mutation เข้ากับสภาพหน้าจอ
  ต้องเพิ่มพารามิเตอร์ใน API และยังไม่ได้อะไรอยู่ดี เพราะคำขอถัดไปของ client cache ให้เองแล้ว
- **ย้ายออกนอกล็อกแต่ยังอุ่น** — Apps Script ไม่มีงานเบื้องหลัง ครูรอเท่าเดิม (เหตุผลเดียวกับข้อ 38)
- **เก็บฟังก์ชันไว้เผื่อใช้** — โค้ดตายที่อ่านแล้วเข้าใจว่าระบบยังอุ่น cache อยู่ ทั้งที่ไม่มีใครเรียก

**ผลรวมของเส้นทางนี้ทั้งหมด** — การอุ่น cache ในล็อกถูกทยอยตัดออก 3 รอบจนหมด
(6 mode แรกถูกกันไว้ก่อนหน้า → `confirm` ข้อ 38 → `clear_holiday` ข้อ 46)
**ไม่มี mutation ตัวไหนสร้าง derived cache ในล็อกอีกแล้ว** · metric `derived_cache_warm_ms`
ไม่มีใครส่งอีก แต่คงเกณฑ์ไว้เพื่ออ่านแถวเก่าใน `_timing_log`

**ข้อสังเกตที่ควรจำ** — ทั้ง 3 รอบ ต้นเหตุเดียวกันคือ **อุ่นหลัง invalidate จึงสร้างใหม่จากศูนย์เสมอ**
ถ้าจะอุ่น cache ที่ไหนอีกในอนาคต ให้ถามก่อนว่า "สิ่งที่อุ่นตรงกับสิ่งที่ผู้ใช้จะขอต่อจริงไหม
และผู้รับปลายทาง cache ให้เองอยู่แล้วหรือเปล่า"

