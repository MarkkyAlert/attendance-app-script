# TODO

คิวงานที่ยังไม่ได้ทำ ลบรายการออกเมื่อทำเสร็จและทดสอบผ่านแล้ว
กติกาถาวรของ repo อยู่ที่ `CLAUDE.md` ไฟล์นี้เก็บเฉพาะงานค้าง

ที่มา: UAT บนมือถือ 390x844 ผ่าน Chrome จริง วันที่ 15 ส.ค. 2569
ข้อมูลทดสอบ 40 คน ย้อนหลัง 3 เดือน

---

## รอบที่ 1 และ 2 — ปิดครบแล้ว

ทุกข้อทดสอบบนของจริงผ่านแล้ว รายละเอียดอยู่ในตาราง "ทำเสร็จและทดสอบผ่านแล้ว" ด้านล่าง

---

## รอบที่ 3 — ปิดครบแล้ว

ทุกข้อเป็นเรื่องความหมายและความชัดเจน ไม่ใช่บั๊ก รายละเอียดอยู่ในตารางด้านล่าง

**บันทึกไว้กันเข้าใจผิดซ้ำ**: TODO ฉบับก่อนเขียนว่าเกณฑ์ "ต้องติดตาม" คือ
"ขาดครบ 3 ครั้งแบบนับสะสมไม่จำกัดช่วง" ซึ่ง**ไม่ตรงกับโค้ดจริงสักที่**
ของจริงตอนนั้นมี 3 นิยามที่ไม่ตรงกันอยู่คนละที่ และตัวการของ "37 จาก 40 คน"
คือเงื่อนไข `absentCount > 0` ในตัวกรองฝั่ง client ไม่ใช่ค่าที่ครูตั้งไว้

---

## แผนการแก้ — ลำดับก่อนหลัง

**หน่วยของต้นทุนคือ "รอบ deploy" ไม่ใช่ "ข้อ"** เพราะทดสอบในเครื่องไม่ได้ ทุกการแก้ต้อง
`clasp push` → สร้าง version ใหม่ → เดินทดสอบบนของจริง แผนนี้จึงจัดกลุ่มตาม**รอบ** ไม่ใช่ตามความรุนแรง

เกณฑ์ที่ใช้จัดกลุ่ม เรียงตามน้ำหนัก
1. **ข้อที่ปลดล็อกการทดสอบอื่นต้องมาก่อน** — ตราบใดที่ปุ่มลิงก์ผู้ปกครองพัง หน้าผู้ปกครอง
   ซึ่งเป็นหน้าเดียวที่เปิดสาธารณะแบบ anonymous จะยังไม่เคยถูกทดสอบเลย
2. **แยกตามชั้น cache 15 นาที** (`CLAUDE.md` ข้อ client asset cache) — `Js*` 6 ตัวและ
   `StyleReport`/`StylePhase3`/`StylePhase4` ติด cache · `JavaScript.html` / `Index.html` /
   `Stylesheet` / ไฟล์ `.js` ฝั่ง server **สดเสมอ**
   → มัดงานที่ไม่ติด cache ไว้รอบแรก จะได้ทดสอบต่อเนื่องไม่ต้องรอ และจ่ายค่ารอ 15 นาทีแค่รอบที่จำเป็น
3. **แยกตามไฟล์ที่แตะ** — `clasp push` เขียนทับทั้งไฟล์ สองรอบที่แตะไฟล์เดียวกันควรยุบเป็นรอบเดียว
4. **แยกตามหน้าที่ต้องเดินทดสอบ** — ให้จบใน 1 การเดินต่อ 1 รอบ

| รอบ | ชื่อ | ติด cache 15 นาที | ค่าใช้จ่ายในการทดสอบ |
|---|---|---|---|
| 1 | ปลดล็อก + งานประจำวัน | ❌ ไม่ติด | ต่ำ — เดินหน้านักเรียน/เช็คชื่อ/นำเข้า |
| 2 | ตัวเลขหน้าติดตาม | ✅ ติด | กลาง — เดินหน้าติดตามอย่างเดียว |
| 3 | archive ให้กดซ้ำได้ | ❌ ไม่ติด | **สูงสุด** — ต้องเตรียมข้อมูลใหม่ทุกครั้ง |
| 4 | หน้ารายงาน | ✅ ติด | ต่ำ |

### รอบ 1 — ปลดล็อกของที่ยังทดสอบไม่ได้ + งานที่ครูเจอทุกวัน
แตะ `JavaScript.html` + ไฟล์ server เท่านั้น **ไม่ติด cache 15 นาที** ทดสอบต่อเนื่องได้ทันที

- 🔴 **ลิงก์ผู้ปกครอง — เช็คค่าที่ `execCommand` คืน + แสดงลิงก์บนจอ + แก้ข้อความ toast**
  ✅ วินิจฉัยแล้ว **อยู่ใน `JavaScript.html` ล้วน ไม่ต้องแตะ `ParentService.js`** จึงอยู่ในรอบนี้ได้
  (เดิมจัดเป็น 💥 เพราะผมสรุปผิดว่าปุ่มพัง ดูบันทึกในหัวข้อ 💥 ด้านล่าง)
- 🔴 **นำเข้า `enrolled_from` ผิด 289 วัน** — server อย่างเดียว แก้ 2 จุด
  ⚠️ **ให้ `getImportBootstrapData_` / `getSettingsBootstrapData_` ใช้ `getActiveSemesterRangeSafe_()`
  ซึ่งคืนทั้ง `from`/`to` และ `start_date`/`end_date`** — เป็นการ*เพิ่ม*ฟิลด์ ไม่ใช่เปลี่ยนของเดิม
  จึงไม่พังโค้ดที่อ่าน `start_date` อยู่แล้ว (`JavaScript.html:3269` และ `Code.js:294`)
- 🟠 **จอนิ่ง 16 วินาที 2 จุด** (ยืนยันวัน / ประวัติแก้ไข) — รูปแบบจอกำลังโหลดมีอยู่แล้วในหน้าติดตาม
- 🟠 **ตัวกรอง "เฉพาะคนที่ยังไม่เช็ค" ติดค้างข้ามวัน**
- 🟡 **ชิป "ยังไม่ระบุกลุ่ม" + ป้ายในหน้าเช็คชื่อ** — ทั้งสองหน้าอยู่ใน `JavaScript.html` ทั้งคู่
- 🟡 **ปี ค.ศ. ในประวัติแก้ไข** (ส่วนของ `JsImport` ยกไปรอบ 2 เพราะติด cache)

**ทดสอบหน้าผู้ปกครองได้แล้วตั้งแต่ตอนนี้ ไม่ต้องรอรอบ 1 จบ** — ปุ่มออกลิงก์ทำงานได้จริง
ลิงก์ที่ออกล่าสุด (16 ส.ค. 2569, นักเรียนเลขที่ 1) อยู่ในคลิปบอร์ด **ยังไม่ถูกใช้และยังไม่หมดอายุ (14 วัน)**
→ วางลงแถบที่อยู่ของเบราว์เซอร์แล้วเปิดได้เลย ก่อนจะกดปุ่มนั้นซ้ำ (กดซ้ำ = ลิงก์นี้ถูกเพิกถอนทันที)

### รอบ 2 — ตัวเลขบนหน้าติดตาม
แตะ `AnalyticsService.js` + `JsAnalytics.html` (+ `JsImport.html` ข้อปี ค.ศ.) **ติด cache 15 นาที**

ทำทั้ง 6 ข้อย่อยของ 🔴 หน้าติดตามในรอบเดียว เพราะแตะไฟล์เดียวกันทั้งหมด และจ่ายค่ารอ 15 นาทีครั้งเดียว
**ลำดับภายในรอบ**: แก้ข้อ (1) เพดาน → (2) เกณฑ์เข้ารายการ → (3) คะแนนเทียบคนแย่สุด → (4) เดือนไม่เต็ม
เพราะข้อ (5) ข้อความสำเร็จรูป และ (6) นิยามไม่ตรงกัน **หายไปเองเมื่อ 4 ข้อแรกถูก** ไม่ต้องแก้แยก

### รอบ 3 — archive ให้กดซ้ำได้
แตะ `SemesterService.js` / `AttendanceService.js` + UI ภาคเรียนใน `JavaScript.html`

- 🔴 **rollback เมื่อ archive ล้มกลางคัน**
- 🟠 **ปุ่ม "เก็บข้อมูลเช็คชื่อ" หายเมื่อชีต archive มีแถวค้าง** ← ต้นเหตุเดียวกัน แก้พร้อมกัน

**ทำทีหลังทั้งที่เป็น 🔴 เพราะ 3 เหตุผล**
- ทดสอบแพงที่สุดในลิสต์ — ต้องล้าง/สร้างข้อมูล 2,586 แถวใหม่ทุกครั้งที่ลอง
- ครูใช้ปีละ 2 ครั้ง ไม่ใช่ทุกวัน ต่างจากรอบ 1 ที่ครูเจอทุกเช้า
- ทางแก้ที่ปลอดภัยที่สุด (`isSemesterAttendanceArchived_` ดูว่าชีตหลักหมดจริงไหม)
  แตะฟังก์ชันที่ `CLAUDE.md` ระบุว่าห้ามแตะโดยไม่ถาม → ต้องตกลงทางเลือกกันก่อนลงมือ

### รอบ 4 — หน้ารายงาน
แตะ `JsReports.html` (+ `StyleReport`) **ติด cache 15 นาที** — จัดคู่กันเพราะติด cache เหมือนกัน

- 🟠 **มือถือต้องเลื่อน 40+ ครั้งกว่าจะถึงตัวเลขรายคน**
- 🟡 **ไอคอนแว่นขยายลอยกลางที่ว่าง**

### ไม่ต้อง deploy — ทำแทรกระหว่างรอ cache ได้
- 🔴 **`#ERROR!` บน ปพ.6** — ตัว fix อยู่ใน `SeedTestData.js` ซึ่งเป็นไฟล์ dev ที่ถอดก่อนส่งมอบอยู่แล้ว
  **ของจริงที่ต้องทำคือ audit** ว่ามีเส้นทางไหนในโค้ด production ที่เขียนชื่อลงชีตโดยไม่ผ่าน
  `sanitizeSheetRows_` อีกไหม (`ImportService` / `StudentService` / `restoreBackup`)
  → ถ้า audit แล้วสะอาด ข้อนี้ลดเหลือ 🟡 ทันที
- ⏱ **สืบว่าทำไมเช็คชื่อวันเดียวช้ากว่ารายงาน 3 เดือน** — อ่านโค้ด + `_timing_log` ก่อน ยังไม่ต้องแก้
- **ความไม่สม่ำเสมอในโค้ดเบส 8 ข้อ** — แทรกเข้าไปในรอบที่แตะไฟล์นั้นอยู่แล้ว ไม่ตั้งรอบของตัวเอง
  (เช่น `esc()` และ `CSS ส่งซ้ำ 2 รอบ` ควรไปกับรอบ 4)

### คุณต้องทำเอง ผมทำแทนไม่ได้
- **ปิดอีเมลสรุปสัปดาห์บนสำเนาทดสอบ** ← ทำเลยวันนี้ ไม่ต้องรอรอบไหน มันส่งจริงทุกศุกร์
- ลบไฟล์ CSV ค้างใน Drive 2 ไฟล์
- สั่งพิมพ์ PDF จริงเพื่อดูว่าตาราง 40 แถวตัดหน้าตรงไหน
- ทดสอบ restore backup ทั้งเส้น (P0 ข้อเดียวที่ยังไม่เคยทดสอบ)

---

## รอบที่ 1 — ปิดครบแล้ว ทดสอบผ่านทุกข้อบนของจริง (17 ส.ค. 2569)

| ข้อ | หลักฐาน |
|---|---|
| ลิงก์ผู้ปกครอง | กล่องขึ้นพร้อมลิงก์เต็ม · คำเตือน "แสดงได้ครั้งเดียว" · **"หมดอายุ อา. 30 สิงหาคม 2569"** เป็น พ.ศ. · toast "คัดลอกลิงก์ผู้ปกครองแล้ว" |
| **หน้าผู้ปกครองด้วยลิงก์จริง** | **ทดสอบได้ครั้งแรกในโปรเจกต์** · 59 มา + 1 ขาด = 60 วันยืนยัน ตรงกับฝั่งครู · 98.3% = 59/60 · ปฏิทินติ๊ก 5 วันถูก · **ไม่หลุดข้อมูลเด็กคนอื่นเลย** |
| นำเข้า `enrolled_from` | ช่องขึ้น **01/11/2025** (วันเปิดภาคเรียน) ไม่ใช่ 17/08/2026 |
| **หน้าเช็คชื่ออ่านชีต archive** | 18 พ.ค. 2569: **0/39 → 39/39** · มา 33 · สาย 2 · ขาด 3 · ลาป่วย 1 = 39 พอดี |
| ชิป "ยังไม่ระบุกลุ่ม" | ขึ้นทั้ง 2 หน้า · 8+7+6+7+7+4 = 39 · กดแล้วกรองเหลือ 4 คน + ปุ่ม "ล้างตัวกรอง" |
| ตัวกรองข้ามวัน | ติ๊กไว้บน 18 พ.ค. → กด ◀ → **ติ๊กหลุดเอง** ขึ้น "ทั้งหมด 39 คน" |
| ปุ่มยืนยัน/ยกเลิกยืนยัน | **"กำลังยกเลิกการยืนยัน..."** และ **"กำลังยืนยัน..."** ขึ้นทันทีที่กดตกลง · คืนสภาพเดิมแล้ว |
| ปี พ.ศ. ในประวัติแก้ไข | **"จ. 17 ส.ค. 2569 09:33 น."** · เรียงใหม่→เก่าถูกต้อง (`changed_at` ดิบยังใช้เรียงได้) |

**★ พิสูจน์บั๊กเดิม 2 ตัวด้วยการทดลอง ไม่ใช่แค่อ่านโค้ด**
- ลิงก์จากคลิปบอร์ดตอนใช้โค้ดเก่าเปิดแล้วขึ้น "ลิงก์ไม่ถูกต้อง" แต่ลิงก์จากกล่องใหม่เปิดได้ทันที
  → การคัดลอกของโค้ดเก่าล้มเหลวเงียบๆ จริง ทั้งที่ toast บอกสำเร็จ
- `runP0Diagnostics` ฟ้อง `readAttendanceRecordsByDate_` คืน **0** แถวขณะที่ bucket เห็น **39** แถว
  → ตัดเรื่อง deployment version ออกได้ และชี้ไปที่ `createTextFinder` กับชีต archive

**บทเรียนวิธีทำงาน**: รัน Diagnostics จากเมนูในชีตก่อน deploy ประหยัดไป 1 รอบ และตัดคำถาม
"เว็บแอปรันโค้ดเวอร์ชันไหน" ออกได้เด็ดขาด — ควรทำแบบนี้ทุกครั้งที่แก้ฝั่ง server

---

## รอบที่ 4 — จาก UAT 16 ส.ค. 2569 (ยังไม่ได้แก้)

เดินในบทบาทครูประจำชั้นบนของจริง · เช็คชื่อ 39 คน → เทียบตัวเลข 3 หน้า → ตรวจเคสย้ายเข้า/ย้ายออก
→ แก้สถานะย้อนหลัง → เปิดเอกสาร ปพ.6
**ทดสอบที่ 500x699 ไม่ใช่ 390x844** เพราะหน้าต่างแคบกว่านั้นไม่ได้ → เรื่องล้นจอรายงานน้อยกว่าความจริง

### 💥 พัง

**ไม่มีแล้ว** — ข้อเดิม "ปุ่มลิงก์ผู้ปกครองกดแล้วไม่มีอะไรเกิดขึ้น" **ผมสรุปผิดเอง**
ตรวจซ้ำด้วยการถ่ายภาพถี่ๆ แล้วพบว่า **ปุ่มทำงานปกติ** — ขึ้น toast เขียว *"ออกลิงก์ผู้ปกครองใหม่แล้ว"*
ที่ราววินาทีที่ 5 และ**คัดลอกลิงก์ลงคลิปบอร์ดสำเร็จจริง** (พิสูจน์ด้วยการวางลงช่องค้นหาแล้วได้
`https://script.google.com/macros/s/...` ออกมา)

**สาเหตุที่ผมพลาด**: `toast()` [JavaScript.html:5024] ตั้งอายุไว้ **2,500 มิลลิวินาที** สำหรับ type success
และ toast โผล่หลังคลิกราว 5 วินาที (รอ server) ส่วนผมถ่ายภาพหลังคลิก 20+ วินาที
**จึงพลาดหน้าต่าง 2.5 วินาทีนั้นทั้ง 4 ครั้ง** → บันทึกบทเรียนไว้ใน `UAT.md` แล้ว
ของที่เหลือจากข้อนี้ย้ายลงไปอยู่ใน 🔴 ด้านล่าง ในขนาดที่ตรงกับความจริง

### 🔴 เสี่ยง

- [x] ~~**★★ หน้าเช็คชื่อไม่อ่านชีต archive — วันเก่าขึ้น "ยืนยันแล้ว" แต่ทุกคนเป็น "ยังไม่เช็ค"**~~
      **แก้แล้วในรอบ 1 รอทดสอบ** · เจอระหว่างทดสอบรอบ 1 เมื่อเปิด พ. 15 ก.ค. 2569 บนภาคเรียนที่เก็บถาวรแล้ว
      หัวข้อขึ้น **"ยืนยันแล้ว"** แต่ความคืบหน้าเป็น **0/39** และนักเรียนทุกคนขึ้นป้าย **"ยังไม่เช็ค"**
      **ต้นเหตุ**: `readAttendanceRecordsByDate_` เรียก `getAttendanceSourceSheet_` ซึ่งคืน**ชีตเดียว**
      ขณะที่สถานะวันอ่านผ่าน `getAttendanceDayReadSheets_` ที่รวม archive อยู่แล้ว → สองอย่างไม่ตรงกัน
      (เป็นเศษที่หลงเหลือจากการแก้ routing รอบก่อน ซึ่งแก้ฝั่งรายงานกับสถานะวันไปแล้วแต่ตกฝั่งนี้)
      **อันตรายกว่าที่เห็น**: ครูอาจเช็คชื่อทับวันที่มีข้อมูลอยู่แล้วโดยไม่รู้ตัว
      **ทางแก้ที่ทำ**: ให้อ่านรวมชีต archive (archive ก่อน ชีตหลักทีหลัง ตามกติกาแถวหลังชนะ)
      และ**ติด `sheet_name` ทุกแถว** + เพิ่ม `filterMainAttendanceRecords_` แล้วบังคับใช้ที่ทุกจุด
      ที่เอา `row_index` ไปเขียน/ลบ (`markStatus` · undo เดี่ยว · undo ชุด · `clearHolidayAttendance`)
      **ถ้าไม่กรอง row_index ของสองชีตจะชนกันและลบผิดแถวแบบเงียบๆ**


- [x] ~~**★ ลิงก์ผู้ปกครองไปอยู่ในคลิปบอร์ดเงียบๆ และ toast บอก "สำเร็จ" แม้การคัดลอกจะล้มเหลว**~~
      **ปิดในรอบที่ 1 ทดสอบผ่านแล้ว** — `copyParentLink` ใช้ `copyTextToClipboard_` ชุดเดียวกับ
      `JsReports`/`JsAnalytics`/`JsDashboard` ซึ่งลอง `navigator.clipboard` ก่อนและ**เช็คค่าที่
      `execCommand` คืนจริง** · แสดงลิงก์ในกล่องบนหน้าจอพร้อมปุ่มคัดลอกซ้ำ ไม่ฝากไว้กับ toast
      · server ส่ง `expires_at_th` มาให้เป็น พ.ศ.
      **พิสูจน์บั๊กเดิมด้วยการทดลอง**: ลิงก์จากคลิปบอร์ดตอนใช้โค้ดเก่าเปิดแล้วขึ้น "ลิงก์ไม่ถูกต้อง"
      แต่ลิงก์จากกล่องใหม่เปิดได้ทันที → การคัดลอกเดิมล้มเหลวเงียบๆ จริงทั้งที่ toast บอกสำเร็จ

- [x] ~~**★★ หน้าติดตาม: ตัวเลข "นักเรียนเสี่ยง N คน" ไม่ใช่จำนวนคน แต่เป็นความยาวของรายการที่ถูกตัด**~~
      **แก้แล้วในรอบ 2 ยังไม่ได้ทดสอบ** — แก้ทั้ง 4 ข้อต้นเหตุ ส่วนข้อ (5) ข้อความสำเร็จรูป
      และ (6) นิยามไม่ตรงกัน หายไปเองตามที่คาดไว้
      1. **เพดานไม่ใช่จำนวนคน** — server ส่ง `total_count` / `improved_total` / `declined_total` /
         `mon_fri_pattern_total` / `consecutive_streaks_total` มาคู่กับรายการที่ตัดแล้วทุกตัว
         client มี `totalCountOf_()` อ่านเฉพาะฟิลด์เหล่านี้ และขึ้น **"แสดง X จาก N คน"** เมื่อถูกตัดจริง
      2. **เกณฑ์เข้ารายการ** — เปลี่ยนจาก `rawScore > 0` เป็น **ขาด+สาย ≥ `attention_threshold_days`**
         ซึ่งเป็นค่าเดียวกับที่ครูตั้งบนหน้าตั้งค่าและหน้าแรกใช้อยู่ → นิยามตรงกันแล้ว
         และ**ตัด ลา ออกจากคะแนนทั้งหมด** (`RISK_WEIGHTS.leave` หายไป) เพราะ ลา คือการขาดที่
         ครูอนุมัติแล้ว และ `basis_days` ก็หักออกจากตัวหารอยู่แล้ว การนับซ้ำเป็นความเสี่ยงขัดกันเอง
      3. **คะแนนเป็นสเกลคงที่** — `calculateRiskPercent_` = คะแนนดิบ ÷ (วันเรียน × น้ำหนักขาด)
         → **100% = ขาดทุกวันเรียน** ไม่ใช่ "เทียบกับคนแย่ที่สุดในห้อง" อีกต่อไป
         ระดับผูกกับเส้น ปพ.6: **≥20% = สูง** (เท่ากับมาเรียนต่ำกว่า 80%) · ≥10% = ปานกลาง
      4. **บอกฐานที่ใช้คิดทุกที่** — การ์ดหลักขึ้น "จากวันเรียนที่มีข้อมูล N วัน" ·
         หัวข้อเทียบเดือนขึ้น "เดือนนี้มีข้อมูล 5 วันเรียน เทียบกับ 22 วันของเดือนก่อน"
         และเตือน "เดือนนี้ยังไม่จบ ตัวเลขจึงแกว่งง่ายกว่าปกติ" เมื่อวันเรียนน้อยกว่าครึ่ง
      5. **ข้อความสำเร็จรูปทั้ง 2 กล่อง**ใช้ยอดจริงแล้ว และต่อท้ายด้วย
         "(คิดจากวันเรียนที่มีข้อมูลแล้ว N วันในเดือนนี้)"

      **ทดสอบบนของจริงแล้ว 17 ส.ค. 2569 — ผ่านทุกข้อ**
      | เดิม | ใหม่ |
      |---|---|
      | เสี่ยง **15** (เพดาน) | **0** — ไม่มีใครขาด+สายถึง 3 ครั้งใน 5 วันเรียน |
      | ดีขึ้น **5** · แย่ลง **5** (เพดานทั้งคู่) | **17** · **9** — ยอดจริง ไม่ติดเพดานแล้ว |
      | หัวข้อไม่บอกเกณฑ์ | *"คัดจากนักเรียนที่ขาดรวมกับสาย ตั้งแต่ 3 ครั้งขึ้นไปในเดือนนี้ (เกณฑ์เดียวกับที่ตั้งไว้ในหน้าตั้งค่า) · ลาที่แจ้งแล้วไม่นับเป็นความเสี่ยง · อ้างอิงวันเรียนที่มีข้อมูล 5 วัน"* |
      | เทียบเดือนไม่บอกฐาน | *"เดือนนี้มีข้อมูล 5 วันเรียน เทียบกับ 23 วันของเดือนก่อน — เดือนนี้ยังไม่จบ ตัวเลขจึงแกว่งง่ายกว่าปกติ"* |
      | ข้อความหาผู้ปกครองว่า "ควรติดตาม 15 คน" | ประโยคนั้นหายไปเอง (0 คน) และต่อท้ายด้วย *"(คิดจากวันเรียนที่มีข้อมูลแล้ว 5 วันในเดือนนี้)"* |

      ⚠️ **ยังไม่ได้ยืนยันด้วยตา 1 จุด**: บรรทัด "แสดง 5 จาก 17 คน" ในรายการเทียบเดือน
      เพิ่มทีหลังจึงยังติด cache 15 นาทีตอนทดสอบ · ความเสี่ยงต่ำเพราะเรียก `renderTruncationNote_`
      ตัวเดียวกับที่ยืนยันแล้วในส่วนดัชนีความเสี่ยง — ดูตอนเปิดหน้าติดตามครั้งถัดไป

      **หมายเหตุ**: หน้าแรกขึ้น 5 คน หน้าติดตามขึ้น 0 คน — ต่างกันเพราะ**หน้าต่างเวลา**
      (30 วันล่าสุด vs เดือนปฏิทิน) ไม่ใช่เพราะนิยาม และตอนนี้ทั้งสองหน้าบอกหน้าต่างของตัวเองชัดแล้ว

- [x] ~~**archive ล้มเหลวกลางคันแล้วไม่มี rollback ทำให้กดซ้ำไม่ได้**~~ + ~~**ปุ่มหายถาวร**~~
      **แก้แล้วในรอบ 3 ยังไม่ได้ทดสอบ** — สองข้อนี้เป็นอาการของต้นเหตุเดียวกัน

      **ต้นเหตุจริงลึกกว่าที่เขียนไว้เดิม**: `isSemesterAttendanceArchived_` **ตัวเดียว**
      ถูกใช้ตอบ **3 คำถามที่ต่างกัน** — (1) ต้องอ่านชีต archive ด้วยไหม [routing]
      (2) เก็บถาวรเสร็จหรือยัง [ป้าย+ปุ่มบนหน้าจอ] (3) ยังมีข้อมูลอยู่ไหม [guard ตอนลบภาคเรียน]
      คำตอบเดิม "ชีต archive ไม่ว่าง" **ถูกสำหรับ (1) และ (3) แต่ผิดสำหรับ (2)**
      → แถวค้างในชีต archive แม้แถวเดียวก็ทำให้ป้าย "เก็บข้อมูลแล้ว" ติดและปุ่มหาย
      **ทางแก้**: แยกเป็น `isSemesterAttendanceFullyArchived_` [SemesterService.js] สำหรับหน้าจอ
      = archive มีของ **และ** ชีตหลักไม่เหลือของภาคเรียนนี้แล้ว · routing กับ guard ลบใช้ตัวเดิมไม่แตะ
      (อ่านคอลัมน์วันที่ของชีตหลักครั้งเดียวแล้วใช้ซ้ำทุกภาคเรียน กันไม่ให้หน้าตั้งค่าช้าลง)

      **★ เรื่อง rollback — ตัดสินใจว่าไม่ทำ และนี่คือเหตุผล**
      TODO เดิมเขียนว่าควร "ครอบ archive ด้วย rollback" **แต่พออ่านลำดับจริงแล้วอันตราย**:
      ถ้าการลบล้มกลางคัน แถวในชีตหลักอาจถูกลบไปแล้ว**บางส่วน** การย้อนชีต archive ออก
      จะทำให้แถวเหล่านั้น**หายไปทั้งคู่** — ชีต archive คือตาข่ายรองของข้อมูลที่ลบไปแล้ว
      **สิ่งที่ทำแทน**: จับ error ตอนลบ แล้วบอกครูตรงๆ ว่าข้อมูลยังอยู่ครบและกดซ้ำได้
      ปลอดภัยเพราะ `appendArchive...IfMissing_` ข้าม id ที่มีอยู่แล้ว และปุ่มยังอยู่จากการแก้ข้างบน

      **ทดสอบบนของจริงแล้ว 17 ส.ค. 2569 — ผ่านทั้ง 3 ข้อ**
      ข้อมูลเดิมแยกโค้ดเก่า/ใหม่ไม่ได้ (ชีตหลักว่างอยู่แล้ว ทั้งสองเวอร์ชันตอบเหมือนกัน)
      จึง**สร้างสถานะที่แยกได้**: เช็คชื่อเลขที่ 1 ในวันนี้ (17 ส.ค. อยู่ในช่วง 2/2569)
      → ชีตหลักมีแถวใหม่ทั้งที่ชีต archive มี 2,587 แถว = เคสที่ครูจะเจอจริงเมื่อมีข้อมูลใหม่หลัง archive
      | ทดสอบ | ผล |
      |---|---|
      | 2/2569 (archive แล้ว + มีข้อมูลใหม่) | **ป้าย "เก็บข้อมูลแล้ว" หายไปถูกต้อง** ← บั๊กเดิมหายแล้ว |
      | 1/2569 (ไม่มีข้อมูล ไม่มีชีต archive) | ไม่มีป้าย · ปุ่มครบ 4 ปุ่ม |
      | สลับให้ 1/2569 ใช้งาน แล้วดู 2/2569 | **ปุ่ม "เก็บข้อมูลเช็คชื่อ" กลับมาแล้ว** ← เดิมหายถาวร |
      สลับกลับเป็น 2/2569 และล้างข้อมูลทดสอบคืนสภาพ 0/39 แล้ว

      **★ ป้ายไม่กลับมาหลังล้างข้อมูล — ตรวจแล้วเป็นคำตอบที่ถูก ไม่ใช่บั๊ก**
      ตอนทดสอบรอบ 1 ผมกด **ยกเลิกการยืนยัน → ยืนยันวันนี้** ที่ 18 พ.ค. 2569
      `saveDayStatus_` เขียนลง**ชีตหลัก**เสมอ (เส้นทางเขียนไม่เคยแตะชีต archive)
      ชีต `สถานะวัน` หลักจึงมีแถว `2026-05-18` ซึ่งอยู่ในช่วง 2/2569 จริงๆ
      → `isSemesterAttendanceFullyArchived_` ตอบ false ถูกต้องแล้ว เพราะยังมีข้อมูลไม่ได้เก็บถาวรจริง
      **เป็นการยืนยันเพิ่มว่าตัวใหม่ไวกับชีต `สถานะวัน` ด้วย ไม่ใช่ดูแค่ชีต `เช็คชื่อ`**
      (ของเดิมจะซ่อนความจริงข้อนี้ไว้ใต้ป้าย "เก็บข้อมูลแล้ว")

### 🟠 กวนใจ

- [x] ~~**กดแล้วจอนิ่ง ~16 วินาทีโดยไม่มีตัวบอกสถานะ**~~ **ปิดในรอบที่ 1 ทดสอบผ่านแล้ว**
      (1) **ยืนยันวันนี้ / ยกเลิกการยืนยัน** — ใส่สถานะกำลังทำงานด้วย `setActionButtonState` ที่มีอยู่แล้ว
      บนของจริงขึ้น "กำลังยืนยัน..." และ "กำลังยกเลิกการยืนยัน..." ทันทีที่กดตกลง
      (2) **ประวัติแก้ไข — ไม่ได้แก้ เพราะไม่ได้พัง** `showAttendanceAuditTrail` มีข้อความ
      "กำลังโหลดประวัติการแก้ไข..." อยู่แล้วในโค้ด · ที่ผมจดว่า "เนื้อว่างเปล่า" น่าจะอ่านจอผิด
      **บทเรียน**: อ่านโค้ดของปุ่มก่อนสรุปว่าไม่มี feedback (ดู `UAT.md`)
- [x] ~~**ตัวกรอง "เฉพาะคนที่ยังไม่เช็ค" ติดค้างข้ามวัน**~~ **ปิดในรอบที่ 1 ทดสอบผ่านแล้ว**
      ล้างตัวกรองเองเมื่อเปลี่ยนวันใน `loadAttendance` · คำค้นหาและชิปกลุ่มไม่ล้าง เพราะครูใช้ข้ามวันได้
      ยืนยันบนของจริง: ติ๊กไว้บน 18 พ.ค. → กด ◀ → ติ๊กหลุดเอง ขึ้น "ทั้งหมด 39 คน"
- [x] ~~**หน้ารายงานบนมือถือต้องเลื่อน 40+ ครั้งกว่าจะถึงตัวเลขรายคน**~~ **แก้แล้วรอบ 4 ยังไม่ได้ทดสอบ**
      ก่อนถึง "สรุปรายคน" มีบล็อกคั่น **6 ชั้น** (spotlight → สรุป 5 วินาที → ภาพรวมช่วงวันที่ →
      วันหยุดที่ขัดกัน → เทียบช่วงก่อนหน้า → ควรทำอะไรต่อ → ตัวกรอง)
      **ทางแก้**: ใส่ปุ่ม **"ข้ามไปรายชื่อรายคน"** ใน spotlight ซึ่งเป็นบล็อกแรกสุดของหน้า
      และใส่ `id="reports-summary-people"` ให้ section ปลายทาง · ไม่ยุบการ์ดทิ้ง
      เพราะครูที่เปิดครั้งแรกยังได้ประโยชน์จากมัน แค่ให้คนที่รู้อยู่แล้วข้ามได้
      ✅ **ทดสอบผ่านแล้ว** — กดครั้งเดียวจากบล็อกแรกถึงการ์ดนักเรียนเลขที่ 1 เลย

### 🟡 ขัดใจ

- [x] ~~**`readSchoolCalendarEntryByDate_` ใช้ `createTextFinder` กับคอลัมน์วันที่**~~
      **แก้แล้ว ยังไม่ได้ทดสอบ · ไม่เหลือ `createTextFinder` ในโปรเจกต์แล้ว**
      เปลี่ยนไปอ่านจาก `getAllSchoolCalendarEntries_` (ตัวที่สร้างตอนแก้ความช้า)
      ซึ่งอ่านทั้งชีตด้วย `getValues()` + `formatDate_` จึงรับได้ทั้ง Date และข้อความ

      **★ เหตุผลที่กล้าแก้ทั้งที่แตะเส้นทางลบปฏิทิน** — ไล่แล้วพบว่า `row_index`
      **ไหลไปถึง `deleteRow` จริง** (`deleteSchoolCalendarEntry` → `getSchoolCalendarRowByDate_`
      → `getSchoolCalendarEntryByDate_` → ตัวนี้) แต่ `getAllSchoolCalendarEntries_`
      ให้ `row_index` **ชุดเดียวกันเป๊ะ** เพราะสแกนทั้งชีตเหมือนกัน นับ `index + 2` เหมือนกัน
      และตัดคีย์ซ้ำด้วย `shouldReplaceSchoolCalendarEntry_` **ตัวเดียวกัน**
      ผลพลอยได้: ของเดิมอ่านชีตซ้ำ 1 ครั้งต่อแถวที่ match ตอนนี้ไม่อ่านชีตเพิ่มเลย

      **บันทึกอันตรายที่เจอระหว่างไล่** (ลง `ARCHITECTURE.md` แล้ว)
      `row_index` ที่เอาไปลบแถว**มาจาก cache 300 วินาที + memo ระดับ execution**
      สิ่งที่กันไม่ให้ลบผิดแถวมีแค่ 2 อย่าง คือ `LockService.getDocumentLock()` ครอบไว้
      และ `invalidateSchoolCalendarCaches_()` ที่ bump version ทุกครั้งหลังแก้
      สำคัญเป็นพิเศษเพราะ `saveSchoolCalendarEntry` เรียก `sortSchoolCalendarSheet_`
      ซึ่ง**สลับลำดับทุกแถว** → `row_index` เก่าทั้งชุดใช้ไม่ได้ทันที
      **เพิ่ม mutation ใหม่ที่แตะชีตปฏิทินต้องมีครบทั้งสองข้อ**

      **★★ ทดสอบผ่านครบแล้ว 18 ส.ค. 2569**
      - `calendar:date_lookup_vs_scan` **เขียว** = เส้นทางที่ cache ไว้ให้ผลตรงกับการสแกนชีตตรงๆ
      - บนของจริง: เพิ่ม อา. 30 ส.ค. → **218 → 219 ทันที** · ลบทิ้ง → **219 → 218 ทันที**
        พร้อมข้อความ "ไม่พบวันที่ตรงกับที่ค้น จากทั้งหมด 218 รายการ"
        → **`row_index` จากเส้นทางใหม่ชี้ถูกแถว ลบถูกวัน** ปฏิทินคืนสภาพ 218/0
      - **ของแถมที่วัดได้**: `pf_calendar_prime_ms` บน pass1 ลงมาเหลือ **55 ms**
        (เดิม 420–1,400) เพราะการหาวันเดี่ยวกับการอ่านทั้งช่วง**ใช้ cache ก้อนเดียวกันแล้ว**
        pass1 รวม 2,175 ms · pass2 166 ms · **pass3 (วันที่สอง) 669 ms**
      ★ **วัดแล้ว 17 ส.ค. 2569 — ยังไม่พัง แต่ที่ไม่พังคือความบังเอิญ ไม่ใช่เพราะโค้ดถูก**
      `calendar:date_lookup_vs_scan` ได้ `dates_via_scan: 218` · `lookup_found: true`
      **แต่ข้อมูลที่ได้มาพร้อมกันล้มสมมติฐานเดิมทั้งหมด**: คอลัมน์วันที่**เป็น Date object
      ทุกชีต** ไม่ใช่แค่ชีต archive — `สถานะวัน` · `ปฏิทินวันเรียน` · `ภาคเรียน` ก็เป็น Date
      → **ตัวที่ตัดสินว่า TextFinder เจอหรือไม่ คือ number format ของเซลล์ ซึ่งมองไม่เห็นจากโค้ด**
      `ปฏิทินวันเรียน` ได้ format `yyyy-mm-dd` ตามสตริงที่โค้ดพิมพ์ลงไป จึงแสดง `2025-11-03` แล้วเจอ
      ส่วน `_att_archive_1` รับ Date object มาจาก `getValues()` จึงได้ format เริ่มต้น
      แสดง `18/5/2026` แล้วหาไม่เจอ — **ต่างกันที่ format ไม่ได้ต่างกันที่ชนิดของค่า**
      → แก้ `ARCHITECTURE.md` ที่เขียนว่า "ชีตหลักเก็บเป็นข้อความ" แล้ว (ไม่จริง)
      **เพิ่มความเร่งด่วน**: ไฟล์สำรองเขียนวันที่กลับเป็นข้อความเสมอ (`exportSheetSnapshotFromSheet_`
      แปลงทุกเซลล์ผ่าน `formatDate_`) กู้คืนทับที่เดิมไม่มีปัญหาเพราะ `clearContents` เก็บ
      number format ไว้ แต่**กู้คืนลงสำเนาใหม่** ซึ่งเป็นสถานการณ์จริงของการส่งโค้ดใหม่
      `restoreBackupSheet_` จะ `insertSheet` ใหม่หมด → Sheets อาจแปลงข้อความเป็น Date เอง
      เป็นบั๊กชนิดเดียวกับที่เพิ่งแก้ใน `AttendanceService` — ถ้าเซลล์วันที่เป็น Date object
      TextFinder จะหาไม่เจอเงียบๆ · ปฏิทินไม่ถูก archive จึงยังไม่เคยพัง
      แต่เกิดได้ถ้าครูแก้ชีตด้วยมือหรือกู้คืนจากไฟล์สำรอง
      (`buildSchoolCalendarEntry_` เช็ค `instanceof Date` อยู่แล้ว = โค้ดรู้ว่าเป็นไปได้)
      ⚠️ **ยังไม่แก้เพราะทดสอบไม่ได้ในรอบนี้** และ `CLAUDE.md` ระบุว่าเส้นทางลบปฏิทินเป็นจุดห้ามแตะ
      → แก้พร้อมรอบที่ได้ทดสอบปฏิทินจริง

- [x] ~~**ไม่มีชิปให้กรองหานักเรียนที่ยังไม่ระบุกลุ่ม + สองหน้าแสดงไม่ตรงกัน**~~
      **ปิดครบแล้ว** — ทำเป็น 2 รอบ
      **รอบ 1 (ทดสอบผ่านแล้ว)** เพิ่มชิป "ยังไม่ระบุกลุ่ม" ทั้งหน้าเช็คชื่อและหน้านักเรียน
      ด้วยค่าพิเศษ `NO_GROUP_FILTER_` (ตัวกรองเทียบชื่อกลุ่มแบบตรงตัว จึงต้องมีค่าที่ไม่ชนชื่อจริง)
      และเพิ่มป้าย "ยังไม่ระบุกลุ่ม" บนการ์ดหน้าเช็คชื่อให้ตรงกับหน้านักเรียน
      บนของจริง: 8+7+6+7+7+4 = 39 · กดชิปแล้วเหลือ 4 คน
      **รอบ 5 (ยังไม่ได้ทดสอบ)** ปิดสองเรื่องที่เหลือ
      - **การ์ดสรุป "N ยังไม่ระบุกลุ่ม" กดได้แล้ว** — `renderStudentsMetric` รับ action เพิ่ม
        และเปลี่ยนเป็น `<button>` ตอนกดได้ เพื่อให้ได้ focus กับ Enter/Space ฟรีจากเบราว์เซอร์
        ข้อความรองเปลี่ยนเป็น "กดเพื่อดูรายชื่อ แล้วจัดกลุ่มให้ครบ"
      - **หัวข้อบนหน้า** เดิมบอกแค่ "แบ่งเป็น 5 กลุ่ม" ทำให้ครูเข้าใจว่าทุกคนอยู่ในกลุ่มแล้ว
        เพิ่ม "และยังไม่ระบุกลุ่มอีก N คน" ต่อท้าย
      ✅ **ทดสอบผ่านแล้ว 17 ส.ค. 2569** — หัวข้อขึ้น "แบ่งเป็น 5 กลุ่ม และยังไม่ระบุกลุ่มอีก 4 คน" ·
      กดการ์ดแล้วกรองเหลือ 4 คน ป้าย "ตรงเงื่อนไข 4 คน" ขึ้น และ**เลื่อนไปที่รายการให้เอง**
      **★ เจอตอนทดสอบว่ากดแล้วไม่เห็นอะไรเปลี่ยน** เพราะการ์ดอยู่ท้ายหน้าแต่รายการอยู่เหนือมัน
      = ปุ่มที่ทำงานแต่ไม่มี feedback ซึ่งเป็นปัญหาแบบเดียวกับที่ไล่แก้มาทั้งโปรเจกต์
      แยกเป็น `showUngroupedStudents()` ที่เลื่อนให้ด้วย (ชิปไม่ต้องเลื่อน เพราะติดรายการอยู่แล้ว)

- [x] ~~**ไอคอนแว่นขยายลอยอยู่กลางที่ว่าง**~~ **แก้แล้วรอบ 4 ยังไม่ได้ทดสอบ**
      **ต้นเหตุตรงกับความสูงที่วัดได้เป๊ะ**: `.reports-search-box` มี `flex: 1 1 260px`
      พอ `.reports-filter-toolbar` เปลี่ยนเป็น `flex-direction: column` ที่ ≤900px
      เลข `260px` กลายเป็น **flex-basis ของความสูง** กล่องจึงสูง 260px ทั้งที่ช่องค้นหาสูงแค่ ~3rem
      ไอคอนถูกวาง `position:absolute; top:50%` จึงไปลอยกลางที่ว่างนั้น
      (media query เดิมล้างแค่ `min-width`/`width` ไม่ได้ล้าง `flex`)
      **ทางแก้**: เพิ่ม `flex: 0 0 auto` ใน media query ≤900px
      ✅ **ทดสอบผ่านแล้ว 17 ส.ค. 2569 ที่ 620px** — ไอคอนอยู่ในช่องค้นหา ไม่มีที่ว่างคั่นก่อนชิปกรอง
- [x] ~~**ปี ค.ศ. โผล่ปนกับ พ.ศ.**~~ **ปิดครบทั้ง 2 จุดแล้ว**
      ~~1. ประวัติแก้ไข~~ **ปิดในรอบที่ 1** เพิ่ม `changed_at_th` แยกฟิลด์ (คง `changed_at` ดิบไว้
      เพราะ `entries.sort` เรียงด้วยสตริงนั้น) ยืนยันบนจอแล้วขึ้น "จ. 17 ส.ค. 2569 09:33 น."
      ~~2. หน้านำเข้า~~ **ปิดในรอบที่ 5 ยังไม่ได้ทดสอบ** `formatEffectiveFrom()` บวก 543 แล้ว
      **คงรูปแบบ `d/m/y` ไว้ ไม่ใช้ชื่อเดือนไทย** เพราะข้อความนี้อยู่ติดกับช่อง `<input type="date">`
      ที่เบราว์เซอร์วาดเป็น dd/mm/yyyy — ถ้าเปลี่ยนเป็น "1 พ.ย. 2568" สองบรรทัดที่ติดกันจะอ่านไม่เข้าชุด
      ⚠️ **ยืนยันด้วยตายังไม่ได้ ไม่ใช่ว่าไม่ได้ทดสอบ** — ผลของฟังก์ชันนี้โผล่แค่ 2 ที่และ**ทั้งคู่
      อยู่หลังการอัปโหลด CSV** (ป้ายในผลตรวจไฟล์ [JsImport.html:513] และข้อความยืนยันก่อนนำเข้า [:561])
      ซึ่งต้องขับกล่องเลือกไฟล์ที่อยู่นอกเบราว์เซอร์ · `01/11/2025` ที่เห็นบนหน้าคือช่อง
      `<input type="date">` ที่เบราว์เซอร์วาดเอง **ไม่ใช่ฟังก์ชันนี้ อย่าใช้ยืนยัน**
      → ให้ตรวจตอนที่ทดสอบการนำเข้า CSV จริง (อยู่ในรายการห้ามกดตอนนี้)
      ⚠️ **แยกให้ออกจากช่อง `<input type="date">` ที่ขึ้น 16/08/2026 เหมือนกัน** — ช่องนั้นเบราว์เซอร์
      เป็นคนวาดตาม locale ของเครื่อง แอปสั่งไม่ได้ ไม่ใช่บั๊กของโค้ด (แต่ **ค่า**ในช่องผิดจริง ดูข้อ 🔴)

### ⏱ ความเร็ว — เกินเกณฑ์ 8 วินาทีหลายหน้า

วัดจากคลิก → เนื้อหาขึ้นครบ **เป็นค่าหยาบ ถ่ายภาพเป็นช่วงๆ ไม่ได้จับต่อเนื่อง**

| หน้า | ช่วงข้อมูล | วินาที |
|---|---|---|
| หน้าแรก | เดือน ส.ค. 2569 | 3–6 |
| เช็คชื่อ 39 คน | 1 วัน | **8–14** |
| รายงานสรุป | ทั้งภาคเรียน | **~10** |
| ยืนยันวัน | — | **~16** |
| ประวัติแก้ไข | 1 วัน | **~16** |
| เปลี่ยนวันด้วยปุ่ม ◀ | 1 วัน | **~15** |
| รายงานรายวัน (เดสก์ท็อป) | 3 เดือน 39 คน × 60 วัน | **≤5** ✓ |
| หน้าติดตาม | เดือน ส.ค. 2569 | **9–14** (แต่มีจอกำลังโหลดบอกสถานะ) |
| หน้าตั้งค่า | — | **~10** |
| หน้านำเข้า | — | ≤8 ✓ |

- [x] ~~**★★ ความช้าหน้าเช็คชื่อ**~~ **แก้และทดสอบผ่านครบแล้ว 18 ส.ค. 2569**
      (รายละเอียดการวัดและการทดสอบอยู่ข้างล่าง · ส่วนที่ยังเหลือแยกเป็นข้อใหม่ท้ายก้อนนี้)
      `perf:attendance_vs_report` [Diagnostics.js] 17 ส.ค. 2569 · วัดในการรันเดียวกันจึงเทียบได้จริง

      | สิ่งที่วัด | เวลา |
      |---|---|
      | เช็คชื่อ 1 วัน 39 คน **ครั้งแรก (cache ยังไม่มี)** | **4,734 ms** |
      | เช็คชื่อวันเดียวกัน ครั้งที่สอง (cache อุ่น) | **229 ms** |
      | รายงานรายวันทั้งภาคเรียน **218 วัน × 40 คน** | **1,251 ms** |

      แยกตามขั้นตอนของครั้งแรก
      `preflight_validate_ms=845` · `students_ms=556` · **`records_snapshot_ms=2301`**
      (ในนั้น **`raw_records_ms=2203`**) · `records_day_status_ms=380` · `alerts_ms=344` · **`rows_ms=1`**

      **ตอบคำถามเดิมได้ว่า "ไม่ใช่ความช้าของ Apps Script เอง"**
      - **`rows_ms=1`** — การปั้นแถวนักเรียน 39 คนใช้ **1 มิลลิวินาที** → ความช้าอยู่ที่**การดึงข้อมูล
        ล้วนๆ ไม่ใช่การประมวลผล** ปิดประตูการไปแก้ตรรกะเปล่าๆ
      - **`raw_records_ms=2203` คือตัวการ** — `readAttendanceRecordsByDate_` สแกน
        **คอลัมน์วันที่ทั้ง 2,586 แถวของ `_att_archive_1`** ทุกครั้งที่เจอวันที่ยังไม่มีใน cache
        → **ต้นทุนโตตามขนาด archive ไม่ใช่ตามจำนวนข้อมูลของวันนั้น**
      - ฝั่งรายงานใช้ `getCachedAttendanceDateBuckets_` ซึ่ง**อ่านทั้งชีตครั้งเดียวแล้วแยกเป็นถัง
        ตามวันที่** ใช้ได้ทั้ง 218 วันพร้อมกัน → จ่ายค่าสแกนครั้งเดียว
      - **สรุป: เส้นทางรายวันจ่ายค่าสแกนทั้งชีต "ต่อหนึ่งวัน" ส่วนรายงานจ่าย "ครั้งเดียวสำหรับทุกวัน"**
      ⚠️ **ข้อจำกัดของการวัด**: `report_uncached_ms` เลี่ยงแค่ cache ของ daily grid
      ตัว `attendance_date_buckets` อุ่นอยู่แล้วจาก check ก่อนหน้า จึงไม่ใช่เลข cold แท้
      แต่ข้อสรุปไม่เปลี่ยน เพราะฝั่งรายวัน**เย็นจริง** และถังที่มันเอาไปใช้ได้ก็สร้างเสร็จอยู่แล้ว

      **★★ แก้แล้วทั้ง 2 ต้นเหตุ ยังไม่ได้ทดสอบ** — เหตุผลและราคาที่ยอมจ่ายอยู่ที่ `DECISIONS.md` ข้อ 32
      1. **ปฏิทิน** แยก `getAllSchoolCalendarEntries_` (คีย์ไม่ผูกช่วง) แล้วกรองในหน่วยความจำ
      2. **เรคอร์ดรายวัน** ให้ `getCachedRawAttendanceRecordsByDate_` เสิร์ฟจากถัง

      **วัดยืนยันแล้วก่อนแก้** (`perf:attendance_vs_report` รอบที่ 2 · cache เย็น)
      `pf_calendar_prime_ms` **808** จาก `preflight_validate_ms` **1,087** = **74%**
      ส่วน `pf_source_ms` กับ `pf_guard_ms` เป็น **0** ทั้งคู่ → วินิจฉัยถูก
      `raw_records_ms` **3,738** · `students_ms` 665 · `records_day_status_ms` 367 ·
      `alerts_ms` 338 · `pf_settings_ms` 279 · **`rows_ms` 1**
      ⚠️ **ตัวเลขรอบนี้สูงกว่ารอบแรกทั้งกระดาน** (รวม 6,570 vs 4,734 ms · `duration_ms`
      34,533 vs 27,689) = Apps Script แกว่งเอง **ให้ดูสัดส่วน ไม่ใช่ตัวเลขดิบ**

      **★★ วัดหลังแก้แล้ว — ข้อ 2 ได้ผลมาก ข้อ 1 ยังพิสูจน์ไม่ได้เพราะ check ออกแบบผิด**
      | ขั้นตอน | ก่อน | หลัง |
      |---|---|---|
      | `raw_records_ms` | 3,738 | **313** (−92%) |
      | `records_snapshot_ms` | 3,843 | **449** |
      | รวม pass1 | 6,570 | **3,398** |
      | `pf_calendar_prime_ms` | 808 | **789** ← ไม่ขยับ |

      **`pf_calendar_prime_ms` ไม่ขยับไม่ได้แปลว่า fix ไม่ทำงาน — check วัดมันไม่ได้เลย**
      fix แก้เคส "เปลี่ยนวันที่แล้วคีย์ใหม่" แต่ pass1/pass2 อยู่บน**วันเดียวกัน**
      ทั้งโค้ดเก่าและใหม่ pass1 ต้องอ่านชีตหนึ่งครั้ง (cache ว่าง) และ pass2 ก็ hit เหมือนกัน
      → เพิ่ม **pass3 บนวันที่ต่างออกไป** ซึ่งเป็นเคสเดียวที่แยกสองเวอร์ชันออกจากกันได้
      **เกณฑ์ตัดสิน**: `attendance_pass3_other_date_steps` → `pf_calendar_prime_ms`
      ถ้า **ใกล้ 0** = fix ทำงาน · ถ้า **~800** = fix ไม่ทำงาน ต้องกลับไปหาสาเหตุ

      **บทเรียน**: instrument ที่วัดผิดเคสให้คำตอบที่ดูเหมือนล้มเหลว
      ก่อนสรุปว่า fix ไม่ได้ผล **ต้องเช็คก่อนว่าการวัดแตะเคสที่ fix แก้จริงไหม**

      **★★ pass3 ตอบแล้ว — ข้อ 1 ทำงานจริง ปิดงานนี้ได้**
      วันที่สอง (2026-05-19) ได้ **`pf_calendar_prime_ms` = 68 ms**
      ช่วงที่ขอคือ `[2026-04-14, 2026-05-19]` **คนละช่วงกับ pass1** (`[2026-04-13, 2026-05-18]`)
      → โค้ดเก่าต้อง miss และอ่านชีตใหม่ (~800 ms) · โค้ดใหม่ hit เพราะคีย์ไม่ผูกช่วง
      **68 vs ~800 คือหลักฐานตรงตัว ไม่ใช่การตีความ**

      | | pass1 (วันแรก cache ว่าง) | pass2 (วันเดิม) | **pass3 (วันที่สอง)** |
      |---|---|---|---|
      | `pf_calendar_prime_ms` | 688 | 45 | **68** |
      | `raw_records_ms` | 157 | — | **175** |
      | **รวม** | 2,827 | 281 | **919** |

      **สรุปผลรวมของงานนี้ — ครูกดไล่วัน จาก ~4.7-6.6 วิ เหลือ ~0.9 วิ ตั้งแต่วันที่สอง**
      วันแรกของ session ยังราว 2.8 วิ เพราะต้องอ่านชีตครั้งแรกอยู่ดี
      ตัวเลข ปพ.6/CSV เท่าเดิมทุกหลัก (`2077,114,119,23,19,,2352,94.8`) และ
      `attendance:daily_reads_archive` ยังเขียว = **ถังกับการอ่านตรงให้ผลตรงกัน**
      (ปฏิทิน 208→209 กับ CSV ที่มี 2026-08-18 เพิ่มมา เป็นเพราะวันเปลี่ยน ไม่ใช่ผลของการแก้)

      **★★ ทดสอบบนของจริงแล้ว 18 ส.ค. 2569 — ผ่านครบ 3 ข้อ ปิดงานนี้ได้**
      | ทดสอบ | ผล |
      |---|---|
      | **cache ปฏิทินถูกล้างจริงไหม** (ความเสี่ยงหลัก) | เพิ่ม อา. 23 ส.ค. เป็นวันเรียน → **218 → 219 ทันที** · ลบทิ้ง → **219 → 218 ทันที** พร้อมข้อความ "ไม่พบวันที่ตรงกับที่ค้น จากทั้งหมด 218 รายการ" |
      | กด ◀ ไล่วัน 18→17→16→15→14 ส.ค. | ขึ้นครบทุกวัน · **14 ส.ค. ได้ 39/39 · มา 35 · สาย 2 · ขาด 2** = อ่านผ่านเส้นทางถังบน deployment จริงถูกต้อง |
      | หน้ารายงาน | **ยืนยันแล้ว 60 จาก 218 วัน · ยังไม่ยืนยัน 158 วัน** เท่าฐานเดิมทุกตัว |
      ⚠️ **ไม่รายงานเป็นวินาทีจากการเดินมือ** เพราะเวลาที่จับได้รวม overhead ของเครื่องมือ
      ตัวเลขความเร็วที่เชื่อถือได้อยู่ที่ `perf:attendance_vs_report` เท่านั้น
      ✅ **คืนสภาพครบแล้ว** ปฏิทินกลับเป็น 218/0 และไม่ได้แตะข้อมูลเช็คชื่อเลย

      **ยังเหลือ ~1.6 วินาทีที่ไม่ได้แตะ** (แยกเป็นข้อใหม่ด้านล่าง)

      ~~ทางแก้ที่ตรงจุด: ให้ `getCachedRawAttendanceRecordsByDate_` เสิร์ฟจากถัง~~

      **★ แก้ข้อความที่ผมเขียนไว้เองรอบก่อน — "ตัวขวาง" ที่บันทึกไว้ไม่มีจริง**
      รอบก่อนเขียนว่าเรคอร์ดในถัง **ไม่มี `sheet_name` / `row_index`**
      **ไม่จริง** — `readAllAttendanceRecords_` [ReportService.js:556-561] ใส่ทั้งสองฟิลด์
      พร้อม comment กำกับไว้แล้วว่าทำไม (ผม grep แค่ `AttendanceService.js`
      ไม่ได้ดู `ReportService.js` จึงสรุปผิด)
      ตรวจเพิ่มแล้วด้วยว่า **ลำดับการอ่านตรงกัน** — ทั้งสองเส้นทางใช้
      `getAttendanceReadSheets_` (archive ก่อน ชีตหลักทีหลัง) กติกาแถวหลังชนะจึงไม่เปลี่ยน
      และ `row_index` จาก cache ปลอดภัยเท่ากับของเดิม เพราะ `getOrBuildLargeCachedJson_`
      ทำคีย์ผ่าน `buildDerivedCacheKey_` และ `invalidateAttendanceCaches_` เรียก
      `bumpDerivedDataCacheVersion_` ซึ่งล้าง **ทุก** derived cache พร้อมกัน

      **★★ อ่านโค้ดต่อแล้วเจอต้นเหตุของอีก 845 ms — เล็กกว่าที่คิดมาก และไม่แตะ `row_index` เลย**
      `getSchoolCalendarEntriesForRange_` [CalendarService.js:232] cache ด้วยคีย์
      **`['school_calendar_entries', from, to]` = ผูกกับช่วงวันที่แบบเป๊ะ**
      แต่ `primeAttendanceDailyCalendarEntries_` เรียกด้วยช่วง **`date-35` ถึง `date`**
      → **เปลี่ยนวันที่ = คีย์ใหม่ = cache miss ทุกครั้ง** ครูกด ◀ ▶ ไล่วันจึงพลาด cache 100%
      **และตัว builder อ่านทั้งชีตอยู่แล้ว** (บรรทัด 239 `getRange(2, 1, lastRow-1, ...)`
      ไม่ได้อ่านแค่ช่วง) แล้วค่อยกรองด้วย JS ทีหลัง
      → **cache ผูกช่วงจึงไม่ได้ประหยัดการอ่านเลย แค่ทำให้ cache ใช้ไม่ได้**

      **ทางแก้**: cache รายการทั้งชีตครั้งเดียวด้วยคีย์ที่ไม่ผูกช่วง แล้วกรองในหน่วยความจำ
      **ไม่เปลี่ยนพฤติกรรม** เพราะการกรองเดิมก็ทำใน JS อยู่แล้ว
      **ได้ประโยชน์ 8 จุดที่เรียกฟังก์ชันนี้** ไม่ใช่แค่หน้าเช็คชื่อ —
      `CalendarService` 3 จุด · `AttendanceService:1544` · `ReportService:633` · `SeedTestData` 3 จุด

      **ตรวจครบก่อนเปลี่ยนแล้ว ไม่ได้เดา**
      - **ไม่มีใครพึ่ง `student_key` จริง** — `getRecordStudentKey_` [StudentService.js:1035]
        ปั้นคีย์จาก `student_id` / `student_number` ซึ่งถังมีครบ ส่วน 2 จุดที่อ่าน
        `record.student_key` ใช้เป็น fallback ในนิพจน์ที่ปั้นคีย์เดียวกันได้เองอยู่แล้ว
      - `student_number` เป็น `NaN` ได้ (ไม่มี `|| 0`) แต่ปลายทางทุกตัวห่อ
        `parseInt(...) || 0` หรือเทียบ `> 0` → ผลลัพธ์เท่าเดิม
      - **ลำดับแถวตรงกัน** ทั้งสองทางใช้ `getAttendanceReadSheets_` (archive ก่อน ชีตหลักทีหลัง)
        และไล่แถวจากน้อยไปมาก → กติกา "แถวหลังชนะ" ไม่เปลี่ยน
      - `invalidateSchoolCalendarCaches_` เรียก `bumpDerivedDataCacheVersion_`
        จึงล้างคีย์ใหม่ `school_calendar_entries_all` ด้วย **แก้ปฏิทินแล้วเห็นผลทันทีเหมือนเดิม**

      **ทดสอบยังไง**
      - `clasp push` → `runP0Diagnostics` → เทียบ `attendance_pass1_steps`
        ต้องเห็น `pf_calendar_prime_ms` และ `raw_records_ms` **ลดลงชัดเจน** ส่วน
        `attendance:daily_reads_archive` ต้องยังเขียว = **ถังกับการอ่านตรงให้ผลตรงกัน**
      - บนของจริง: เปิดหน้าเช็คชื่อ **แล้วกด ◀ ไล่ 3-4 วัน** วันที่ 2 เป็นต้นไปควรเร็วขึ้นชัด
      - **แก้ปฏิทิน 1 รายการ แล้วดูว่าตัวเลขวันเรียนเปลี่ยนทันที** (ยืนยันว่า cache ใหม่ถูกล้าง)
      - หน้ารายงาน / ปพ.6 ตัวเลขต้องเท่าเดิมทุกตัว (`2077,114,119,23,19,,2352,94.8`)
      ✅ **ตรวจแล้วว่า `row_index` ที่มาจาก cache ไม่ใช่บั๊ก** — `getOrBuildCachedJson_`
      ทำคีย์ผ่าน `buildDerivedCacheKey_` และ `markStatus` เรียก `invalidateAttendanceCaches_(date)`
      หลังเขียนทุกครั้ง จึงไม่มีทางได้ `row_index` ค้างจากก่อนการลบแถว
      **ส่วนที่เหลืออีก 2 ก้อน** `preflight_validate_ms=845` และ `students_ms=556`
      รวมกัน 1.4 วินาที มากกว่าการสร้างรายงานทั้งภาคเรียน — ยังไม่ได้สืบ

- [x] **★★★ เช็คชื่อ 1 ครั้ง ทิ้ง cache ที่เพิ่งใช้เวลาสร้าง 3–18 วินาที** — **แก้แล้ว รอทดสอบ**
      แยก `getAllAttendanceRecords_` เป็น 2 cache: ส่วน archive ใช้ `attendance_archive_version` (6 ชม.)
      ส่วนชีตหลักใช้ `derived_cache_version` (180 วิ) ตามเดิม แล้ว `archive.concat(main)` ลำดับเดิมเป๊ะ
      ไม่แตะ `getUniqueLatestRecords_` · ภาคเรียนที่ยังไม่เก็บถาวรพฤติกรรมเท่าเดิมทุกประการ
      **วัดแล้ว 18 ส.ค. 2569 19:40** — `perf:archive_cache_survives_mark` เขียว
      `direct_scan_ms` 3,275 → `after_simulated_mark_ms` **273** · `records_identical: true` (2,586 แถว)
      `_timing_log` ยืนยันอีกทาง: มี `scan=archive` ครั้งเดียวแล้วไม่มีอีก
      รายละเอียดเต็มใน `DECISIONS.md` ข้อ 37

- [x] **รอบ 1 เลิกอ่านของเดิมซ้ำ** — **แก้แล้ว รอทดสอบ** (`DECISIONS.md` ข้อ 36)
      memo ชีตตั้งค่า + `getTeacherSessionUser_` / `getPinStateData_` เลิกอ่านสด ·
      `getStudentList` ใช้ตัวที่ cache · memo `getDerivedDataCacheVersion_` · memo `getCurrentAttendanceSourceInfo_`
      **วัดแล้ว** — `pf_settings_ms` pass2/pass3 เหลือ **0** (เดิม 17/17) · `records_day_status_ms` **0** (เดิม 18/14)
      ⚠️ pass1 1264 → 1492 ms เป็นการแกว่งของ Apps Script เอง ไม่ใช่การถดถอย (ให้ดูสัดส่วน ไม่ใช่เลขดิบ)

<details><summary>บันทึกผลวัดที่นำไปสู่การแก้ 2 ข้อบน</summary>

**★★★ เช็คชื่อ 1 ครั้ง ทิ้ง cache ที่เพิ่งใช้เวลาสร้าง 3–18 วินาที** (วัดแล้ว 18 ส.ค. 2569)
      **ข้อมูลจริงจาก `_timing_log` 594 แถว** (อ่านผ่าน `perf:timing_log_summary`)
      | metric | ครั้งที่ช้า | ค่ากลาง | สูงสุด |
      |---|---:|---:|---:|
      | `attendance_load_ms` | 123 | **6,246 ms** | 33,975 |
      | `dashboard_build_ms` | 81 | **4,226 ms** | 17,645 |
      | `attendance_scan_ms` | 65 | **2,838 ms** | 18,620 |
      | `report_build_ms` | 47 | 4,165 ms | 18,646 |
      | `analytics_build_ms` | 28 | 5,652 ms | 11,457 |
      | `derived_cache_warm_ms` | 6 | **7,510 ms** | 12,243 |

      **ต้นเหตุ — ไล่ครบวงจรแล้ว**
      `markStatus` [AttendanceService.js:313] → `invalidateAttendanceCaches_` [:985]
      → `bumpDerivedDataCacheVersion_()` → **คีย์ derived cache ทุกตัวเปลี่ยน**
      รวม `attendance_date_buckets` [:819] → ครั้งถัดไปต้อง `readAllAttendanceRecords_` ใหม่
      = **สแกน 2,586 แถวอีกรอบ (2.8–18.6 วินาที)**

      **ชีตหลัก `เช็คชื่อ` มี 0 แถว — ทั้ง 2,586 แถวคือชีต archive ซึ่งไม่เปลี่ยนเลยเมื่อครูเช็คชื่อ**
      แต่ถูกทิ้งทุกครั้งที่แตะสถานะ · เช้าหนึ่งเช็ค 40 คน = ทิ้ง cache 40 รอบ
      ครูไม่รู้สึกตอนแตะเพราะ optimistic UI **แต่พอเปิดหน้าแรกหรือรายงานจะจ่ายค่าสแกนใหม่เต็มๆ**

      **ทางแก้ที่เลือก**: แยก cache ของชีต archive ออกจากชีตหลัก แล้ว `archive.concat(main)` ลำดับเดิม
      (ถามก่อนลงมือแล้วเพราะใกล้จุดห้ามแตะใน `CLAUDE.md` — ผลคือไม่ได้แตะ `getUniqueLatestRecords_` เลย)

</details>

- [x] **`confirmDay` อุ่น cache 7.5–12 วินาทีในล็อก ขณะครูรออยู่** — **ตัดออกแล้ว รอทดสอบ**
      วัดซ้ำหลังแยก cache archive: `cold_warm_ms` **5,896** · `hot_warm_ms` **128**
      → การแยก cache **ไม่ได้แก้ข้อนี้ให้** เพราะอุ่นหลัง invalidate จึงสร้างใหม่จากศูนย์เสมอ
      ตัดสินด้วยข้อเท็จจริงฝั่ง client: `confirmDay` สำเร็จแล้ว**ไม่ยิงคำขอใดๆ ต่อ** แค่วาดจอเอง
      การอุ่นจึงเป็นการเดา แต่ครูจ่ายแน่นอน 100% ทุกครั้ง · `DECISIONS.md` ข้อ 38
      ลบผู้เรียกที่ตายแล้วอีก 5 จุด (`record`/`undo`/`bulk`/`undo_bulk`/`draft`) ซึ่งถูกกันออกที่ต้นฟังก์ชันอยู่แล้ว

- [x] **ปุ่ม ◀ ▶ ไม่มี feedback เลย และคำตอบที่มาช้าสุดชนะ** — **✅ ทดสอบบน deployment จริงผ่านแล้ว 20 ส.ค. 2569**
      กด ◀ 3 ครั้งจาก 18 ส.ค. → ลงที่ 15 ส.ค. · ปุ่มไม่จางไม่ถูก disable · สถานะโหลดครบ 3 สัญญาณ
      3 เรื่องในจุดเดียว: `setLoading` คือ `state.loading = show;` เฉยๆ ไม่วาดอะไร ·
      `loadAttendance` ไม่มี load token แบบที่ `loadPage` มี ·
      `shiftDate` คำนวณจาก `state.currentDate` ที่อัปเดตเมื่อสำเร็จเท่านั้น = กด ◀ รัวๆ ถอยได้แค่วันเดียว
      แก้ด้วย token + `pendingAttendanceDate` + `setAttendanceDateLoading_` · `DECISIONS.md` ข้อ 39
      **ยังไม่ผ่านมือผู้ทดสอบ** — เพิ่มขั้นตอนทดสอบไว้ใน `UAT.md` ส่วน ง. แล้ว

- [x] **แตะสถานะ 1 คน = วาด HTML ของนักเรียนทุกคนใหม่** — **✅ ทดสอบผ่านแล้ว 20 ส.ค. 2569**
      ติ๊ก "ยังไม่เช็ค" แล้วแตะสถานะ → แถวหายทันที นับเป็น "แสดง 38/39 คน" · ยกเลิกแล้วคืนสภาพถูกต้อง
      `refreshAttendanceStudentRow_` แทนแถวเดียวด้วย `replaceChild` ใช้ `id="srow-<เลขที่>"` ที่มีอยู่แล้ว
      **คืน false เมื่อทางลัดใช้ไม่ได้ ให้วาดทั้งลิสต์แทน** — โหมดรูป · แถวถูกกรองออก ·
      แถวต้องหายหลังเปลี่ยน · จำนวนแถวไม่ตรง
      `markStatus`/`undoStatus` 4 จุดใช้ทางลัด · bulk/ยืนยัน 5 จุดวาดทั้งลิสต์เหมือนเดิม
      `DECISIONS.md` ข้อ 40 · **ยังไม่ผ่านมือผู้ทดสอบ** เพิ่มขั้นตอนใน `UAT.md` ส่วน ง. แล้ว

- [x] **~~`JsReports` เขียน cache ไม่ติดเลย~~ — สมมติฐานผิด วัดแล้วมันติดมาตลอด**
      `cache:client_modules_fit` บนของจริง: `JsReports` **107,972 ไบต์ `single_key_cached: true`**
      `broken_with_single_key: []` → **CacheService ไม่บังคับเพดาน 100 KB ตามที่เอกสารระบุ**
      ผมคำนวณขนาดในเครื่องแล้วสรุปโดยไม่วัดก่อน · ตัวเลขในเครื่องก็คลาด (115,010 เทียบ 107,972)
      เพราะ `include_` ผ่าน `HtmlService` ประมวลผลไม่เหมือนอ่านไฟล์ตรงๆ
      **คงการหั่นตามไบต์ไว้ในฐานะการกันไว้ล่วงหน้า ไม่ใช่การแก้บั๊ก** — ตัวเดิมหั่น 80,000
      **ตัวอักษร** ซึ่งไทยล้วนได้ 240 KB ห่างจาก 108 KB ที่พิสูจน์แล้วว่าผ่านอยู่มาก
      ⚠️ ข้อเสียที่ยอมรับ: หลาย chunk แล้ว chunk เดียวถูก evict = พลาดทั้งก้อน
      `DECISIONS.md` ข้อ 41 + บล็อกแก้ไขต่อท้าย

- [x] **🔴 อีเมลผู้ปกครองในชีตเป็นที่อยู่ Gmail จริง** — **แก้แล้วและยืนยันเขียว 20 ส.ค. 2569**
      รอบยืนยัน: `domains: { "example.invalid": 17 }` · `deliverable_addresses: []`
      ⚠️ ตัวเลข 17 ไม่ขัดกับการ์ดบนหน้าจอที่ขึ้น 16 — check อ่านทั้ง 40 แถวรวมคนในถังขยะ (เลขที่ 30)
      ส่วนการ์ดนับเฉพาะนักเรียนที่ใช้งานอยู่ 39 คน
      `privacy:test_data_emails_are_reserved` ตอบขอบเขตให้: **มีแค่ 1 รายการ คือเลขที่ 1**
      (จากทั้งหมด 16 คนที่มีอีเมล อีก 15 เป็น `@example.invalid` อยู่แล้ว)
      **โค้ด seed ไม่ผิด** — `buildSeedParentEmail_` คืน `parent<N>@example.invalid` ทุกคน
      และไม่มีที่อยู่จริงฝังในโค้ดที่ไหน → ถูกพิมพ์เข้าชีตทีหลัง
      เปลี่ยนเป็น `parent1@example.invalid` ผ่านฟอร์มแก้ไขนักเรียนแล้ว
      ★ **ถ้าที่อยู่เดิมตั้งใจใส่ไว้ทดสอบการส่งอีเมลจริง ให้ใส่กลับเองได้**
      แต่ต้องรู้ว่า `privacy:test_data_emails_are_reserved` จะแดงทุกรอบ ซึ่งเป็นพฤติกรรมที่ตั้งใจ

- [x] **★ เลขที่ 28 ชื่อแสดงเป็น `#ERROR!`** — **ไล่เจอต้นเหตุและแก้โค้ดแล้ว 20 ส.ค. 2569**
      `SeedTestData.js` กันสูตรด้วย `setNumberFormat('@')` ตัวเดียว ซึ่ง `SecurityService.js:561`
      บันทึกไว้เองตั้งแต่ต้นว่า **"กันไม่อยู่ ทดสอบแล้ว"** และ seed เรียก `sanitizeSheetText_` **0 ครั้ง**
      แก้ให้ 3 จุดที่เขียนข้อความอิสระผ่าน `sanitizeSheetRows_` (นักเรียน · ป้ายปฏิทิน · หมายเหตุ)
      เพิ่ม `sheet_text:stored_names_intact` อ่านชื่อที่เก็บอยู่จริงในชีต แดงถ้าเจอ `#ERROR!`
      `DECISIONS.md` ข้อ 48
      **แก้ข้อมูลในชีตแล้ว 20 ส.ค. 2569** — พิมพ์ชื่อที่ถูกต้องกลับเข้าฟอร์มแก้ไขนักเรียน
      ซึ่งเดินผ่าน `sanitizeSheetText_` ตามเส้นทางเขียนจริง · ชื่อกลับมาแสดงเป็น
      `=ด.ช.วีรภาพ สูตรทดสอบ` ถูกต้อง = **พิสูจน์ตัวกัน end-to-end ไปในตัว**
      รอบยืนยัน: `broken_names: []` และ `names_starting_with_formula_char: 1`
      ★ ตัวหลังสำคัญ — แปลว่า**เคสทดสอบยังอยู่ครบ** ไม่ได้หายไปเพราะแก้ชื่อเป็นอย่างอื่น
      ⚠️ ผลข้างเคียงที่ตรวจแล้วว่าไม่กระทบ: `csv:monthly` `content_length` 2179 → 2194
      เพราะชื่อยาวขึ้นจาก `#ERROR!` (7 ตัว) เป็นชื่อเต็ม (21 ตัว) · **บรรทัดค่ารวมเท่าเดิมทุกหลัก**
      และเอกสารทุกที่อ้างบรรทัดค่ารวม ไม่ได้อ้างขนาดไฟล์ จึงไม่มีอะไรกลายเป็นเท็จ

- [ ] **`createStudent` / `deleteStudent` / `restoreStudent` ยังไม่ได้ทดสอบ** — **งานที่ต้องทำเอง**
      `deleteStudent` [StudentService.js:655] เป็น soft delete (เซ็ต `is_active=false` +
      `inactive_at` แถวยังอยู่ในชีตตลอดไป) สร้างแล้วลบจึงเหลือแถวส่วนเกินถาวร
      กู้ผ่านเว็บแอปไม่ได้ **ต้องลบแถวในชีตเอง**
      ⚠️ จะทำให้ `csv:row_counts_agree` (ตอนนี้ `student_rows: 40`) ขยับถาวร ซึ่งเป็นฐานที่
      Diagnostics ใช้เทียบทุกรอบ
      → **ทำบนสำเนาสเปรดชีต** (ก๊อป Spreadsheet + deploy ใหม่) ไม่ใช่บนชุดที่ใช้ทดสอบอยู่
      ผมสร้างสำเนาให้ไม่ได้ ต้องเป็นคนสั่งงานทำ
      ★ ส่วน `updateStudent` ทดสอบไปแล้ว (ดูรายการที่ปิดไปด้านบน) เพราะย้อนกลับได้

- [x] **🔴 เทียบตัวเลขข้าม 3 หน้า (รายงาน ↔ โปรไฟล์ ↔ ผู้ปกครอง)** — **✅ ผ่าน 21 ส.ค. 2569**
      เป็นข้อที่ผมกังวลที่สุด เพราะคำนวณด้วยโค้ด 3 ชุดคนละไฟล์และไม่มีใครเทียบให้
      เลขที่ 11 เดือน ก.ค. 2569: มา 18 · สาย 0 · ขาด 3 · ลาป่วย 0 · ลากิจ 2 · 23 วัน · 85.7%
      **ตรงกันทุกช่องทั้งสามหน้า** · หน้าผู้ปกครองไม่มีชื่อครู/เด็กคนอื่น/รหัสหลุด

- [x] **🔴 ตรวจตัวเลขบน ปพ.6** — **✅ ผ่านทุกข้อที่ตรวจได้ 21 ส.ค. 2569**
      เลขที่ 28 ชื่อเต็มถูกต้อง · 29 วันเรียน 17 · 30 วันเรียน 55 และยังอยู่ในเอกสาร ·
      25 ชื่อยาวครบ · 39 ขึ้นแดง "ต่ำกว่า 80%" ที่ 75% · หัวเอกสาร 218/60/40 ถูก
      ⚠️ แถวรวมท้ายตารางเลื่อนไปไม่ถึงเพราะเครื่องมือ — ยังไม่ได้ตรวจด้วยตา
      แต่แถวรายคนตรงกับตารางในหน้ารายงานทุกแถวที่เทียบได้

- [x] ~~🟠 หน้า ปพ.6 โหลด 80–120 วินาที และจอขาวสนิท~~ — **ผมรายงานผิดเอง ตกไป 21 ส.ค. 2569**
      วัดใหม่แล้วไม่จริงทั้งสองส่วน:
      - **HTML มาถึงใน ~2 วินาที** (curl 3 รอบ: 2.42 / 1.99 / 1.71 วิ) และ**มี spinner
        "กำลังโหลดข้อมูล..." อยู่ในนั้นแล้ว** [PrintReport.html:343]
        `doGet` เส้นทาง print ใช้ `createHtmlOutputFromFile` เฉยๆ [Code.js] ไม่ประกอบข้อมูลก่อนส่ง
        จอจึงไม่มีทางขาวเพราะรอ server ประกอบหน้า
      - กดจากปุ่ม "พิมพ์ ปพ.6" จริง **เอกสารขึ้นครบภายใน ~8 วินาที**
      ทำไมรอบก่อนถึงช้า: รอบนั้น cache เย็น (เพิ่ง deploy + เพิ่งล้าง derived cache)
      **เป็นกับดักเดิมที่ `CLAUDE.md` เตือนไว้เองว่าอย่าสรุปก่อนตัดเรื่อง cache ออก
      และเป็นข้อเดียวกับที่แผนเขียนว่า "ดูสัดส่วน ไม่ใช่ตัวเลขดิบ Apps Script แกว่งเองได้เท่าตัว"**
      ผมไม่ได้ตัดสองข้อนี้ออกก่อนรายงาน + อ่านภาพหน้าจอที่ค้างเป็นของจริง
      → ยังไม่รู้ว่ารอบเย็นจริงๆ ใช้เวลาเท่าไหร่ **ถ้าจะวัดต้องวัดตอน cache เย็นเท่านั้น
      และต้องดู `print_build_ms` ใน `_timing_log` ไม่ใช่จับเวลาด้วยภาพหน้าจอ**

- [ ] 🟡 **หน้าตั้งค่ามีช่องว่างครึ่งจอ 2 จุด** (เจอ 21 ส.ค. 2569)
      `.settings-grid` เป็นกริด 2 คอลัมน์ [StylePhase3.html:1383]
      แต่ `settings-section-wide` (span 2) ถูกวางคั่นกลาง [JavaScript.html:3611]
      ผลคือ **ขั้นตอนที่ 2 (สร้างภาคเรียน)** และ **ขั้นตอนที่ 3 (เชื่อมรูป)**
      ตกไปอยู่คอลัมน์ซ้ายลำพัง เหลือคอลัมน์ขวาว่างเปล่าอย่างละแถว
      ```
      แถว 1  [ ขั้นตอน 1 เต็มความกว้าง        ]
      แถว 2  [ ขั้นตอน 2 ] [   ว่าง   ]  ←
      แถว 3  [ ปฏิทิน เต็มความกว้าง           ]
      แถว 4  [ ขั้นตอน 3 ] [   ว่าง   ]  ←
      ```
      ไม่พังอะไร แต่หน้าตั้งค่าคือหน้าแรกที่ครูเจอตอนติดตั้ง — **มันดูเหมือนของยังทำไม่เสร็จ**
      บนจอมือถือไม่มีปัญหา (breakpoint เปลี่ยนเป็น 1 คอลัมน์ [StylePhase3.html:1726])
      แก้ได้ด้วยการเติม `settings-section-wide` ให้ทั้งสองอัน หรือย้ายให้อยู่ติดกัน
      **ยังไม่แก้ — เป็นงานความสวยงาม ไม่ใช่บั๊ก รอคนสั่งงานตัดสิน**

- [x] ~~🟡 ตั้ง PIN แล้วยังไม่ได้ป้องกันอะไร ต้องเปิดสวิตช์ที่สองด้วย~~
      **ผมรายงานผิดเอง ตกไป 21 ส.ค. 2569** — ไล่โค้ดแล้ว **ไม่มีสวิตช์ที่สอง**
      `setPin` [PinService.js:49] ปิดท้ายด้วย `saveSetting_('pin_enabled', 'true')` เสมอ
      **ตั้ง PIN เสร็จ = ล็อกเปิดทันที** ไม่ต้องไปกดอะไรเพิ่ม
      มีทางเขียน `pin_enabled` แค่ 3 ทางเท่านั้น และตรวจครบทั้งสามแล้ว:
      `setPin` → `true` เสมอ · `setPinEnabled` [:121] → ตามที่ครูเลือก **และต้องกรอก PIN ให้ถูกก่อน**
      · `removePin` [:161] → `false` พร้อมล้าง hash ทิ้ง (`pin_set` จะเป็นเท็จตามไปด้วย)
      `SeedTestData` ไม่แตะ PIN เลยสักบรรทัด
      → สถานะ "ตั้ง PIN แล้วแต่ยังไม่ได้เปิดล็อก" ที่เห็นบนสำเนาทดสอบ **ไปถึงได้ทางเดียว
      คือมีคนกดปิดเองพร้อมกรอก PIN ยืนยัน** ซึ่งเป็นความตั้งใจ ไม่ใช่การลืม
      **บทเรียน: ผมอ่านสถานะบนจอแล้วเดาสาเหตุ แทนที่จะไล่ว่าสถานะนั้นเกิดจากอะไรได้บ้าง**

- [ ] 🟡 **หมายเหตุที่ครูพิมพ์ ถูกส่งเข้าอีเมลผู้ปกครองด้วย และไม่มีอะไรบอกครู**
      [EmailService.js:88-89] ใส่ `record.note` ลงในตารางของอีเมลตรงๆ
      ครูใช้ช่องหมายเหตุจดเรื่องที่คุยกันเองได้ (เช่น "โทรแล้วไม่รับ") แล้วมันไปถึงผู้ปกครองเต็มๆ
      อาจเป็นเจตนาเดิมของฟีเจอร์ แต่**บนจอไม่มีที่ไหนบอกว่าหมายเหตุจะถูกส่งออกไปด้วย**
      → ถ้าตั้งใจให้ส่ง ควรเขียนกำกับไว้ที่ช่องหมายเหตุ · ถ้าไม่ได้ตั้งใจ ควรตัดออกจากอีเมล
      **ยังไม่ตัดสิน — ต้องถามคนสั่งงานก่อนว่าอยากให้เป็นแบบไหน**

- [ ] **เก็บ round trip ต่อเนื่อง ไม่ใช่วัดครั้งเดียว**
      วัดครั้งเดียวได้ค่ากลาง 4,062 ms (n=7) แล้วถอดเครื่องมือออก — พอบอกลำดับความสำคัญ
      **แต่ไม่พอจะตั้งเกณฑ์** เพราะเครื่องเดียว เน็ตเดียว ช่วงเวลาเดียว
      ถ้าจะใช้ตัดสินใจต่อ ต้องส่งเข้า `_timing_log` เป็น metric ใหม่ (เช่น `client_rtt_ms`)
      ⚠️ ห้ามยิงคำขอเพิ่มเพื่อส่งค่า — จะเพิ่ม overhead ที่กำลังพยายามวัด
      ให้สะสมฝั่ง client แล้วแนบไปกับคำขอถัดไป หรือ flush ทีเดียวตอนออกจากหน้า

- [ ] **ทบทวนงานที่เหลือด้วยหน่วย "จำนวนคำขอ" ไม่ใช่ "เวลาฝั่ง server"**
      overhead ราว 2-3.5 วินาทีต่อคำขอ ไม่ขึ้นกับ payload (`DECISIONS.md` ข้อ 49)
      → ตัดคำขอทิ้ง 1 ครั้งคุ้มกว่าเร่ง server ให้เร็วขึ้นหลักร้อย ms หลายเท่า
      ยังไม่ได้ไล่ว่าเหลือจุดไหนที่ยิงคำขอเกินจำเป็นอีกบ้าง

- [ ] 🟢 **โค้ดตายใน `getInitialData` — สาขา `params.date` / `params.month` / `params.tab`**
      [Code.js:121-130](Code.js:121) มีสาขารับ params เหล่านี้ แต่ไล่แล้ว**ไม่มีทางถูกเรียก**
      `fetchPageInitialData_(page, {})` [JavaScript.html:1729] ส่ง object ว่างเสมอ ·
      `syncPageUrl_` [:329] เขียน URL แค่ `?page=<x>` ไม่เคยใส่ค่าอื่น ·
      `readRequestedPageFromUrl_` [:318] อ่านแค่ `page`
      → **ไม่ใช่บั๊กที่ครูเจอ** เพราะแอปไม่เคยสร้าง URL แบบนั้นให้ครูกด
      แต่เป็นโค้ดที่อ่านแล้วเข้าใจผิดว่ารองรับลิงก์ตรง · เลือกได้ 2 ทาง: ลบสาขาทิ้ง
      หรือต่อสายให้ใช้งานได้จริง (ส่ง params จาก URL เข้า `fetchPageInitialData_`)

- [ ] **ยังไม่รู้ว่าเพดานจริงของ CacheService อยู่ที่เท่าไร**
      รู้แค่ว่า **107,972 ไบต์ยังผ่าน** (วัด 19 ส.ค. 2569) ซึ่งเกินตัวเลข 100 KB ที่เอกสาร Google ระบุ
      ถ้าอยากรู้ให้เขียน check ไล่ขนาดขึ้นทีละขั้นจนเขียนไม่ติด แล้วบันทึกไว้
      มีผลต่อการตั้ง `LARGE_CACHE_CHUNK_MAX_BYTES` ให้พอดี ไม่ใช่เดา

- [ ] **`putCachedJsonByKey_` (ตัวเล็ก) ยังไม่เช็คขนาดและกลืน error**
      ถ้าเขียนไม่ติดจะเงียบสนิท ไม่มีทางรู้จากการอ่านโค้ด · `daily_grid` / `summary_table` ยังไม่เคยวัดขนาด
      ทางแก้คือให้มันเตือนเมื่อพลาด แทนที่จะกลืน

- [x] **22 จุดโหลดทั้งหน้าใหม่เพื่อแก้ค่าเดียว** — **✅ ทดสอบผ่านแล้ว 20 ส.ค. 2569**
      บันทึกในหน้าตั้งค่า → ไม่ล้างจอเลย scroll ยังอยู่ที่เดิม · บันทึกแล้วรีบเปลี่ยนหน้า → กลับมาไม่ค้างจาง
      แก้ที่ `loadPage` จุดเดียว ไม่แตะ 22 จุด · จำสถานะ panel รายหน้าแล้วแยกทางเดิน 3 ทาง
      `'content'` = โหลดซ้ำหน้าที่เปิดอยู่ → หรี่ของเดิมไว้ ·
      `'loading'` = `navigateTo` วาด shell แล้ว → ไม่วาดซ้ำ (เดิมวาด 2 รอบด้วย state เดียวกัน) ·
      อื่นๆ → วาด shell ตามปกติ
      ของแถม: `loadSettingsDetails_` เลิกวาดซ้ำด้วย state เดิม (หน้าตั้งค่า 3 รอบ → 2) ·
      `addCurrentDateToCalendar` ใช้ `loadAttendance` แทน `loadPage` (คำขอเดียวพอ)
      บั๊กที่เจอตอนทำ: เปลี่ยนหน้าระหว่าง soft reload แล้วคลาสหรี่ค้าง กดไม่ได้ตลอดไป
      → `clearAllPageSoftReloading_()` ใน `beginPageLoad_`
      `DECISIONS.md` ข้อ 42 · **ยังไม่ผ่านมือผู้ทดสอบ** เพิ่มขั้นตอนใน `UAT.md` ส่วน ง. แล้ว

- [x] **หน้าแรกยิง 2 คำขอที่อ่านชีตชุดเดียวกันแบบต่อแถว** — **แก้แล้ว รอทดสอบบนของจริง**
      `getDashboardBootstrapData_` กับ `getDashboardData` อ่าน settings/รายชื่อ/ปฏิทิน/บันทึกวันนี้ชุดเดียวกัน
      และตัวที่สองไม่ได้ใช้ผลของตัวแรกเลย → `prefetchDashboardFullData_` ยิงขนานตั้งแต่ต้น
      ⚠️ client เดาเดือนเอง ถ้าวันนี้อยู่นอกภาคเรียน server จะเลื่อนเดือน แล้วต้องยิงใหม่
      = แย่สุดเท่าเดิมบวก 1 คำขอ (โควตา 120/นาที) · `DECISIONS.md` ข้อ 43.1

- [x] **หน้าติดตาม: เลื่อนเดือน 3 ครั้ง = 12 คำขอ และข้อมูลหลายเดือนปนกันได้** — **✅ ทดสอบผ่านแล้ว 20 ส.ค. 2569**
      กด "ก่อนหน้า" 3 ครั้งจาก ส.ค. → ลงที่ พ.ค. 2569 ข้อมูลสอดคล้องทั้งหน้า ไม่มีเดือนปน
      หน้าเดิมจางแต่ยังอยู่ ปุ่มเลื่อนเดือนกดต่อได้
      `loadAll` ยิง 4 คำขอโดยไม่มีอะไรกันชุดเก่า callback ของชุดเก่าเขียนลง `analyticsData`
      ที่ชุดใหม่เพิ่งล้าง พร้อมลด `pending` ของชุดใหม่ → **วาดจอก่อนโหลดครบด้วยข้อมูลปนเดือน**
      แก้ด้วย `analyticsLoadId` (ความถูกต้อง) แล้วค่อย debounce 400ms (ประหยัดคำขอ)
      `DECISIONS.md` ข้อ 43.2 · **ยังไม่ผ่านมือผู้ทดสอบ** เพิ่มขั้นตอนใน `UAT.md` แล้ว

- [x] **CSS 106 KB ที่หน้าส่วนใหญ่ไม่ใช้** — **วัดแล้วไม่คุ้ม ปิดข้อนี้ ไม่แก้โค้ด**
      `perf:inline_css_cost` บนของจริง: `css_total_ms` **17 ms** (รอบสอง 14) · `shell_ms` 21
      ตัวเลข 106 KB ในแผนคือขนาด**ดิบ** สิ่งที่วิ่งบนสายคือ gzip **16.8 KB จาก ~107 KB** (15%)
      เกณฑ์ที่ตั้งไว้ก่อนเห็นตัวเลข: <300 ms ไม่ทำ · 300–1500 ms cache ฝั่ง server · >1500 ms ค่อยคิดเรื่อง lazy
      แลกกับ FOUC + ลำดับ `var(--...)` ที่พึ่ง network (ข้อ 27 เจอมาแล้ว) ไม่คุ้ม · `DECISIONS.md` ข้อ 44
      **เงื่อนไขกลับมาทบทวน**: `css_total_ms` เกิน 1,500 ms หรือ CSS โตเกิน 3 เท่าของวันนี้
      **ห้ามกลับมาทำเพราะ "ไฟล์ดูใหญ่" อย่างเดียว**

- [x] **`duration_ms` ของทุก check เป็น 0 มาตลอด** — **แก้แล้ว**
      `runPreReleaseSmokeCheck_` [Code.js] จับเวลาใน object literal ก่อนบรรทัด `detail: fn()`
      ซึ่งประเมินทีหลัง · ไม่มีข้อสรุปไหนผิดเพราะเรื่องนี้ (ทุก check ที่ใช้ตัดสินใจรายงานเวลาใน
      `detail` ของตัวเอง) แต่เลขที่เห็นตลอดวันเป็นของปลอม · `DECISIONS.md` ข้อ 45.1

- [x] **Diagnostics เกือบตายทั้งรอบเพราะตัวควบคุมที่แพงเกินไป** — **แก้แล้ว**
      รอบ 19 ส.ค. 14:54 ใช้ **344 วินาที จากเพดาน 360** เพราะ `readAllAttendanceRecords_`
      (สแกนตรง ใช้เป็น control) ใช้ **136,459 ms** ตอน Apps Script ช้าทั้งกระดาน
      ข้ามการสแกนตรงถ้าเดินมาถึงช้ากว่า 30 วินาที แล้วรายงาน `direct_scan_skipped`
      การเทียบก่อน/หลัง bump ยังทำได้เสมอ · `DECISIONS.md` ข้อ 45.2

- [x] **`clearHolidayAttendance` อุ่น cache ในล็อก** — **ตัดแล้ว รอทดสอบ**
      วัดได้ `cold_warm_ms` 4,528 / 5,343 / 5,569 ms ทั้งหมดอยู่ในล็อกขณะครูรอ
      ตอนแรกดูต่างจาก `confirmDay` เพราะ client ยิง `loadSummary`/`loadDailyGrid` ต่อทันที
      **แต่ไล่แล้วไม่มีสถานการณ์ไหนที่ช่วย** — อุ่น "เดือนของวันที่ล้าง" แต่ client ขอ
      "ช่วงที่เลือกในหน้ารายงาน" ซึ่งมักไม่ตรง · และถึงตรง `getSummaryTable` ก็ cache ให้เองอยู่แล้ว
      = ย้ายงานเดิมเข้าไปทำในล็อกเฉยๆ
      เหลือผู้เรียก 0 ตัว จึง**ลบ `warmLikelyDerivedCachesForDate_` ทิ้งทั้งฟังก์ชัน**
      พร้อม check `perf:in_lock_cache_warm_cost` · `DECISIONS.md` ข้อ 46
      **ไม่มี mutation ตัวไหนสร้าง derived cache ในล็อกอีกแล้ว**

<details><summary>ตัวเลขเดิมก่อนแก้</summary>

      `derived_cache_warm_ms` ค่ากลาง **7,510 ms** สูงสุด **12,243 ms**
      [AttendanceService.js:478-501] ในล็อก `withAttendanceMutationLock_` ทำตามลำดับ
      `saveDayStatus_` → `invalidateAttendanceCaches_` (ล้างทุก cache) →
      `warmLikelyDerivedCachesForDate_` (สร้าง dashboard + รายงาน + ตารางรายวันทั้งเดือนใหม่จากศูนย์)
      ตรงกับ "ยืนยันวัน ~16 วิ" ในตาราง UAT
      ทางเลือก: ย้ายออกนอกล็อก → อุ่นเฉพาะตัวที่เปิดต่อบ่อยสุด → ตัดทิ้ง

</details>

- [ ] **เหลืออีก ~1.6 วินาทีบนการเปิดหน้าเช็คชื่อครั้งแรกของ session** (ไม่เร่ง)
      `students_ms` 665 · `records_day_status_ms` 367 · `alerts_ms` 338 · `pf_settings_ms` 279
      ทุกตัวเป็นการอ่านชีตคนละใบ **ยังไม่ได้สืบว่าแต่ละตัวไปหมดเวลากับอะไร**
      ⚠️ **คุ้มน้อยลงมากแล้ว** เพราะการเปิดวันที่ 2 เป็นต้นไปลงมาเหลือ ~0.9 วินาที
      ซึ่งอยู่ใต้เกณฑ์ 8 วินาทีสบายๆ · ที่เหลือกระทบเฉพาะการเปิดครั้งแรกของแต่ละ session
      **ถ้าจะทำ ให้เริ่มด้วยการเพิ่ม instrument ย่อยแบบเดียวกับ `pf_*` ก่อน อย่าเดา**

### 💡 ~~สิ่งที่ครูอยากรู้แต่ระบบไม่บอก~~ — ตรวจแล้วมีอยู่แล้วทั้ง 2 ข้อ ผมเขียนผิดเอง

- [x] ~~**ขาดวันไหนของสัปดาห์บ่อยสุด**~~ — **มีอยู่แล้ว 3 ที่** ตอนเขียนข้อนี้ยังไม่ได้เปิดหน้าติดตาม
      หน้าแรก → ตาราง **"วิเคราะห์ตามวัน"** แยก จ.–ศ. พร้อม %ขาด และแถบสรุปด้านบน
      *"วันจันทร์ มีอัตราขาดสูงสุด (10%) · วันอังคาร มาตรงเวลามากสุด (8%)"*
      หน้าติดตาม → การ์ด **"วันขาดสูงสุด จ. 10%"** + รายการ **"ขาดวันจันทร์/ศุกร์"** (`mon_fri_pattern`)
- [x] ~~**ขาดติดกันกี่วัน**~~ — **มีอยู่แล้ว** หน้าติดตาม → **"ขาดติดต่อกันยาวที่สุด"**
      (`consecutive_streaks` [AnalyticsService.js:318]) วันนี้ไม่ขึ้นเพราะข้อมูลมีแค่ 5 วันเรียน
      ⚠️ ทั้งสองรายการติดเพดาน `slice(0, 10)` เหมือนข้ออื่นในหน้านี้ ดูข้อ 🔴 ด้านบน

---

## งานค้างอื่น

- [x] ~~**รอบแก้ `UAT.md` — เอกสารตามรอบ 1-4 ไม่ทัน**~~ **ทำแล้ว 17 ส.ค. 2569**
      ไล่ตาม 6 ประเภทในกติกา `CLAUDE.md` แก้ไป 6 จุด (286 → 303 บรรทัด)
      1. **ประเภท 2 — cache** ตัด "CSS บางไฟล์" ออก (ไม่จริงแล้วตั้งแต่รอบ 4) และ**เติม
         โปรไฟล์นักเรียน + มุมมองรูปภาพ** ที่ตกหล่นมาตลอด ทำเป็นตารางติด/ไม่ติดให้ดูจบในตาเดียว
      2. **ประเภท 6 — ทำไม่ได้** เพิ่ม "อ่าน DOM ไม่ได้" กับ "ตั้ง viewport แคบไม่ได้"
         ซึ่งเจอทุกรอบแต่ไม่เคยเขียนไว้ · และประกาศว่า**หน้าผู้ปกครองทดสอบได้แล้ว**
      3. **ประเภท 1 — ลิงก์ผู้ปกครอง** บอกว่าลิงก์ขึ้นเป็นกล่องบนจอแล้ว ไม่ต้องพึ่งคลิปบอร์ด
      4. **บทเรียนวิธีทำงาน** เพิ่มลงกับดักข้อ 3: แก้ฝั่ง server แล้ว**รัน Diagnostics ก่อน deploy**
         เมนูรันโค้ดล่าสุดเสมอ จึงแยก "โค้ดผิด" ออกจาก "deployment เก่า" ได้โดยไม่เสียรอบ
      5. **viewport** เตือนว่าถ้าตั้ง 390px ไม่ได้ ต้องเขียนกำกับ ไม่ใช่เงียบ
         เพราะเรื่องล้นจอจะถูกรายงานน้อยกว่าความจริง
      **เจอบั๊กในเอกสารเองด้วย 2 จุด** — หัวข้อเขียน "กับดัก 3 ข้อ" แต่มี 4 ข้อ
      และโฟลว์ ง. อ้าง "วิธีในกับดักข้อ 3" ซึ่งเป็นเรื่อง deployment version ไม่ใช่วิธีจับเวลา (ข้อ 4)
      → คนอ่านจะเปิดไปผิดหัวข้อ แก้ทั้งคู่แล้ว

- [x] ~~**★★ ไฟล์สำรองไม่มีชีต `ลิงก์ผู้ปกครอง`**~~ **แก้แล้ว ยังไม่ได้ทดสอบ**
      เพิ่ม `parent_links` เข้า snapshot (`exportParentLinkSnapshot_`) และกู้คืนด้วย
      `BACKUP_VERSION` 3 → **4** · เหตุผลและทางที่ทิ้งอยู่ที่ `DECISIONS.md` ข้อ 33

      **3 จุดที่ตั้งใจทำแบบนี้**
      1. **ล้างคอลัมน์ `token` ทิ้งก่อนใส่ไฟล์เสมอ** — ปกติว่างอยู่แล้ว แต่ไฟล์สำรองเป็นของ
         ที่ครูดาวน์โหลดไปเก็บและอาจส่งต่อ ถ้ามี token หลงเหลือมันจะเป็นกุญแจเปิดข้อมูล
         นักเรียนที่อยู่ในไฟล์เดียวกัน · กู้คืนไม่ต้องใช้คอลัมน์นี้เพราะตรวจด้วย hash
      2. **ไฟล์ v3 ต้องข้ามชีตนี้ ห้ามเขียนทับ** — `restoreBackupSheet_` เขียนทับด้วยข้อมูลว่าง
         ถ้า snapshot ไม่มีคีย์ → **กู้คืนไฟล์เก่าจะล้างลิงก์ที่ใช้อยู่ทิ้งหมด**
         ครอบด้วย `if (sheets.parent_links)` แล้ว
      3. **คำสัญญาบนหน้าจอเหลือชุดเดียว** — ฝั่ง client เคยเขียนประโยคของตัวเอง
         **ไม่ได้ใช้ `data.note` ของ server เลย** ต้องแก้ 2 ที่ทุกครั้งและตกหล่นมาแล้ว
         → เปลี่ยนให้แสดง `data.note` และข้อความต่างกันตามรุ่นไฟล์ได้

      **★★ ทดสอบผ่านแล้ว 18 ส.ค. 2569** — `backup:includes_parent_links` [Diagnostics.js]
      เรียก `buildBackupSnapshot_()` ตรงๆ **อ่านอย่างเดียว ไม่สร้างไฟล์บน Drive
      และไม่ต้องพึ่ง pop-up** (ซึ่งถูกบล็อกอยู่ ดูข้อด้านล่าง)
      | ตรวจ | ผล |
      |---|---|
      | `backup_version` | **4** |
      | มี `parent_links` ใน snapshot | ✓ |
      | จำนวนแถว | **10 แถว** = มีข้อมูลจริง ไม่ใช่ผ่านเพราะชีตว่าง |
      | หัวตาราง | ครบ 7 คอลัมน์ |
      | **`rows_with_plaintext_token`** | **0** = ไม่มี token อ่านได้หลุดเข้าไฟล์สักแถว |

      **★★ ตรวจจากไฟล์จริงในเครื่องแล้ว 18 ส.ค. 2569** (แกะ ZIP ที่ดาวน์โหลดมาดูตรงๆ)
      | ตรวจ | ผล |
      |---|---|
      | `README.txt` | *"รูปแบบไฟล์สำรอง: v4"* · *"จำนวนลิงก์ผู้ปกครอง: 10"* · มีบรรทัดอธิบายว่าเก็บเป็นรหัสย่อ |
      | `system_snapshot.json` → `parent_links` | **10 แถว** หัวตารางครบ 7 คอลัมน์ |
      | **คอลัมน์ `token`** | **ว่างทุกแถว** |
      | `token_hash` | มี 8 จาก 10 แถว · ความยาว **64 ตัวอักษรทุกค่า** = SHA-256 ตามที่ `hashToken_` สร้าง |
      | **หา token ที่ใช้งานได้ทั้ง ZIP** | **ไม่พบเลย** — สตริง hex ยาวเป๊ะ 48 (ความยาวของ token จริง) มี **0 ค่า** ในทุกไฟล์ |
      | ไฟล์ CSV ในชุด | **ไม่มีคอลัมน์ token เลย** ลิงก์ผู้ปกครองอยู่เฉพาะใน JSON |
      → **ไฟล์สำรองเปิดข้อมูลนักเรียนด้วยตัวเองไม่ได้** ตรงกับที่ `README.txt` เขียนไว้

      **ที่ยังไม่ได้ยืนยัน** — ฝั่ง**กู้คืน** (กล่องยืนยันขึ้น "ลิงก์ผู้ปกครอง: N รายการ" ไหม
      และลิงก์ยังเปิดได้ไหมหลังกู้คืน) ต้องรอทดสอบ restore ทั้งเส้น
      ✅ **ตอนนี้มีไฟล์สำรองอยู่ในเครื่องแล้ว** (`~/Downloads/backup-pack-2026-08-18-12-53-44.zip`)
      จึงพร้อมทดสอบ restore ได้ทันทีเมื่อปลดล็อก

      ~~เจอจากการอ่านโค้ดก่อนทดสอบ~~
      `buildBackupSnapshot_` [BackupService.js:246] สำรอง 7 ชีต + ชีต archive ทั้งหมด
      **แต่ไม่มี `PARENT_LINK_SHEET` เลย** (`grep PARENT_LINK BackupService.js` = 0 ครั้ง)
      **ทำไมถึงสำคัญ**: `restoreBackup` มีไว้เพื่อ**ส่งโค้ดเวอร์ชันใหม่ให้ครูที่ซื้อไปแล้ว**
      ซึ่งแปลว่าครูจะ copy Spreadsheet+Script ชุดใหม่ แล้วกู้คืนข้อมูลเข้าไป
      → สำเนาใหม่จะ**ไม่มีลิงก์ผู้ปกครองเลย** ลิงก์ทุกใบที่ครูแจกผู้ปกครองไปแล้วใช้ไม่ได้ทันที
      และครูต้องออกใหม่ทีละคนโดยไม่มีอะไรบอกว่าต้องทำ
      **หน้าจอยังสัญญาไม่ครบ** — กล่องกู้คืนบอกว่า "ไม่รวมรหัสครู, PIN และ session ปัจจุบัน"
      **ไม่ได้บอกว่าไม่รวมลิงก์ผู้ปกครอง**
      **น่าจะเป็นการตกหล่น ไม่ใช่การตัดสินใจ** เพราะ token ในชีตถูกเก็บเป็น hash อยู่แล้ว
      (คอลัมน์ token ถูกล้างทิ้งเสมอ ดู `getParentLinkRows_`) การสำรองไปด้วยจึงไม่ได้เปิดความเสี่ยงใหม่
      **ทางแก้ที่ควรพิจารณา**
      - เพิ่ม `parent_links` เข้า snapshot แล้วกู้คืนด้วย — ตรงจุดที่สุด
      - หรือถ้าตั้งใจไม่สำรอง ต้อง**เขียนบอกในกล่องกู้คืน** และเตือนหลังกู้คืนว่าต้องออกลิงก์ใหม่

- [x] ~~**ทดสอบ restore backup ทั้งเส้น**~~ **ทำแล้ว 18 ส.ค. 2569 — ผ่าน**
      P0 ข้อเดียวที่ไม่เคยทดสอบจริงมาตลอด และเป็นช่องทางเดียวที่ครูซึ่งซื้อไปแล้ว
      จะได้รับโค้ดเวอร์ชันใหม่ · ทดสอบแบบ**วนกลับที่เดิม** คือกู้คืนไฟล์ที่เพิ่งสำรองมา
      ทับที่เดิม ข้อมูลจึงต้องเหมือนเดิมทุกตัว ถ้าต่างแม้ตัวเดียวคือมีบั๊ก

      **กล่องยืนยันถูกต้องครบ** — นักเรียน 40 · เช็คชื่อ 0 · ภาคเรียน 2 · ปฏิทิน 218 ·
      **ลิงก์ผู้ปกครอง 10 รายการ** (บรรทัดใหม่ขึ้นแล้ว) และข้อความท้ายกล่องมาจาก server
      พร้อมคำว่า "ลิงก์ผู้ปกครอง" = ใช้ข้อความรุ่น v4 ไม่ใช่ประโยคเดิมที่ client เขียนเอง
      → **ยืนยันงานสองข้อของรอบก่อนไปพร้อมกัน**

      **ตัวเลขหลังกู้คืน ตรงฐานทุกตัว**
      | | ก่อน | หลัง |
      |---|---|---|
      | วันเรียนตามปฏิทิน · ยืนยันแล้ว | 218 · 60 | **218 · 60** |
      | มา · สาย · ขาด | 2077 · 114 · 119 | **2077 · 114 · 119** |
      | ลาป่วย · ลากิจ · อัตรามาเรียน | 23 · 19 · 94.8% | **23 · 19 · 94.8%** |
      | นักเรียน · ต้องติดตาม | 40 · 1 | **40 · 1** |
      | ยังไม่ยืนยัน | 158 วัน | **158 วัน** |
      ตารางรายคนยังครบ (เลขที่ 39 = 75% ต้องติดตาม · เลขที่ 40 = 85%)

      **Diagnostics หลังกู้คืน เขียวครบ 15 check**
      | ตรวจ | ผล |
      |---|---|
      | `_att_archive_1` | **`last_row: 2587`** = 2,586 แถวรอดจากการ `clearContents()` แล้วเขียนกลับทั้งใบ |
      | `_att_day_archive_1` | **66 แถว** ครบ |
      | **`parent_link_rows`** | **10** = ชีตลิงก์ผู้ปกครองรอดการกู้คืน |
      | `rows_with_plaintext_token` | **0** |
      | อ่านชีต archive | `records_via_date_read: 39` = `records_via_bucket: 39` = **ถังกับการอ่านตรงยังตรงกัน** |
      | CSV รวม · นักเรียน · ปฏิทิน | `2077,114,119,23,19,,2352,94.8` · 40 · 218 แถว |
      **ขั้นที่เสี่ยงสุดคือชีต archive** เพราะถูกล้างทั้งใบแล้วเขียน 2,586 แถวกลับ
      → ผ่านโดยไม่ต้องพึ่ง rollback เลย

      **ยังไม่ได้ยืนยัน 1 อย่าง** — ลิงก์ผู้ปกครองที่ออกไว้ยัง**เปิดได้จริง**ไหมหลังกู้คืน
      ตรวจโครงสร้างได้ว่าชีตยังมี 10 แถวและ `token_hash` ครบ แต่การเปิดลิงก์จริงต้องมี token
      ซึ่งแสดงครั้งเดียวตอนออกลิงก์ · ถ้าจะปิดข้อนี้ให้สนิท ลำดับต้องเป็น
      ออกลิงก์ใหม่ 1 ใบ → **เก็บ URL ไว้** → สำรองข้อมูล → กู้คืน → ลองเปิด URL เดิม
      **ความเสี่ยงต่ำ** เพราะการตรวจลิงก์ใช้ `token_hash` ซึ่งยืนยันแล้วว่ารอดครบ 8 ค่า
- [x] ~~**★★ กดสร้างไฟล์สำรองแล้วไฟล์ไม่ถึงเครื่อง — pop-up ถูกบล็อก**~~
      **แก้และทดสอบผ่านแล้ว 18 ส.ค. 2569** (เจอและปิดในวันเดียวกัน)
      กด "สร้างไฟล์สำรอง" 2 ครั้ง **ฝั่ง server สำเร็จทั้งคู่** (ป้าย "สำรองล่าสุด" ขยับ
      11:25 → 11:28) แต่**ไฟล์ไม่ลงเครื่องเลย**
      **หลักฐาน**: ประวัติดาวน์โหลดของ Chrome ล่าสุดคือ 6 ก.ค. 2569 ไม่มีรายการของวันนี้ ·
      ไม่มีไฟล์ `backup-pack*` ที่ไหนใน home · **ไม่มีแท็บใหม่เปิดขึ้นมา**
      → `openDownloadWindow` [JavaScript.html] เรียก `window.open(url, '_blank', 'noopener')`
      **หลังจบ round-trip ของ `serverCall` แล้ว จึงเสีย transient user activation**
      เป็นปัญหาชนิดเดียวกับบั๊ก `execCommand('copy')` ที่แก้ไปในรอบ 1
      **โค้ดจับกรณีนี้ไว้แล้ว** — `openDownloadWindow` คืน false แล้ว toast แดงว่า
      *"เบราว์เซอร์บล็อกไฟล์ดาวน์โหลด กรุณาอนุญาต pop-up แล้วลองใหม่"*
      (ผมถ่ายภาพไม่ทัน toast อายุ 4.5 วิ จึงยังไม่ได้เห็นกับตา)
      ⚠️ **ผลข้างเคียงที่ต้องรู้**: ไฟล์ถูกสร้างบน Drive ทุกครั้งที่กด แล้ว `cleanupBackupFile`
      ลบทิ้งหลัง 60 วินาที **ถ้าครูปิดแท็บก่อน ไฟล์จะค้างใน Drive** — ตรงกับข้อ
      "ลบไฟล์ CSV ค้างใน Drive 2 ไฟล์" ที่ค้างอยู่แล้ว
      **แก้แล้ว ยังไม่ได้ทดสอบ** — เลือกทาง (ข) เหตุผลและทางที่ทิ้งอยู่ที่ `DECISIONS.md` ข้อ 35
      แก้ที่ **`openDownloadWindow` ตัวเดียว** ให้เปิดกล่องที่มีลิงก์กดเองเมื่อถูกบล็อก
      การกดลิงก์คือ user gesture จริงจึงไม่ถูกบล็อก · **ได้ทั้ง 3 ที่ที่ใช้ร่วมกัน**
      (ไฟล์สำรอง · CSV หน้ารายงาน · แม่แบบหน้านำเข้า)
      แก้ตามอีก 2 จุด: **ยืดตัวจับเวลาลบไฟล์จาก 60 วินาที เป็น 5 นาที** เพราะครูต้อง
      อ่านกล่องแล้วกดเอง ของเดิมไฟล์หายก่อนกดทัน · และแก้ข้อความหน้าตั้งค่าจาก
      "ดาวน์โหลดลงเครื่องนี้ทันที" เป็น "ดาวน์โหลดลงเครื่องนี้" เพราะ "ทันที" ไม่จริงเสมอไปแล้ว

      **★★ ทดสอบผ่านแล้ว 18 ส.ค. 2569 — ครบวงจรตั้งแต่กดปุ่มจนได้ไฟล์ในเครื่อง**
      กด "สร้างไฟล์สำรอง" → ขึ้นกล่อง **"ดาวน์โหลดไฟล์"** พร้อมข้อความ
      *"เบราว์เซอร์บล็อกการเปิดหน้าต่างดาวน์โหลดอัตโนมัติ ไฟล์ถูกเตรียมไว้เรียบร้อยแล้ว"*
      → กดปุ่มในกล่อง → **ได้ไฟล์ `backup-pack-2026-08-18-12-53-44.zip` (37 KB) ลงเครื่องจริง**
      **ยืนยันด้วยว่า pop-up ถูกบล็อกจริง** เพราะทางสำรองถูกเรียกใช้ ไม่ใช่ทางปกติ

- [ ] **ทดสอบล็อกอินใหม่** ยืนยันว่าโควตาอุปกรณ์ 5 เครื่องไม่ได้ล็อกตัวเอง
      ✅ **ตรวจจากหน้าจอแล้ว 18 ส.ค. 2569 — "อุปกรณ์ที่อนุญาต: ใช้อยู่ 1 จาก 5 เครื่อง"**
      โควตาไม่ได้ล็อกตัวเอง เหลือแค่ยืนยันด้วยการล็อกอินจริงจากเครื่องที่สอง
- [ ] **ลบไฟล์ CSV ค้างใน Drive 2 ไฟล์** จากการทดสอบก่อนแก้บั๊ก pop-up

- [ ] **★ อีเมลสรุปประจำสัปดาห์เปิดใช้งานค้างอยู่บนสำเนาทดสอบ** — หน้าตั้งค่า > ส่วนขั้นสูง ขึ้น
      *"ระบบส่งสรุปรายสัปดาห์เปิดใช้งานอยู่แล้ว"* → มี time-driven trigger รันจริงทุกศุกร์ 16:00
      และจะส่งอีเมลสรุปที่มีข้อมูลจำลองไปหาเจ้าของสำเนาทุกสัปดาห์จนกว่าจะกด "ปิดการส่งอัตโนมัติ"
      **ปิดก่อนเลิกใช้สำเนาทดสอบ** และตรวจว่า `seedTestData` ไม่ได้เป็นคนเปิดให้เอง

- [ ] **ปฏิทินชุดที่ใช้ทดสอบอยู่ไม่มีวันหยุดเลย** (`school_days: 218`, `holidays: 0`)
      `seedTestData` ไม่เขียนทับปฏิทินที่มีอยู่แล้ว จึงไม่ได้สร้างวันหยุดให้
      → เคส "วันหยุดพิเศษของโรงเรียน" ต้องสร้างเองก่อนทดสอบ
      และมี ส. 15 ส.ค. 2569 เป็นวันเรียนค้างอยู่จากการทดสอบข้อ 1.1 รอบก่อน (ถือเป็นเคสวันกิจกรรมวันเสาร์)

### ความไม่สม่ำเสมอในโค้ดเบส — รู้อยู่แล้ว ยังไม่แก้

ไม่มีข้อไหนเร่ง แต่ควรรู้ก่อนแตะส่วนนั้น ปิดข้อไหนแล้วให้ลบออกจากที่นี่

- [x] ~~**CSS ถูกส่งลง client ซ้ำสองรอบทุกครั้งที่โหลดหน้า**~~ **ปิดในรอบที่ 4 ยังไม่ได้ทดสอบ**
      เลือกทาง "เหลือ inline อย่างเดียว" ตามที่เสนอไว้ · ลบ `getClientStyleContent`
      `extractClientStyleContent_` `CLIENT_STYLE_REGISTRY` `PAGE_STYLE_MAP` ทิ้งทั้งหมด
      **ผลพลอยได้ที่สำคัญ: CSS ไม่ติด cache 15 นาทีอีกต่อไป แก้แล้วเห็นผลทันที**
      และฟังก์ชัน public ลดไป 1 ตัว · เหตุผลเต็มอยู่ที่ `DECISIONS.md` ข้อ 27
      ✅ **ทดสอบผ่านแล้ว — ไล่เปิดครบทั้ง 7 จุดที่เคยพึ่ง lazy CSS ไม่มีอะไรพัง**
      รายงาน · หน้าแรก · ติดตาม · นำเข้า · ตั้งค่า · **โปรไฟล์นักเรียน** · **มุมมองรูปภาพ**
      (สองอันท้ายเสี่ยงสุดเพราะเคยเรียก `ensureClientStyleLoaded('phase3'/'phase4')` ตรงๆ
      ตอนนี้เป็นเปลือกที่ resolve ทันที)
      ~~เดิม~~ — `StyleReport` / `StylePhase3` / `StylePhase4`
      ถูก `include_` inline ตั้งแต่ `doGet` (`Index.html:125-127`) **และ** ถูกดึงซ้ำผ่าน `getClientStyleContent`
      เพราะ `isClientStyleLoaded_` เช็คว่ามี `<style id="client-style-X">` ซึ่งตัวจาก `include_` ไม่มี `id`
      → เสีย round-trip ต่อหน้า + มี CSS 2 ชุดซ้อนกัน + ตัวจาก cache (เก่าได้ถึง 15 นาที) ชนะ cascade
      **ควรเลือกทางเดียวแล้วลบอีกทางทิ้ง**
      - ตัดออกจาก `styleMap` [Code.js] + `PAGE_STYLE_MAP`/`CLIENT_STYLE_REGISTRY` → เหลือ inline อย่างเดียว
        ปลอดภัยกว่า และปิดปัญหา cache 15 นาทีฝั่ง CSS ไปเลย แลกกับหน้าแรกหนักขึ้น
      - ตัด `include_` ออก → เหลือ lazy อย่างเดียว **เสี่ยง FOUC เพราะ CSS มาช้ากว่า HTML**
        และต้องระวัง `StyleReport`/`Phase3`/`Phase4` ใช้ `var(--...)` แต่ไม่ได้นิยามเอง ต้องให้ `Stylesheet` มาก่อนเสมอ
- [x] ~~**เก็บกวาด: ลบ `pin` ออกจาก `styleMap`**~~ **หายไปเองในรอบที่ 4**
      ตัดเส้นทาง lazy CSS ทิ้งทั้งก้อน `styleMap` และ `getClientStyleContent` จึงถูกลบไปด้วย
      dead entry นี้หมดไปพร้อมกัน

- [x] ~~**`esc()` มีหลายชุด แยกเป็น 2 พฤติกรรม**~~ **ปิดในรอบที่ 4 ยังไม่ได้ทดสอบ**
      รวมเป็นพฤติกรรมเดียวทั้ง 9 ไฟล์: เช็ค `null`/`undefined` เท่านั้น และ escape `"` `'` ด้วย
      **ที่เป็นบั๊กจริงคือ `if (!str) return ''` ทำให้เลข `0` กลายเป็นสตริงว่าง**
      พบใน `JsDashboard` `JsImport` `JsPhotoGrid` `JsProfile` และ `String(s || '')`
      ใน `ParentView` `PrintReport` · เหตุผลที่ไม่ทำเป็นฟังก์ชันกลางอยู่ที่ `DECISIONS.md` ข้อ 28
      ✅ **ทดสอบผ่านแล้ว และพิสูจน์ได้ว่าเป็นบั๊กที่มองเห็นจริง ไม่ใช่แค่ป้องกันไว้**
      `profileStat(st.late, ...)` [JsProfile.html:252] **ส่งตัวเลขดิบเข้า `esc()` ตรงๆ ไม่ห่อ `String()`**
      → โค้ดเก่าจะให้ `esc(0) === ''` ทำให้**การ์ดสถิติในหน้าโปรไฟล์ว่างเปล่าแทนที่จะขึ้น "0"**
      บนของจริงตอนนี้ขึ้น สาย 0 · ขาด 0 · ลาป่วย 0 · ลากิจ 0 ครบทุกช่อง
      ⚠️ **อย่าใช้หน้าแรกยืนยันข้อนี้** — `JsDashboard` ห่อ `esc(String(r.absent))` ไว้แล้ว
      เลข 0 จึงขึ้นถูกอยู่แล้วทั้งก่อนและหลังแก้ ดูไม่ออกว่าต่างกัน
      ~~เดิม~~ — `JavaScript.html` / `JsProfile` / `ParentView` / `PrintReport`
      escape `"` และ `'` ด้วย ส่วน `JsReports` / `JsDashboard` / `JsAnalytics` / `JsImport` / `JsPhotoGrid` ไม่ escape
      ทั้งที่ทุกที่สร้าง HTML ด้วย string concat และยัดค่าลง attribute เป็นปกติ
      (ยังไม่เป็นช่องโหว่ที่ยิงได้จริง เพราะ `normalizeLimitedText_` กันไว้ที่ชั้น validation แล้ว)
      บางชุดใช้ `if (!str) return ''` ทำให้เลข `0` กลายเป็นสตริงว่าง
      **ถ้าจะรวมเป็นตัวเดียว ต้องใช้เวอร์ชันที่เข้มที่สุด (`JavaScript.html`)**
- [x] ~~**แยกประเภท error ด้วยการหาคำภาษาไทยในข้อความ**~~ **แก้ฝั่งปลายทางครบแล้ว ยังไม่ได้ทดสอบ**
      เหตุผลและทางที่ทิ้งอยู่ที่ `DECISIONS.md` ข้อ 29 · ตารางสภาพปัจจุบันอยู่ที่ `ARCHITECTURE.md`

      **★ เจอบั๊กที่ผู้ปกครองเห็นจริงระหว่างทาง** — `ParentView.html` ยิง `google.script.run`
      ตรงๆ ไม่ผ่าน `serverCall` (ซึ่งถอด code ให้ก่อนอยู่แล้ว) `err.message` จึงเป็นค่าดิบ
      และหน้านั้นเอาไปแสดงทั้งก้อนที่ 2 จุด → **ลิงก์หมดอายุแล้วผู้ปกครองเห็น
      `PARENT_AUTH_EXPIRED|ลิงก์สำหรับเปิดข้อมูลหมดอายุ กรุณาขอลิงก์ใหม่จากครู` บนจอ**
      เป็นหน้าเดียวที่เปิดสาธารณะแบบ anonymous จึงเป็นจุดที่แย่ที่สุดที่จะหลุด
      แถมหัวข้อขึ้นว่า "เกิดข้อผิดพลาด" ทั้งที่ควรเป็น "ลิงก์หมดอายุ" → คำแนะนำที่ควรทำต่อผิดไปด้วย

      **ที่แก้**
      1. **`loadPage`** — `moduleLoadError_()` ติดธง `__source = 'deps'` ที่**จุดโยนทุกจุด**
         ปลายทางอ่านธงเดียว · `'สไตล์'` ที่เคยค้นหา**ไม่มีใครโยนอีกแล้ว**ตั้งแต่ตัด lazy CSS รอบ 4
      2. **`getTeacherFacingErrorCopy_`** — ลบการค้น `'วันที่เลือกถูกตั้งเป็นวันหยุด'`
         ซึ่ง**ไม่มีใครโยน เป็นข้อความที่บล็อกนั้นสร้างขึ้นเอง** (ค้นเจอแต่ผลผลิตของตัวเอง)
      3. **`loadAttendance`** — parse ก่อนเทียบและก่อนแสดง (ตอนนี้เป็น no-op เพราะ `serverCall`
         ถอดให้แล้ว ใส่ไว้ให้ทนเมื่อฝั่ง server ใส่ code ในอนาคต)
      4. **`ParentView.html`** — `parseParentServerError_` + `describeParentAuthError_`
         อ่าน code ก่อน คืน `kind`/`title`/`should_clear_auth` · แสดงเฉพาะส่วนข้อความ
         · `errorPage(title, sub, kind)` รับชนิดตรงๆ เลิกเดาจาก title ที่ตัวเองเพิ่งสร้าง
      5. **`PrintReport.html`** — `handlePrintFailure` **อ่าน code มาแล้วแต่ตัดทิ้งก่อนส่งต่อ**
         ทำให้ `showError` ต้องเดาใหม่จากคำไทย · เพิ่ม `PRINT_ERROR_KIND_BY_CODE_`
         แล้วส่งชนิดเข้า `showError(err, kind)` ตรงๆ ทั้ง 3 จุดที่เรียก

      **ทดสอบยังไง** (ไม่ต้องเตรียมข้อมูลใหม่)
      - หน้าผู้ปกครอง: เปิดลิงก์ที่ถูกเพิกถอนแล้ว → ต้องขึ้น **"ลิงก์หมดอายุ"**
        และ**ไม่มีคำว่า `PARENT_AUTH_EXPIRED|` บนจอ**
      - เปิดลิงก์ที่ token มั่ว → **"ลิงก์ไม่ถูกต้อง"** พร้อมคำแนะนำคนละแบบกับข้อบน
      - หน้าครู: เปิดหน้ารายงาน/ติดตาม ต้องโหลดได้ปกติ (แตะเส้นทางโหลดโมดูล)
      - เช็คชื่อในวันที่เป็นวันหยุด → ยังขึ้นข้อความวันหยุดเหมือนเดิม

- [x] ~~**เติม `CODE|` ให้ข้อความ error ฝั่ง server 3 ก้อนที่เหลือ**~~ **ทำแล้ว ยังไม่ได้ทดสอบ**
      เติมครบ 6 จุดที่โยน → `DATE_IS_HOLIDAY` · `OUT_OF_SEMESTER` (4 จุด) · `SHEET_MISSING`
      แล้วเปลี่ยนฝั่ง client มาเทียบ code ล้วน **ไม่เหลือการค้นคำไทยเพื่อตัดสินใจในเส้นทาง server เลย**
      (`OUT_OF_SEMESTER` เคยมี `code === 'OUT_OF_SEMESTER'` เขียนรออยู่ใน client อยู่แล้ว
      แต่ไม่มีใครส่งมา — เติมแล้วใช้งานได้ทันที)
      ตาราง code ทั้งหมดอยู่ที่ `ARCHITECTURE.md`

      **★★ เจอตัวขวางที่ทำให้ข้อสรุปรอบก่อนของผมผิด — ต้องแก้ก่อนถึงจะเติม code ได้**
      รอบก่อนสรุปว่า "จุดแสดงผลถอด code ออกได้ครบแล้ว จึงเติม code ได้เลย" **ไม่จริง**
      เพราะนับแต่เส้นทางของ error ที่ถูก `throw` ยังมีเส้นทางที่สองที่มองข้ามไป:
      **ฝั่ง server จับ error เองแล้วก๊อป `e.message` ใส่ payload กลับมา 27 จุด**
      ในรูป `{ success: false, message: e.message }` ซึ่ง client เอา `data.message`
      ไป toast ตรงๆ **ไม่ได้ผ่าน `parseServerError_`** (ตัวนั้นทำงานกับ error ที่ throw เท่านั้น)
      → ถ้าเติม code ก่อนแก้จุดพวกนี้ **โค้ดหลุดขึ้นจอทาง payload ทันที 27 ทาง**
      **ทางแก้**: `splitErrorCode_` + `buildFailurePayload_` [Utils.js] แทนที่ทั้ง 27 จุด
      payload ได้ `code` แยกฟิลด์มาด้วย · ตัว parse ยอมรับเป็น code เฉพาะ `^[A-Z][A-Z0-9_]*$`
      **ไม่ใช่เจอ `|` แล้วตัดทันที** กันข้อความปกติที่มี `|` ตามธรรมชาติถูกตัดหัวหาย
      **บทเรียน**: เปลี่ยนรูปแบบข้อความที่ข้ามพรมแดน server → client ต้องไล่ให้ครบ
      **ทั้งเส้นทาง throw และเส้นทาง payload** นับแค่ทางเดียวทำให้มั่นใจผิดว่าปลอดภัย

      **★ ทดสอบบนของจริงแล้ว 17 ส.ค. 2569 — หน้าผู้ปกครองผ่าน และเจอของแถม 2 อย่าง**
      เปิดลิงก์ที่ token มั่ว → ขึ้นกล่อง error สะอาด **ไม่มี `|` และไม่มีตัวอักษรอังกฤษพิมพ์ใหญ่**
      พร้อมบรรทัด "สิ่งที่ควรทำต่อ" = บั๊กที่ผู้ปกครองเห็นโค้ดดิบปิดแล้วจริง

      **ของแถม 1 — หัวข้อพูดแน่นอนกว่าที่ระบบรู้จริง (แก้แล้ว)**
      `openParentSession` ตอบ `'ลิงก์ไม่ถูกต้องหรือหมดอายุ'` **ก้อนเดียวโดยเจตนา**
      ไม่บอกว่าเป็นกรณีไหน (ไม่ให้คนเดา token รู้ว่าเดาใกล้แล้วหรือยัง) แต่โค้ดผมเช็ค
      "หมดอายุ" ก่อน จึงขึ้นหัวข้อ **"ลิงก์หมดอายุ"** ทั้งที่ token มั่ว
      → เปลี่ยนพฤติกรรมที่ TODO เคยยืนยันไว้ว่าขึ้น "ลิงก์ไม่ถูกต้อง"
      **แก้เป็นชนิดที่สามชื่อ `expired_or_invalid`** หัวข้อ "ลิงก์นี้ใช้งานไม่ได้"
      และคำแนะนำครอบทั้งสองกรณี — **ห้ามเลือกข้างแทน server ที่ตั้งใจไม่บอก**

      **ของแถม 2 — sweep 27 จุดรอบก่อนยังไม่ครบ (แก้แล้ว)**
      จับเฉพาะรูป `message: e.message` จึงพลาดรูป `message: String(e && e.message ? ...)`
      เหลืออีก **2 จุดใน `ParentService.js`** ซึ่งเป็นหน้าสาธารณะ
      และจุดที่ 2 (`getParentViewDataBySession`) **ถอด code ของ `PARENT_AUTH_*` ทิ้งไปเลย**
      หน้าจอจึงต้องเดาจากคำไทย ส่วน code อื่น (เช่น `SHEET_MISSING`) หลุดขึ้นจอทั้งก้อน
      → ใช้ `buildFailurePayload_` ทั้งคู่ · payload มี `code` แล้ว
      `describeParentAuthError_` รับเป็น argument ที่สอง · `shouldClearParentAuth_` เลยไม่มีใครเรียก ลบทิ้ง
      + `SeedTestData.js` 3 จุดที่ `ui.alert` ก็ถอด code ให้เหมือนกัน

      **★ หน้าพิมพ์ผ่านด้วย และเป็นหลักฐานว่าโค้ดใหม่ทำงาน ไม่ใช่แค่ไม่พัง**
      เปิด `?page=print&print_token=<มั่ว>` → ขึ้น **"ลิงก์พิมพ์ไม่ถูกต้อง"** พร้อมคำแนะนำที่ตรงกรณี
      **โค้ดเก่าจะขึ้นหัวข้อกลางๆ ว่า "ยังเปิดหน้าพิมพ์ไม่ได้"** เพราะข้อความที่จุดนั้นส่งเข้ามาคือ
      `'กรุณาเปิดหน้าพิมพ์จากระบบครูอีกครั้ง'` ซึ่ง**ไม่มีทั้งคำว่า "หมดอายุ" และ "ไม่ถูกต้อง"**
      การค้นคำไทยจึงตกไปที่ค่า default = ปุ่มที่ทำงานแต่บอกผู้ใช้ไม่ตรงเรื่อง

      **ยังทดสอบไม่ได้ 3 ข้อ — ต้องล็อกอินครู ซึ่งผมพิมพ์รหัสไม่ได้**
      deploy ทำให้ session ใน CacheService ถูกล้าง หน้าครูจึงกลับไปที่หน้าเข้าสู่ระบบ
      ⚠️ **และมี 2 commit ที่ยังไม่ได้ deploy** (`afd90ea`) หัวข้อบนหน้าผู้ปกครองจะเปลี่ยนจาก
      "ลิงก์หมดอายุ" เป็น **"ลิงก์นี้ใช้งานไม่ได้"** หลัง deploy รอบหน้า ต้องดูซ้ำ

      **★ ทดสอบฝั่งครูแล้ว 17 ส.ค. 2569 — `OUT_OF_SEMESTER` ผ่านจากปลายทางถึงต้นทาง**
      ฟอร์มปฏิทิน → ใส่วันที่ **05/09/2026** (นอกภาคเรียนที่จบ 31 ส.ค.) → กดบันทึก
      → toast แดง **"วันที่ อยู่นอกภาคเรียนที่เปิดใช้งานอยู่ (2025-11-01 - 2026-08-31)"**
      **ไม่มี `OUT_OF_SEMESTER|` นำหน้า** = server เติม code · `parseServerError_` ถอด ·
      `toast` ได้ข้อความไทยล้วน ครบวงจร
      เลือกจุดนี้ทดสอบเพราะ `ensureDateInActiveSemester_` ถูกเรียก**ก่อนเขียนและก่อน lock**
      ใน `saveSchoolCalendarEntry` → ข้อมูลไม่เปลี่ยนแม้แต่แถวเดียว
      **และข้อนี้ครอบ "toast ตอนบันทึกล้มเหลว" ไปด้วย** เพราะเป็น toast ของการบันทึกที่ล้มจริง

      **`OUT_OF_SEMESTER` ทางหน้าเช็คชื่อกดไม่ถึง — ไม่ใช่บั๊ก**
      ช่องวันที่หน้าเช็คชื่อมี `max="todayStr()"` [JavaScript.html:1824] และวันนี้อยู่ในภาคเรียน
      → เลือกวันนอกภาคเรียนจากหน้านั้นไม่ได้เลย เป็นการกันไว้ตั้งแต่ฝั่ง client

      **⏳ `DATE_IS_HOLIDAY` ยังทดสอบไม่ได้ — ติดข้อจำกัดของเครื่องมือ ไม่ใช่ของระบบ**
      ต้องมีวันหยุดในปฏิทินก่อน (ชุดที่ใช้อยู่มี `holidays: 0`) และการสร้างต้องเปลี่ยนช่อง
      **`<select>` ประเภท เป็น "วันหยุด"** ซึ่ง**ผมขับ dropdown เนทีฟในหน้านี้ไม่ได้**
      (ข้อจำกัดเดิมที่ `UAT.md` บันทึกไว้ · ลอง click + ArrowDown แล้วค่าไม่เปลี่ยน)
      **วิธีที่เหลืออยู่**: ให้คุณเปลี่ยนช่องนั้นเป็น "วันหยุด" ให้ 1 ครั้ง
      เลือกวันอาทิตย์ **16 ส.ค. 2569** ไว้แล้วเพราะ**ยังไม่อยู่ในปฏิทิน** → จำนวนวันเรียน
      ยังเป็น 218 ไม่กระทบ ปพ.6 และลบทิ้งได้ง่ายหลังทดสอบ
      สิ่งที่ยังไม่ได้ยืนยันคือ**สาขาเดียว** — `err.__code === 'DATE_IS_HOLIDAY'` ใน `loadAttendance`
      (ส่วนการถอด code ยืนยันแล้วด้วย `OUT_OF_SEMESTER` เพราะเดินเส้นทาง `serverCall` เดียวกัน)

      **★★ `DATE_IS_HOLIDAY` ทดสอบแล้วและ**เจอ regression ที่ผมทำเอง** (แก้แล้ว ยังไม่ได้ทดสอบซ้ำ)
      ตั้ง 16 ส.ค. 2569 เป็นวันหยุด → เปิดหน้าเช็คชื่อวันนั้น
      **ผลที่ได้**: ข้อความ *"วันที่เช็คชื่อ ตรงกับวันหยุดในปฏิทินวันเรียน"* สะอาด **ไม่มีโค้ดดิบ** ✓
      และการ์ดถูกต้อง (ปุ่ม "เปิดปฏิทินวันเรียน" / "กลับมาวันนี้") = สาขา
      `err.__code === 'DATE_IS_HOLIDAY'` ใน `loadAttendance` ทำงาน ✓
      **แต่หัวข้อขึ้น "เปิดหน้าเช็กชื่อไม่สำเร็จ" ซึ่งเป็นข้อความกลางๆ**
      ที่ถูกคือ **"วันนี้เป็นวันหยุด จึงเช็กชื่อไม่ได้"** → **แย่กว่าโค้ดเดิมที่ค้นคำไทย**

      **ต้นเหตุ — บทเรียนต่อยอดจากเรื่องเส้นทาง payload**
      การเปลี่ยนจากค้นคำไทยมาเทียบ code ใช้ได้เฉพาะจุดที่**ยังเห็น code**
      แต่มีจุดกลางทางที่ **"ถอด code แล้วส่งต่อแค่ข้อความ"** อยู่หลายชั้น
      `getTeacherFacingErrorCopy_` รับ `att.blocked_message` ซึ่งถอด code ไปแล้ว
      จึงแยกประเภทไม่ออกและตกไปใช้ข้อความกลางๆ
      **ทางแก้**: ทำให้ code เดินทางไปกับ error ทุกชั้น
      - `serverCall` / `serverCallQuiet` → ติด `err.__code`
      - `fetchPageInitialData_` → `initialDataError.__code` (เดิม `new Error(message)` ทิ้ง code)
      - `getTeacherFacingErrorCopy_(message, context, explicitCode)` → รับ code ตรงๆ ได้
      - จุดเรียกในสาขา `is_holiday_blocked` ส่ง `'DATE_IS_HOLIDAY'` เข้าไปเลย เพราะรู้อยู่แล้ว
      **กฎที่ได้**: เลิกค้นคำไทยแล้วต้องไล่ให้ครบว่า code เดินไปถึงจุดตัดสินใจจริงไหม
      **ไม่ใช่แค่จุดที่รับ error ตัวแรก** — ข้อความเดินทางไกลกว่า code เสมอถ้าไม่ตั้งใจพาไป

      **⚠️ ค้างคืนสภาพ 1 อย่าง — ทดสอบเสร็จแล้ว คืนได้เลย**
      16 ส.ค. 2569 ยังเป็น **วันหยุด** อยู่ วันเรียนทางการจึงเป็น **217 ไม่ใช่ 218**
      (16 ส.ค. อยู่ในปฏิทินมาก่อนแล้วจากรอบ 4 ที่กด "นับเป็นวันเรียน" ตอนนั้นเป็นวันนี้
      — ผมเข้าใจผิดว่ายังไม่มี การบันทึกจึงเป็นการ**เปลี่ยนประเภท** ไม่ใช่เพิ่มใหม่)
      **คืนสภาพต้องเปลี่ยน `<select>` กลับเป็น "วันเรียน" ซึ่งผมทำไม่ได้**
      ตั้งค่า → ปฏิทินวันเรียน → ค้น `2026-08-16` → แก้ไข → ประเภท "วันเรียน" → บันทึก
      **ตัวเลขที่ต้องกลับมา: วันเรียน 218 · วันหยุด 0** (ตัวเลขนี้เป็นตัวหารบน ปพ.6)

      **★★ ทดสอบซ้ำหลัง deploy แล้ว — ผ่านทั้งคู่ ปิดข้อนี้ได้**
      | ทดสอบ | ผล |
      |---|---|
      | หน้าเช็คชื่อ 16 ส.ค. (วันหยุด) | หัวข้อ **"วันนี้เป็นวันหยุด จึงเช็กชื่อไม่ได้"** ← เดิมเป็นข้อความกลางๆ · คำแนะนำ *"กลับไปเลือกวันเรียนอื่น หรือเปิดปฏิทินวันเรียนเพื่อตรวจวันที่นี้อีกครั้ง"* |
      | หน้าผู้ปกครอง token มั่ว | หัวข้อ **"ลิงก์นี้ใช้งานไม่ได้"** ← เดิมอ้างว่า "ลิงก์หมดอายุ" ทั้งที่ระบบไม่รู้ · ข้อความรองคือ *"ลิงก์ไม่ถูกต้องหรือหมดอายุ"* ตามที่ server ตั้งใจตอบแบบกำกวม · คำแนะนำครอบทั้งสองกรณี |
      ทั้งสองหน้า **ไม่มี `|` และไม่มีตัวอักษรอังกฤษพิมพ์ใหญ่โผล่บนจอ**

      **ยังไม่ได้ยืนยันด้วยตา 1 อย่าง — เส้นทาง payload ที่มี code**
      `buildFailurePayload_` ทดสอบไปแล้วเฉพาะกรณีข้อความไม่มี code (ซึ่งเป็น no-op)
      กรณีมี code เช่น `SHEET_MISSING` หลุดเข้า `getParentViewDataBySession`
      ต้องทำให้ชีตหายก่อนจึงจะเกิด **ไม่คุ้มเสี่ยงกับข้อมูลจริง**
      ความเสี่ยงต่ำเพราะใช้ `splitErrorCode_` ตัวเดียวกับที่ยืนยันแล้วในเส้นทาง throw
- [x] ~~**error handling ของ RPC ถูกก๊อปหลายชุด**~~ **ยุบแล้ว ยังไม่ได้ทดสอบ**
      3 ตัวใน `JavaScript.html` ก๊อปตรรกะเดียวกันไว้คนละชุด **ท่อนแปลงข้อความ 4 สาขา
      (`SHEET_MISSING` / permission / Timeout / quota) เหมือนกันเป๊ะทั้ง 3 ที่**
      → เพิ่ม error แบบใหม่ต้องแก้ 3 ที่ **ลืมที่ใดที่หนึ่ง = ข้อความดิบหลุดไปหาครูเฉพาะบางเส้นทาง**
      ซึ่งหาสาเหตุยากเพราะอีกสองเส้นทางยังถูกอยู่

      **แยกเป็นตัวช่วยร่วม 4 ตัว** — `isTeacherSessionExpiredCode_` ·
      `handleTeacherSessionExpired_` (ล้าง session + พากลับหน้าเข้าสู่ระบบ) ·
      `humanizeServerError_` (ท่อนแปลงข้อความทั้งก้อน) · `invokeFailureHandler_`
      **หางที่ต่างกันจริงยังอยู่ที่เดิมของแต่ละตัว** — `serverCall` toast ·
      `serverCallQuiet` เงียบ · `fetchPageInitialData_` reject promise พร้อมธง `__handled`
      | ฟังก์ชัน | เดิม | ใหม่ |
      |---|---|---|
      | `serverCall` | 56 บรรทัด | **37** |
      | `serverCallQuiet` | 49 | **29** |
      | `fetchPageInitialData_` | 52 | **37** |

      **ของแถม: `parseServerError_` ไม่ได้ตรวจรูป code** เจอ `|` แล้วตัดหัวทันที
      ต่างจาก `splitErrorCode_` [Utils.js] ฝั่ง server ที่ตรวจ `^[A-Z][A-Z0-9_]*$`
      → ข้อความที่มี `|` ตามธรรมชาติจะถูกตัดหัวหาย **ใส่เกณฑ์เดียวกันแล้ว**

      **`handlePrintFailure` ไม่ยุบเข้ามา — เป็นการตัดสินใจ ไม่ใช่ตกหล่น**
      อยู่ใน `PrintReport.html` ซึ่ง `DECISIONS.md` ระบุว่า 3 หน้าไม่แชร์โค้ดกันโดยเจตนา
      ยุบเข้ามาต้องเจาะกำแพงนั้น แลกกับการลดโค้ดซ้ำไม่กี่บรรทัด **ไม่คุ้ม** (เหตุผลเต็มที่ข้อ 30)
      และตรวจแล้วว่า**ไม่มีช่องข้อความดิบหลุด** เพราะ `showError` ไม่เอา message ไปแสดงเลย

      **★ ทดสอบบนของจริงแล้ว 17 ส.ค. 2569 — ผ่าน**
      | ทดสอบ | ผล |
      |---|---|
      | โหลด 4 หน้า (แรก · รายงาน · ตั้งค่า · ติดตาม) | ปกติทุกหน้า = เส้นทางสำเร็จของทั้ง 3 ตัวไม่พัง · หน้าติดตามยังขึ้น เสี่ยง 0 · ดีขึ้น 17 · แย่ลง 9 (ยอดจริง ไม่ติดเพดาน) |
      | บันทึกปฏิทินด้วยวันที่ 15/09/2026 (นอกภาคเรียน) | toast แดง **"วันที่ อยู่นอกภาคเรียนที่เปิดใช้งานอยู่ (2025-11-01 - 2026-08-31)"** เหมือนก่อนยุบเป๊ะ = `humanizeServerError_` + `invokeFailureHandler_` ใน `serverCall` ทำงาน |
      | ปฏิทินคืนสภาพแล้ว | **วันเรียน 218 · วันหยุด 0** ยืนยันบนหน้าตั้งค่าและหน้ารายงาน |

      **ยังไม่ได้ยืนยันด้วยตา 3 สาขา — ทำให้เกิดจริงไม่ได้แบบไม่เสี่ยง**
      `SHEET_MISSING` (ต้องทำให้ชีตหาย) · permission / Timeout / quota (ข้อความของ Google เอง)
      · สาขา `AUTH_EXPIRED` ใน `handleTeacherSessionExpired_`
      ทั้งหมดเป็นตรรกะที่**ย้ายมาทั้งก้อนโดยไม่แก้เนื้อ** ความเสี่ยงจึงอยู่ที่การย้าย
      ไม่ใช่ที่ตรรกะ และการที่เส้นทางปกติกับเส้นทางล้มเหลวผ่านทั้งคู่ครอบตัวการย้ายไปแล้ว
- [x] ~~**metadata ปลอมใน registry**~~ **ลบแล้ว ทดสอบผ่าน 18 ส.ค. 2569**
      เปิดหน้าแรกกับหน้ารายงาน โมดูล lazy โหลดครบ = `PAGE_MODULE_MAP` + `global` ทำงานเหมือนเดิม
      ลบ `page` ออกจาก `CLIENT_MODULE_REGISTRY` ทั้ง 4 ตัว เหลือแค่ `global` ซึ่งเป็น
      ฟิลด์เดียวที่โค้ดอ่านจริง · หลักฐานว่ามันตายแล้วคือ `profile` กับ `photoGrid`
      ไม่เคยมี `page` แต่ทำงานได้ปกติมาตลอด
- [x] ~~**Chart.js โหลดจาก CDN ภายนอก**~~ **ปิดด้วยการใส่ SRI · ทดสอบผ่านแล้ว 18 ส.ค. 2569**
      เปิดหน้าแรก**หลัง cache `JsDashboard` หมดอายุแล้ว** (รอเกิน 15 นาที + รีโหลดหลายรอบ)
      → **กราฟขึ้นครบทั้ง 2 อัน** (แนวโน้มรายวัน + สัดส่วนสถานะ) = hash ถูกต้อง
      ถ้าผิด เบราว์เซอร์จะปฏิเสธไฟล์แล้วขึ้นการ์ด "เครือข่ายของโรงเรียนอาจบล็อกการโหลดกราฟ" แทน
      ตรวจแล้ว **ทางล้มเหลวเขียนไว้ดีอยู่แล้ว** — กราฟกลายเป็นการ์ดที่บอกตรงๆ ว่า
      *"เครือข่ายของโรงเรียนอาจบล็อกการโหลดกราฟ..."* ครูยังทำงานได้ครบ ไม่ใช่หน้าพัง
      **สิ่งที่ขาดจริงคือความปลอดภัย** ไฟล์นี้ถูกโหลดเข้าหน้าที่แสดงข้อมูลนักเรียน
      แต่ไม่มีอะไรกันถ้า CDN ถูกแทรกโค้ด → ใส่ `integrity` + `crossOrigin` แล้ว
      **hash ยืนยัน 2 ทาง**: คำนวณจากไฟล์ที่ดาวน์โหลดจริง (200,807 ไบต์) และตรงกับที่
      cdnjs ประกาศผ่าน API ของตัวเอง
      ⚠️ **`CHART_JS_URL` กับ `CHART_JS_SRI` ต้องเปลี่ยนคู่กันเสมอ** เปลี่ยน URL อย่างเดียว
      = เบราว์เซอร์ปฏิเสธไฟล์ กราฟหายทั้งที่เน็ตปกติ
      เหตุผลที่ไม่เอาไฟล์มาไว้ในโปรเจกต์อยู่ที่ `DECISIONS.md` ข้อ 34
- [x] ~~**`SCRIPT_URL` เป็น global ที่ `JsReports.html` ใช้ตรงๆ**~~ **แก้แล้ว ทดสอบผ่าน 18 ส.ค. 2569**
      กดปุ่ม "พิมพ์ ปพ.6" → เปิดแท็บใหม่ด้วย URL ที่ประกอบครบ
      (`?page=print&type=pp6&start=...&end=...&print_token=...`) และเอกสารขึ้นครบ
      **218 วันเรียน · 60 วันยืนยัน · 40 คน** ตรงฐานทุกตัว
      เพิ่ม `AppShared.getScriptUrl()` แล้วให้ `JsReports` เรียกผ่านตัวนั้น
      ไม่เหลือโมดูลลูกที่อ้าง global นี้ตรงๆ แล้ว (ตรวจด้วย grep ทั้ง `Js*.html`)
- [x] ~~**`expires_at` ส่งไป-กลับแต่หน้าครูไม่เคยเช็คเอง**~~ **แก้แล้ว ยังไม่ได้ทดสอบ**
      เพิ่ม `isStoredTeacherAuthExpired_` เรียกจาก `initializeTeacherAuth_`
      → รู้ว่าหมดอายุตั้งแต่ตอนอ่านกลับ **ไม่มีจอโหลดค้างก่อนเด้ง** และหน้าเข้าสู่ระบบ
      บอกเหตุผลด้วย *"เซสชันหมดอายุแล้ว กรุณาใส่รหัสครูเพื่อเข้าใช้งานอีกครั้ง"*
      ไม่ใช่โยนฟอร์มเปล่าให้ครูเดาว่าทำไมต้องล็อกอินอีก
      เหตุผลและ 2 ทางที่ทิ้งอยู่ที่ `DECISIONS.md` ข้อ 31 · กติกา 3 ข้อที่ `ARCHITECTURE.md`

      **★ 3 จุดที่ตั้งใจเขียนแบบนี้ ห้ามลดรูป**
      1. **ไม่มี `expires_at` = ยังไม่หมดอายุ** — session ที่เก็บไว้จากโค้ดเวอร์ชันก่อนไม่มีฟิลด์นี้
         ถ้าตีเป็นหมดอายุ **ครูทุกคนถูกเตะออกหนึ่งครั้งตอนอัปเดตโค้ด** โดยไม่มีเหตุผล
      2. **เผื่อ skew 60 วินาที** เพราะเทียบกับนาฬิกาเครื่องครู ไม่ใช่ของ server
      3. **ไม่กั้นทุก request** — ถ้านาฬิกาเครื่องเพี้ยนเป็นชั่วโมงจะใช้งานไม่ได้เลย
         แทนที่จะแค่ล็อกอินใหม่ครั้งเดียว · ตัวตัดสินจริงยังเป็น `AUTH_EXPIRED` จาก server

      **★★ ทดสอบผ่านแล้ว 17 ส.ค. 2569** — ปลอม `expires_at` เป็นอดีตแล้วรีโหลด
      หน้าเข้าสู่ระบบขึ้นพร้อมกล่องเตือนสีเหลือง
      **"เซสชันหมดอายุแล้ว กรุณาใส่รหัสครูเพื่อเข้าใช้งานอีกครั้ง"**
      **ข้อความนี้เองเป็นหลักฐานว่าเป็นโค้ดใหม่** เพราะของ server เขียนว่า
      *"เซสชันหมดอายุ กรุณาเข้าสู่ระบบใหม่"* คนละประโยค → แยกได้ว่าใครเป็นคนจับ
      **ไม่มีจอโหลดหน้าค้างก่อนเด้ง** · เหลือ round-trip เดียว (`getTeacherLoginOptions`)
      จากเดิมสองรอบ (ลองโหลดหน้าก่อน → ถูกปฏิเสธ → แล้วค่อยโหลดตัวเลือกล็อกอิน)
      **ไม่ใช่ "ขึ้นทันที"** เพราะหน้าเข้าสู่ระบบยังต้องถาม server ว่าตั้งรหัสครูไว้หรือยัง

      **⚠️ บทเรียนจากการทดสอบ 2 ข้อ (เขียนลง `UAT.md` แล้ว)**
      1. **รอ 2 ชม. ให้หมดอายุเองไม่ได้ผล** — ได้ `sessionStorage` ว่าง ไม่ใช่ session
         ที่หมดอายุแต่ยังอยู่ **คนละเคสกัน** และเคสที่ต้องทดสอบคือเคสหลัง
      2. **`sessionStorage` ของแอปอยู่คนละ origin กับหน้าที่ห่ออยู่** ต้องสลับ frame
         ใน Console เป็น `userCodeAppPanel` ก่อน ไม่งั้นได้ `undefined`
         และ **`location.reload()` ใน frame นั้นรีโหลดแค่ iframe → จอขาว**
         ต้องรีโหลดหน้านอก (ค่าที่ปลอมไว้ยังอยู่ เพราะ sessionStorage อยู่ตามอายุแท็บ)

      ~~ลองรอ 2 ชม. ให้หมดอายุเองแล้ว — ไม่ได้ทดสอบโค้ดนี้เลย~~
      หน้าเข้าสู่ระบบขึ้นที่ ~5 วินาที **แต่ไม่มีกล่องเตือนสีเหลือง** ซึ่งเป็นตัวชี้ว่า
      `sessionStorage` **ว่าง** ไม่ใช่มี session ที่หมดอายุ → เส้นทางที่แก้ไม่ถูกเรียกเลย
      (แยกออกได้เพราะถ้ามี session หมดอายุจริง **โค้ดเก่าก็ต้องขึ้นกล่องเตือน**
      จากข้อความ `AUTH_EXPIRED|เซสชันหมดอายุ กรุณาเข้าสู่ระบบใหม่` ที่ server ส่งมา —
      ไม่มีกล่องเลยจึงตัดทั้งสองเวอร์ชันออก ไม่ใช่เรื่อง deploy)
      **ต้นเหตุ**: `sessionStorage` ตายเมื่อปิดแท็บ + session เป็น sliding
      การนั่งรอจึงได้ storage ว่าง ไม่ใช่ session ที่หมดอายุแต่ยังอยู่ **คนละเคสกัน**
      → บันทึกเป็นกับดักข้อใหม่ใน `UAT.md` แล้ว พร้อมตารางแยก 3 อาการจากกล่องเตือน

      **ทดสอบยังไง — ต้องสร้างเคสตรงๆ ไม่ใช่รอ** เข้าระบบ แล้วรันใน DevTools Console
      ```
      var k='att_teacher_auth',a=JSON.parse(sessionStorage.getItem(k));a.expires_at=Date.now()-600000;sessionStorage.setItem(k,JSON.stringify(a));location.reload()
      ```
      → ต้องขึ้นหน้าเข้าสู่ระบบ**ทันที** พร้อมกล่องเตือน *"เซสชันหมดอายุแล้ว กรุณาใส่รหัสครู
      เพื่อเข้าใช้งานอีกครั้ง"* และ**ไม่มีจอโหลดค้าง** · แล้วเช็คด้วยว่าเปิดหน้าใหม่ตอน
      session ยังไม่หมดอายุยังเข้าได้เหมือนเดิม
      ⚠️ **ต้องยืนยันก่อนว่า `419734a` ถูก deploy แล้ว** ไม่ใช่แค่ push

### ก่อนส่งมอบให้ลูกค้าทุกครั้ง
- [ ] ลบ `Diagnostics.gs` + เมนู 🧪 ตรวจงานแก้ P0
- [ ] ลบ `SeedTestData.gs` + เมนู 🧪 [ทดสอบ] ทั้ง 4 รายการ
- [ ] **ประกาศ `oauthScopes` — เขียนแล้ว 21 ส.ค. 2569 แต่ยังไม่ได้ทดสอบการขอสิทธิ์จริง**
      ประกาศไว้ 6 ตัว: `spreadsheets` · `drive` · `script.container.ui` ·
      `script.send_mail` · `script.scriptapp` · `userinfo.email`
      ที่มาของแต่ละบรรทัดและ**ทางที่แคบกว่าซึ่งใช้ไม่ได้** อยู่ใน `DECISIONS.md` ข้อ 51
      (`spreadsheets.currentonly` ใช้ไม่ได้เพราะห้ามใช้ installable trigger ·
      `drive.file` ใช้ไม่ได้เพราะโฟลเดอร์รูปมาจากลิงก์ที่ครูวางเอง ไม่ผ่าน Picker)

      ⚠️ **ถ้าตกไปตัวหนึ่ง จะไม่พังตอน push แต่จะพังตอนรันเป็น "ต้องขออนุญาต"**
      **แผนทดสอบ ต้องเดินให้ครบก่อนส่งมอบ** (ทำบนสำเนาทดสอบเท่านั้น)
      1. `clasp push` แล้วเปิดชีต → เมนู 🎓 ต้องขึ้น (`script.container.ui`)
      2. กดเมนูติดตั้ง → ต้องมีหน้าจอขออนุญาตขึ้นมา **อ่านรายการที่ขอแล้วเทียบกับ 6 ตัวข้างบน**
         ★ ถ้าหน้าจอขอกว้างกว่าที่ประกาศ = ผมไล่ call site ตกไปตัวหนึ่ง ให้บอกผมว่าขออะไรบ้าง
      3. เปิดเว็บแอป เช็คชื่อ 1 คน แล้วดูรายงาน (`spreadsheets`)
      4. หน้าตั้งค่า > ขั้นสูง > **กด "ส่งอีเมลทดสอบตอนนี้"** (`script.send_mail`)
         — ข้อนี้อยู่ในรายการห้ามกดตอน UAT ปกติ **แต่รอบนี้ต้องกดเพื่อพิสูจน์ scope**
      5. **ปิดแล้วเปิดสรุปสัปดาห์ใหม่** (`script.scriptapp` — `deleteTrigger` + `newTrigger`)
      6. หน้ามุมมองรูป — รูปต้องขึ้น (`drive` ผ่าน `getFolderById`)
      7. หน้าตั้งค่า > **"สร้างไฟล์สำรอง"** (`drive` ผ่าน `createFile`)
      8. เข้าสู่ระบบครูใหม่ ต้องผ่าน (`userinfo.email` ผ่าน `getVerifiedTeacherPrincipalEmail_`)

      ⚠️ **ยังไม่รู้ว่าครูที่ติดตั้งไปแล้วจะถูกบังคับให้กดอนุญาตใหม่ไหม** ต้องดูตอนทดสอบ
      ถ้าใช่ ต้องเขียนบอกไว้ใน `README.md` ขั้นตอนอัปเกรดด้วย

---

## บันทึกการตัดสินใจ

| เรื่อง | เลือก | เหตุผล |
|---|---|---|
| วันนอกปฏิทิน | เตือน + เพิ่มเข้าปฏิทินได้ทันที | ไม่ขวางงานครู ไม่ทำข้อมูลสูญ ตรงกับกิจกรรมวันเสาร์ที่เกิดจริง |
| ผูกอุปกรณ์ครู | จำกัดโควตา 5 เครื่อง ปฏิเสธเครื่องที่ 6 | บังคับเครื่องเดียวขัดกับ `MAX_TRUSTED_TEACHER_DEVICES` ที่ตั้งไว้ 5 |
| `AttendanceSummaryService.js` | ลบทิ้ง | ไม่มีใครเรียก และเขียนตรรกะนับซ้ำกับของจริง เสี่ยงตัวเลขสองชุดไม่ตรงกัน |
| กันสูตรในชีต | ใส่ `'` นำหน้า ไม่ใช่ปฏิเสธค่า | พิสูจน์บนชีตจริงแล้วว่า `'` ถูกกลืนตอนเขียน ค่ากลับมาครบ ไม่ต้องไล่ปฏิเสธ input |
| เกณฑ์ "ต้องติดตาม" | มาเรียนต่ำกว่า 80% ตรึงไว้ ไม่ให้ปรับ | เป็นเกณฑ์เดียวกับ ปพ.6 ที่ครูต้องส่งอยู่แล้ว ครูไม่ต้องจำสองมาตรฐาน · ค่าที่ครูตั้งไว้เดิม (`attention_threshold_days`) ไม่ถูกแตะ แค่หดขอบเขตให้เหลือเฉพาะรายการ 30 วันบนหน้าแรก |
| ตัวเลขใหญ่หน้าแรก | นับเฉพาะ "ขาด" ไม่รวมลาป่วย/ลากิจ | ขาดคือสิ่งเดียวที่ครูต้องลงมือทำต่อวันนี้ ส่วนคนที่ลาครูรู้อยู่แล้ว จึงยกไปบรรทัดรอง |

## ทำเสร็จและทดสอบผ่านแล้ว

| รอบ | ข้อ | ยืนยันด้วย |
|---|---|---|
| A | 1.0 กันข้อความผู้ใช้ไม่ให้กลายเป็นสูตร | `sheet_text:formula_injection` — เขียนดิบพังจริง (`=1+1 → 2`, `=ด.ช.สมชาย → #ERROR!`) เขียนผ่าน sanitize กลับมาครบทั้ง 7 ค่า |
| A | 2.3 เวลาอ่านกลับเป็นภาษาอังกฤษ | `timestamp:round_trip` — `sheets_converted_to_date: true` และแปลงกลับได้ตรงเป๊ะ |
| B | 1.4 ปฏิทินเข้าถึงได้แค่ 10 จาก 216 | บนของจริง: ค้น `ส.ค.` ได้ 21 รายการ · ดูเพิ่มจน 21/21 ปุ่มหายเอง · วันที่เป็น `จ. 3 พ.ย. 2568` · พิมพ์แล้ว focus ไม่หลุด |
| B | 1.2 แยกทิศกันแก้ปฏิทินย้อนหลัง | `calendar:guard_direction` 6 เคสผ่าน + ทิศ "เพิ่มเป็นวันเรียน" กดผ่านบนวันที่ยืนยันแล้วจริง |
| B | 1.1 เตือนเมื่อเช็คชื่อนอกปฏิทิน | แถบเตือนขึ้นบน ส. 15 ส.ค. · กดปุ่มแล้วแถบหายเอง · ปฏิทิน 216→217 · ขาด 119→120 สาย 114→115 · CSV รวม 2352→2391 (+39 = จำนวนคนของวันเสาร์พอดี) |
| B | 2.5 วันที่ในปฏิทินเป็น ISO | แสดงเป็นภาษาไทยแล้ว และตัดบรรทัดรายละเอียดที่ซ้ำกับป้ายสถานะออก |
| C | 1.5 วันร่างกับวันไม่มีข้อมูลแยกไม่ออก | บนของจริง: 10-14 ส.ค. เป็นแถบเหลืองต่อเนื่องมี `·` แยกจากวันยืนยันชัดเจน · คำอธิบายสัญลักษณ์ขึ้น "เช็คแล้วแต่ยังไม่ยืนยัน 5 วัน" ตรงกับข้อมูลจริง |
| C | 2.7 รายงานรายวันโหลดเอง + ช่วงเริ่มต้น | ขึ้น "กำลังโหลดรายงานรายวัน" แทน "ยังไม่มีรายงานรายวัน" · ช่วงเริ่มต้นเป็น 1-15 ส.ค. 2569 · คำเตือนเหลือ 5 วันแทน 156 วัน |
| C | 1.3 (ส่วนหน้าจอ) ระบุวันที่ตกหล่น | การ์ดขึ้น "วันเรียนที่ยังไม่ได้ยืนยัน 5 วัน" พร้อมวันที่ไทยครบทั้ง 5 วัน แทนที่จะบอกแค่จำนวน |
| C | ปุ่ม "นับเป็นวันเรียน" บนการ์ด | สร้างเคสจริงแล้วกดทดสอบ: 216→217 · ขาด 119→120 สาย 114→115 · รวม 2076→2113 · การ์ดหายไปเอง · ตัวเลขกลับเท่าเดิมทุกตัว ไม่มีเรคอร์ดสูญหาย |
| B | กันลบวันเรียนของวันที่ยืนยันแล้ว | ทดสอบซ้ำแล้วยังบล็อกถูกต้อง และพอยกเลิกการยืนยันก่อน ลบผ่านทันที — กติกาขึ้นกับสถานะจริง ไม่ใช่บล็อกตายตัว |
| D | 2.1 ปุ่มสถานะที่ 5 ตกบรรทัด | บนมือถือ: 5 ปุ่มอยู่แถวเดียว ปุ่มล้างสถานะกินแถวของตัวเอง |
| D | 2.4 กดทับสถานะได้เลย | แตะ "สาย" ทับ "มา" ได้จังหวะเดียว · ประวัติได้ "แก้ไขสถานะ มาเรียน → สาย" บรรทัดเดียว เทียบกับของเก่าในจอเดียวกันที่เป็น "ยกเลิกสถานะ" + "บันทึกสถานะ" |
| D | กันประวัติเปล่าจากการแตะสถานะเดิม | เจอ "แก้ไขสถานะ สาย → สาย" ตอนทดสอบ แก้ให้ `markStatus` ออกทันทีถ้าสถานะตรงกับของเดิม |
| D | 2.8 ข้อความปฏิเสธการลบปฏิทิน | เพิ่มวิธีแก้ต่อท้ายแล้ว ให้รูปแบบเดียวกับฝั่งบันทึก |
| D | 2.2 ช่องวันที่ต้องกด Enter | พิมพ์ 13082026 โดยไม่กด Enter → ช่องเก็บครบ 13/08/2026 · ไม่มี error แดง · เปลี่ยนไป พฤ. 13 ส.ค. 2569 พร้อมข้อมูลถูก (34/2/3) |
| E | 1.3 คำเตือนบนเอกสาร ปพ.6 | เอกสารไม่มีกรอบแดงแล้ว มีบรรทัดขอบเขตสีปกติ "คำนวณจากวันเรียนที่ยืนยันแล้ว 61 วัน จากทั้งหมด 217 วัน" · หัวเอกสารกับคอลัมน์วันเรียนตรงกันที่ 61 ทุกแถว |
| E | บล็อกพิมพ์เมื่อมีวันหลุดปฏิทิน | สร้างเคสจริงแล้วกดพิมพ์ → ไม่มีแท็บพิมพ์เปิด ขึ้นข้อความบอกทางแก้ · กด "นับเป็นวันเรียน" คืนสภาพ 216→217 แล้วพิมพ์ได้ตามปกติ |
| E | 2.6 ช่องว่างคั่นคอลัมน์ที่ตรึงไว้ | แก้ได้รอบที่ 4 · ต้นเหตุจริงคือ `table-layout: auto` ทำให้ `width` บนเซลล์เป็นแค่ข้อเสนอ ล็อกความกว้างไม่ได้ · เปลี่ยนเป็น `fixed` แล้วคุมด้วยตัวแปรเดียว · ของแถม: ชื่อนักเรียนไม่ถูกตัดด้วย `...` อีกแล้ว (ปิดส่วนหนึ่งของ 3.4) |
| F | 3.2 "ต้องติดตาม" มี 3 นิยามไม่ตรงกัน | บนของจริง: ชิปลดจาก **37 เหลือ 1** และแถวที่ติดป้ายแดงเหลือคนเดียวตรงกัน (เลขที่ 39 ด.ญ.ศิรดา 75.4%) ตรงกับที่ไฮไลต์บน ปพ.6 · ใต้แถบกรองขึ้นบอกเกณฑ์ตรงๆ ว่าคือต่ำกว่า 80% เกณฑ์เดียวกับ ปพ.6 · หน้าแรกไม่มีคำว่า "ต้องติดตาม" ซ้ำอีก เปลี่ยนเป็น "เริ่มขาด/สายบ่อยช่วงนี้" (5 คน คนละชุดกับชิป ตามที่ตั้งใจ) |
| F | 3.1 ตัวเลขใหญ่หน้าแรก | ทดสอบครบ 3 สถานะบนวันเดียวกัน: ยังไม่เช็ค → วงแหวน 0% (0/39) · เช็คมาครบ → "39 มาครบทุกคน" · ตั้งขาด 1 สาย 1 → **"1 คนขาดวันนี้"** สีแดง พร้อมบรรทัดรอง "สาย 1" · ล้างข้อมูลทดสอบคืนสภาพ 0/39 แล้ว |
| F | 3.4 ถ้อยคำอ่านได้สองแบบ | บนของจริง: **"ใช้อยู่ 1 จาก 5 เครื่อง"** (เพดานส่งมาจาก server ไม่ได้ hard-code ฝั่ง client) · **"การล็อกหน้าจอ: ปิดอยู่ · ตั้ง PIN แล้วแต่ยังไม่ได้เปิดล็อก"** อ่านได้แบบเดียว |
| F | 3.3 ช่วงเวลาที่กำลังดู | หน้าติดตามขึ้น **"ช่วงที่ใช้จริง: 1 ส.ค. 2569 – 31 ส.ค. 2569"** และกดถอยเดือนแล้วเปลี่ยนตามเป็น 1–31 ก.ค. 2569 (payload ส่ง `from_th`/`to_th` มาอยู่แล้วแต่ไม่เคยถูกใช้) |
| G | ลบแถวจนหมดบนชีตที่ตรึงหัวตาราง | เจอจาก `clearTestData` พังจริง → พิสูจน์ว่ากระทบ `deleteSheetRowsByIndexes_` ด้วยเช็ค `delete_rows:all_data_rows_frozen_header` (แดงพร้อมข้อความ `Sorry, it is not possible to delete all non-frozen rows.`) → แก้แล้วเช็คเขียว `deleted: 3, last_row_after: 1` · **เก็บถาวรภาคเรียนของครูปีแรกจะไม่พังอีก** |
| G | cache ตรงกับชีตไหม | รัน Diagnostics ห่างกัน 18 นาที (เกิน TTL สูงสุด 300 วิ ทุกตัว) ได้ `2077,114,119,23,19,,2352,94.8` **เท่ากันทุกหลัก** — cache ไม่ค้างค่าเก่า |
| UAT4 | **roster bounds บนเอกสาร ปพ.6 จริง** | เลขที่ 29 ย้ายเข้ากลางเทอม → **วันเรียน 17 วัน** ไม่ใช่ 60 · 88.2% · ไม่ถูกนับขาดในวันก่อนเข้ามา · เลขที่ 30 ย้ายออกแล้ว → **วันเรียน 55 วัน** · 98.2% · ยังอยู่ในเอกสารช่วงที่เขาเรียนอยู่ แต่ไม่โผล่ในหน้าเช็คชื่อวันนี้ (39 คน ไม่ใช่ 40) |
| UAT4 | ตัวเลขไหลข้ามหน้า | เช็ค 39 คน → หน้าแรก "39 มาครบทุกคน" → รายงาน 2077+39 = **2116 มาเรียน** ตรงเป๊ะ · เพิ่มวันนี้เข้าปฏิทิน → 217→**218 วัน** และยืนยัน 60→**61** · ยกเลิกยืนยัน → กลับเป็น **60** ทันที |
| UAT4 | 2.6 คอลัมน์ตรึงบนเดสก์ท็อป | เลื่อนตารางไปทางขวาแล้ว "เลขที่"/"ชื่อ" ตรึงอยู่ **ไม่มีช่องว่างเหลื่อม** ชื่อไม่ถูกตัด · และพบว่า**มือถือไม่มีตารางนี้เลย** เปลี่ยนเป็นการ์ดรายคนแทน |
| UAT4 | 1.3 บรรทัดขอบเขตบน ปพ.6 | *"คำนวณจากวันเรียนที่ยืนยันข้อมูลแล้ว 60 วัน จากวันเรียนตามปฏิทินทั้งหมด 218 วัน"* **สีปกติ ไม่ใช่กรอบแดง** · หัวเอกสารครบ (โรงเรียน/ชั้น/ช่วงวัน/ผู้จัดพิมพ์/เวลา) |
| UAT4 | ชื่อยาว + วรรณยุกต์ซ้อนบนเอกสาร | เลขที่ 25 (60+ ตัวอักษร) ตัด 2 บรรทัดในช่อง ไม่ดันตารางเสีย · `ณัฐฐ์` และ `ปุ๊กกี้` แสดงถูกต้อง |
| UAT4 | 2.7 ช่วงเริ่มต้นของตารางรายวัน | เปิดแท็บรายวันได้ **1–16 ส.ค. 2569 = เดือนปัจจุบันหนีบด้วยวันนี้** ไม่ใช่ทั้งภาคเรียน |
| UAT4 | 3.2 เกณฑ์ "ต้องติดตาม" | ชิปขึ้น **1 คน** พร้อมคำอธิบายบนหน้าจอ *"คือมาเรียนต่ำกว่า 80% ซึ่งเป็นเกณฑ์เดียวกับที่ใช้บนเอกสาร ปพ.6"* |
| UAT4 | 3.1 ตัวเลขใหญ่หน้าแรก | ยังไม่เช็ค → วงแหวน 5% · เช็คครบไม่มีใครขาด → **"39 มาครบทุกคน"** |
| UAT4 | 1.1 เตือนวันนอกปฏิทิน | แถบเตือนขึ้นพร้อมบอกผลกระทบ (*"จะไม่ถูกนับในรายงานและบนเอกสาร ปพ.6"*) และปุ่มแก้ในที่เดียวกัน |
| UAT4 | กล่องยืนยันบอกว่าย้อนได้ | ยืนยันวัน → *"หลังยืนยัน ระบบจะล็อกวันนี้ไว้ก่อน และคุณยังยกเลิกการยืนยันเพื่อกลับมาแก้ไขได้อีกครั้ง"* · ยกเลิกยืนยัน → *"ครูจะกลับมาแก้ไขสถานะและยืนยันใหม่ได้อีกครั้ง"* |
| UAT4 | ประวัติแก้ไขบันทึกครบ | เปลี่ยนสถานะ → *แก้ไขสถานะ · ด.ช.กิตติพงศ์ ใจดี · มาเรียน → ขาด · เวลา* |
| UAT4 | **เก็บถาวรภาคเรียนทั้งเส้น** | archive 2,586 แถวสำเร็จใน ~20–30 วิ ไม่มี error · สลับกลับมาดูแล้ว**ตัวเลขตรงกันทุกตัว**: 218 วันปฏิทิน · 60 วันยืนยัน · 2,077 มาเรียน · 114 สาย · 119 ขาด · 94.8% · 40 คน · ต้องติดตาม 1 · ยังไม่ยืนยัน 158 วัน — ชีตหลักว่างแต่รายงานอ่านผ่านชีต archive ได้ครบ |
| UAT4 | ตัวกรอง "เฉพาะคนที่ยังไม่เช็ค" | พอเช็คเสร็จคนหนึ่ง คนถัดไปเลื่อนขึ้นมาที่พิกัดเดิม → **เช็คทั้งห้อง 39 คนได้โดยไม่ต้องเลื่อนจอเลย** รวม 40 แตะ |

## หมายเหตุการทำงาน

ทดสอบในเครื่องไม่ได้ ทุกรอบต้อง `clasp push` → สร้าง deployment version ใหม่ → ทดสอบบนของจริง
การมัดหลายข้อไว้ในรอบเดียวจึงคุ้มกว่าแก้ทีละข้อ

ข้อ 1.1 ต้องทดสอบด้วยการเช็คชื่อในวันที่ไม่อยู่ในปฏิทิน ซึ่ง seed ปัจจุบันไม่มีเคสนี้
(ปฏิทินชุดที่ใช้อยู่ไม่มีวันหยุดเลย) ต้องลบรายการปฏิทินบางวันออกก่อนทดสอบ
— และตอนนี้ลบผ่านหน้าจอได้แค่ 10 วันแรกเท่านั้นตามข้อ 1.4

### สิ่งที่ทดสอบแล้วผ่าน ไม่ต้องแตะ
- เอกสาร ปพ.6 หารวันเรียนตามช่วงที่เด็กอยู่ในห้องจริง (ย้ายเข้ากลางเทอม 17 วัน / ย้ายออก 55 วัน)
- รายงานรายวันทั้งภาคเรียน (40 คน × 217 วัน) โหลดใน **< 5 วินาที** (วัดตอน cache อุ่น)
- หน้าผู้ปกครองเมื่อลิงก์ไม่ถูกต้อง: ขึ้น "ลิงก์ไม่ถูกต้อง" พร้อมบอกวิธีแก้
  ไม่หลุดชื่อโรงเรียน ชื่อครู หรือข้อความ error ทางเทคนิค
- หน้านำเข้า CSV: มีแม่แบบ บอก 4 คอลัมน์ จำกัด 1 MB มี 2 โหมด และบังคับ "เริ่มอยู่ในห้องตั้งแต่"
- มือถือ: มีแถบเมนูล่าง 5 ปุ่ม ฟอร์มตั้งค่าเต็มความกว้าง ตั้งค่าขั้นสูงถูกซ่อนไว้หลังปุ่ม
- ตั้งค่าขั้นสูง > กู้คืน: บอกชัดว่าจะเขียนทับอะไรบ้าง และบอกว่า "ไม่รวมรหัสครู, PIN และ session"
  ⚠️ **ข้อความนี้เปลี่ยนแล้ว** ตั้งแต่เพิ่มลิงก์ผู้ปกครองเข้าไฟล์สำรอง — ตอนนี้กล่องยืนยัน
  แสดงจำนวนลิงก์ผู้ปกครองเพิ่มมา และใช้ข้อความจาก server (`data.note`) ซึ่งต่างกันตามรุ่นไฟล์
- **ตัวเลขดิบตรงกันเป๊ะทั้ง 3 มุมมอง** (16 ส.ค. 2569 · ส.ค. มีข้อมูลจริง 5 วันเรียน × 39 คน = 195 ครั้ง)
  | | มา | สาย | ขาด | ลา |
  |---|---|---|---|---|
  | หน้าแรก ตาราง "วิเคราะห์ตามวัน" (จ.–ศ. รวม) | 174 | 8 | 9 | 4 (ส่วนที่เหลือจาก 195) |
  | หน้าติดตาม แยกเพศ ชาย 20 คน | 92 | 1 | 5 | 2 |
  | หน้าติดตาม แยกเพศ หญิง 19 คน | 82 | 7 | 4 | 2 |
  | **ชาย+หญิง** | **174** ✓ | **8** ✓ | **9** ✓ | **4** ✓ |
  และ %มาเรียนคิดตามสูตร `basis_days` ถูกต้อง: ชาย (92+1)/(100−2) = **94.9%** ✓ ·
  หญิง (82+7)/(95−2) = **95.7%** ✓ (สาย นับเป็นมา · ลา ถูกหักออกจากตัวหาร)
  → **ชั้นที่คำนวณตัวเลขไม่มีปัญหา** ปัญหาอยู่ที่ชั้นที่เอาตัวเลขไป**ตีความ** (ดูข้อ 🔴 หน้าติดตาม)
- **ข้อความในหน้าตั้งค่าแยกนิยาม "ต้องติดตาม" ไว้ชัดเจนแล้ว** ใต้ช่อง `เกณฑ์รายชื่อ "ขาด/สายบ่อย" บนหน้าแรก`
  เขียนว่า *"นับขาดกับสายรวมกันใน 30 วันล่าสุด ถึงจำนวนนี้จึงขึ้นรายชื่อบนหน้าแรก · ไม่เกี่ยวกับชิป
  "ต้องติดตาม" ในหน้ารายงาน ซึ่งใช้เกณฑ์มาเรียนต่ำกว่า 80% ตาม ปพ.6"* — เป็นผลของรอบ 3/F
  ⚠️ แต่ข้อความนี้พูดถึงแค่ **2 นิยาม** ยังไม่รวมนิยามที่ 3 ในหน้าติดตาม (ดูข้อ 🔴 ด้านบน)
- **หน้าตั้งค่า ส่วนขั้นสูง อ่านแล้วรู้เรื่องทุกช่อง** — สถานะรหัสครู "ตั้งค่าไว้แล้ว" ·
  **อุปกรณ์ที่อนุญาต "ใช้อยู่ 1 จาก 5 เครื่อง"** (ยืนยันผลรอบ F อีกครั้ง) · อายุลิงก์ผู้ปกครอง 14 วัน ·
  PIN บอกสถานะสามชั้นแยกกันชัด **"ปิดอยู่ · ตั้ง PIN แล้วแต่ยังไม่ได้เปิดล็อก"** ไม่ใช่แค่ เปิด/ปิด ·
  สำรองข้อมูลบอกครบว่าไฟล์ไปไหน ควรใช้เมื่อไหร่ และกู้คืนแล้ว **ไม่รวมรหัสครู PIN และ session**
- หน้านำเข้า CSV โครงสร้างดี: มี 3 ขั้นตอนกำกับ · ปุ่มดาวน์โหลดแม่แบบ · บอก 4 คอลัมน์และเพดาน 1 MB ·
  มีโหมด `เพิ่มใหม่เท่านั้น` / `เพิ่มใหม่และอัปเดตรายชื่อเดิม` พร้อมอธิบายว่าโหมดอัปเดตเขียนทับอะไรบ้าง ·
  บังคับตรวจไฟล์ก่อนเพิ่มจริงเสมอ (**ค่าเริ่มต้นของช่องวันที่ผิด ดูข้อ 🔴 แยกต่างหาก**)
- หน้าติดตามมี **หน้าจอกำลังโหลดที่เขียนดี** ("กำลังสรุปแนวโน้มของห้องเรียน / กำลังสรุปภาพรวมของเดือนนี้…")
  → ยืนยันว่ารูปแบบนี้มีอยู่ในโค้ดเบสแล้ว จุดที่จอนิ่ง 16 วินาทีจึงแค่ยังไม่ได้ใช้ซ้ำ

### ยังไม่ได้ทดสอบ
- สั่งพิมพ์ PDF จริง (กล่องพิมพ์ของ macOS อยู่นอกเบราว์เซอร์) — ต้องตรวจเองว่าตาราง 40 แถวตัดหน้าตรงไหน
- หน้าผู้ปกครองแบบมีลิงก์จริง — **ไม่ติดขัดแล้ว** ลิงก์ที่ออกเมื่อ 16 ส.ค. 2569 อยู่ในคลิปบอร์ด
  ยังไม่ถูกใช้ อายุ 14 วัน · วางลงแถบที่อยู่แล้วเปิดได้เลย **แต่อย่ากดปุ่มลิงก์ผู้ปกครองซ้ำก่อน
  เพราะจะเพิกถอนใบนี้ทิ้ง**
- ส่งอีเมล / นำเข้า CSV จริง / กู้คืนไฟล์สำรอง — อยู่ในรายการห้ามกดทั้งหมด
