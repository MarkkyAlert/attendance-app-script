# TODO

คิวงานที่ยังไม่ได้ทำ ลบรายการออกเมื่อทำเสร็จและทดสอบผ่านแล้ว
กติกาถาวรของ repo อยู่ที่ `CLAUDE.md` ไฟล์นี้เก็บเฉพาะงานค้าง

ที่มา: UAT บนมือถือ 390x844 ผ่าน Chrome จริง วันที่ 15 ส.ค. 2569
ข้อมูลทดสอบ 40 คน ย้อนหลัง 3 เดือน

---

## รอบที่ 1 และ 2 — ปิดครบแล้ว

ทุกข้อทดสอบบนของจริงผ่านแล้ว รายละเอียดอยู่ในตาราง "ทำเสร็จและทดสอบผ่านแล้ว" ด้านล่าง

---

## รอบที่ 3 — ปิดครบแล้ว

ทุกข้อเป็นเรื่องความหมายและความชัดเจน ไม่ใช่บั๊ก รายละเอียดอยู่ในตารางด้านล่าง

**บันทึกไว้กันเข้าใจผิดซ้ำ**: TODO ฉบับก่อนเขียนว่าเกณฑ์ "ต้องติดตาม" คือ
"ขาดครบ 3 ครั้งแบบนับสะสมไม่จำกัดช่วง" ซึ่ง**ไม่ตรงกับโค้ดจริงสักที่**
ของจริงตอนนั้นมี 3 นิยามที่ไม่ตรงกันอยู่คนละที่ และตัวการของ "37 จาก 40 คน"
คือเงื่อนไข `absentCount > 0` ในตัวกรองฝั่ง client ไม่ใช่ค่าที่ครูตั้งไว้

---

## งานค้างอื่น

- [ ] **ทดสอบ restore backup ทั้งเส้น** — P0 ข้อเดียวที่ยังไม่เคยทดสอบจริง
      และเป็นช่องทางเดียวที่ครูซึ่งซื้อไปแล้วจะได้รับโค้ดเวอร์ชันใหม่
      ต้องทำเอง เพราะต้องเลือกไฟล์จากเครื่อง
- [ ] **ทดสอบล็อกอินใหม่** ยืนยันว่าโควตาอุปกรณ์ 5 เครื่องไม่ได้ล็อกตัวเอง
- [ ] **ลบไฟล์ CSV ค้างใน Drive 2 ไฟล์** จากการทดสอบก่อนแก้บั๊ก pop-up

- [ ] **★ ตัวเลขรายงานลดลง 39 รายการเองระหว่างรัน Diagnostics สองรอบห่างกัน 5 นาที — ยังหาสาเหตุไม่ได้**
      รอบ 14:24 รวม 2,391 · รอบ 14:29 รวม 2,352 (มาเรียน −36 สาย −1 ขาด −1 ลาป่วย −1)
      **เจ้าของยืนยันว่าไม่ได้แตะชีตด้วยมือระหว่างสองรอบ** และ `calendar:guard_direction`
      เป็นฟังก์ชันตัดสินล้วน ไม่เขียนอะไร จึงไม่ใช่ตัวการ
      −39 เท่ากับจำนวนนักเรียนที่ใช้งานอยู่พอดี = หายไป 1 วันเต็ม และตรงกับตัวเลขที่บันทึกไว้ในตาราง
      "ทำเสร็จและทดสอบผ่านแล้ว" ข้อ 1.1 ว่า **ส. 15 ส.ค. คือวันที่ทำให้ CSV ขยับ 2352 ↔ 2391**
      → วันเสาร์ที่เพิ่มเข้าปฏิทินตอนทดสอบรอบก่อน หลุดออกจากการคำนวณระหว่างสองรอบ
      `tested_date` ของ `calendar:guard_direction` (= วันที่ยืนยันล่าสุด) ก็ขยับ 15 ส.ค. → 7 ส.ค. สอดคล้องกัน
      **ยังไม่รู้ว่าอะไรทำให้มันหลุด** จะถูกกลบตอน re-seed จึงตรวจต่อไม่ได้ในรอบนี้
      → **หลัง seed ใหม่ ให้รัน Diagnostics สองรอบห่างกัน 5 นาทีแล้วเทียบตัวเลข** ถ้าขยับอีกคือบั๊กจริง
- [ ] **ทดสอบเก็บถาวรภาคเรียนบนของจริง** หลังแก้ `deleteSheetRowsByIndexes_` แล้ว
      เคสที่ต้องลอง: ภาคเรียนที่ข้อมูลในชีตเป็นของภาคเรียนนั้นทั้งหมด (ลบแถวข้อมูลจนหมด)
      เช็ค `delete_rows:all_data_rows_frozen_header` เขียวแล้วแต่ยังไม่เคยกด archive จริง

### ความไม่สม่ำเสมอในโค้ดเบส — รู้อยู่แล้ว ยังไม่แก้

ไม่มีข้อไหนเร่ง แต่ควรรู้ก่อนแตะส่วนนั้น ปิดข้อไหนแล้วให้ลบออกจากที่นี่

- [ ] **CSS ถูกส่งลง client ซ้ำสองรอบทุกครั้งที่โหลดหน้า** — `StyleReport` / `StylePhase3` / `StylePhase4`
      ถูก `include_` inline ตั้งแต่ `doGet` (`Index.html:125-127`) **และ** ถูกดึงซ้ำผ่าน `getClientStyleContent`
      เพราะ `isClientStyleLoaded_` เช็คว่ามี `<style id="client-style-X">` ซึ่งตัวจาก `include_` ไม่มี `id`
      → เสีย round-trip ต่อหน้า + มี CSS 2 ชุดซ้อนกัน + ตัวจาก cache (เก่าได้ถึง 15 นาที) ชนะ cascade
      **ควรเลือกทางเดียวแล้วลบอีกทางทิ้ง**
      - ตัดออกจาก `styleMap` [Code.js] + `PAGE_STYLE_MAP`/`CLIENT_STYLE_REGISTRY` → เหลือ inline อย่างเดียว
        ปลอดภัยกว่า และปิดปัญหา cache 15 นาทีฝั่ง CSS ไปเลย แลกกับหน้าแรกหนักขึ้น
      - ตัด `include_` ออก → เหลือ lazy อย่างเดียว **เสี่ยง FOUC เพราะ CSS มาช้ากว่า HTML**
        และต้องระวัง `StyleReport`/`Phase3`/`Phase4` ใช้ `var(--...)` แต่ไม่ได้นิยามเอง ต้องให้ `Stylesheet` มาก่อนเสมอ
- [ ] **เก็บกวาด: ลบ `pin` ออกจาก `styleMap`** [Code.js] — เป็น dead entry
      ฝั่ง server รับคำขอ `getClientStyleContent('pin')` ได้ แต่ฝั่ง client ไม่มี `pin` ใน `CLIENT_STYLE_REGISTRY`
      และไม่มีที่ไหนเรียก `ensureClientStyleLoaded('pin')` เลย (`StylePin` มาทาง `include_` อย่างเดียว)
      **ไม่เร่ง ไม่กระทบการทำงาน** แค่ทำให้คนอ่านเข้าใจผิดว่า `StylePin` โหลดแบบ lazy
- [ ] **`esc()` มีหลายชุด แยกเป็น 2 พฤติกรรม** — `JavaScript.html` / `JsProfile` / `ParentView` / `PrintReport`
      escape `"` และ `'` ด้วย ส่วน `JsReports` / `JsDashboard` / `JsAnalytics` / `JsImport` / `JsPhotoGrid` ไม่ escape
      ทั้งที่ทุกที่สร้าง HTML ด้วย string concat และยัดค่าลง attribute เป็นปกติ
      (ยังไม่เป็นช่องโหว่ที่ยิงได้จริง เพราะ `normalizeLimitedText_` กันไว้ที่ชั้น validation แล้ว)
      บางชุดใช้ `if (!str) return ''` ทำให้เลข `0` กลายเป็นสตริงว่าง
      **ถ้าจะรวมเป็นตัวเดียว ต้องใช้เวอร์ชันที่เข้มที่สุด (`JavaScript.html`)**
- [ ] **แยกประเภท error ด้วยการหาคำภาษาไทยในข้อความ** — `loadPage` (หา `'โมดูล'` / `'สไตล์'` / `'โค้ดของหน้า'`) ·
      `loadAttendance` (หา `'วันหยุดในปฏิทินวันเรียน'`) · `ParentView.html` (หา `'หมดอายุ'` / `'ไม่ถูกต้อง'`)
      **แก้ข้อความ error ฝั่ง server = ตรรกะควบคุมพังแบบไม่มี error ให้เห็น**
      ระบบมี protocol `CODE|message` (`parseServerError_`) อยู่แล้ว แต่จุดพวกนี้ไม่ได้ใช้
- [ ] **error handling ของ RPC ถูกก๊อปหลายชุด** — `serverCall` · `serverCallQuiet` · `fetchPageInitialData_` ·
      `handlePrintFailure` (ตัวหลังรู้จัก `PRINT_AUTH_*` ที่ตัวอื่นไม่รู้จัก) เพิ่ม error code ใหม่ต้องแก้ทุกที่
- [ ] **metadata ปลอมใน registry** — `CLIENT_MODULE_REGISTRY[].page` และ `CLIENT_STYLE_REGISTRY[].pages/.uses`
      **ไม่มีโค้ดตัวไหนอ่านเลย** ตัวที่มีผลจริงคือ `PAGE_MODULE_MAP` / `PAGE_STYLE_MAP` และ `.global`
      เป็นเอกสารที่ปลอมตัวเป็นโค้ด แก้แล้วไม่มีผล ควรลบหรือทำให้มีผลจริง
- [ ] **Chart.js โหลดจาก CDN ภายนอก** (`JsDashboard.html`) — external dependency ตัวเดียวของโปรเจกต์
      ถ้าเครือข่ายโรงเรียนบล็อก กราฟหายทั้งหมด (มี fallback อยู่)
- [ ] **`SCRIPT_URL` เป็น global จาก `Index.html`** ที่ `JsReports.html` ใช้ตรงๆ ไม่ผ่าน `AppShared`
- [ ] **`expires_at` ส่งไป-กลับแต่หน้าครูไม่เคยเช็คเอง** (ต่างจาก `ParentView.html` ที่เช็ค)
      ต้องยิง request ให้เด้งก่อนถึงจะรู้ว่าหมดอายุ อาการคือ "โหลดค้างแล้วเด้งหน้า login"

### ก่อนส่งมอบให้ลูกค้าทุกครั้ง
- [ ] ลบ `Diagnostics.gs` + เมนู 🧪 ตรวจงานแก้ P0
- [ ] ลบ `SeedTestData.gs` + เมนู 🧪 [ทดสอบ] ทั้ง 4 รายการ
- [ ] ประกาศ `oauthScopes` ใน appsscript.json ให้ชัด ไม่ปล่อยให้ auto-derive
      หน้าจอขอสิทธิ์ที่ครูเห็นตอนติดตั้งจะได้ไม่กว้างเกินจำเป็น

---

## บันทึกการตัดสินใจ

| เรื่อง | เลือก | เหตุผล |
|---|---|---|
| วันนอกปฏิทิน | เตือน + เพิ่มเข้าปฏิทินได้ทันที | ไม่ขวางงานครู ไม่ทำข้อมูลสูญ ตรงกับกิจกรรมวันเสาร์ที่เกิดจริง |
| ผูกอุปกรณ์ครู | จำกัดโควตา 5 เครื่อง ปฏิเสธเครื่องที่ 6 | บังคับเครื่องเดียวขัดกับ `MAX_TRUSTED_TEACHER_DEVICES` ที่ตั้งไว้ 5 |
| `AttendanceSummaryService.js` | ลบทิ้ง | ไม่มีใครเรียก และเขียนตรรกะนับซ้ำกับของจริง เสี่ยงตัวเลขสองชุดไม่ตรงกัน |
| กันสูตรในชีต | ใส่ `'` นำหน้า ไม่ใช่ปฏิเสธค่า | พิสูจน์บนชีตจริงแล้วว่า `'` ถูกกลืนตอนเขียน ค่ากลับมาครบ ไม่ต้องไล่ปฏิเสธ input |
| เกณฑ์ "ต้องติดตาม" | มาเรียนต่ำกว่า 80% ตรึงไว้ ไม่ให้ปรับ | เป็นเกณฑ์เดียวกับ ปพ.6 ที่ครูต้องส่งอยู่แล้ว ครูไม่ต้องจำสองมาตรฐาน · ค่าที่ครูตั้งไว้เดิม (`attention_threshold_days`) ไม่ถูกแตะ แค่หดขอบเขตให้เหลือเฉพาะรายการ 30 วันบนหน้าแรก |
| ตัวเลขใหญ่หน้าแรก | นับเฉพาะ "ขาด" ไม่รวมลาป่วย/ลากิจ | ขาดคือสิ่งเดียวที่ครูต้องลงมือทำต่อวันนี้ ส่วนคนที่ลาครูรู้อยู่แล้ว จึงยกไปบรรทัดรอง |

## ทำเสร็จและทดสอบผ่านแล้ว

| รอบ | ข้อ | ยืนยันด้วย |
|---|---|---|
| A | 1.0 กันข้อความผู้ใช้ไม่ให้กลายเป็นสูตร | `sheet_text:formula_injection` — เขียนดิบพังจริง (`=1+1 → 2`, `=ด.ช.สมชาย → #ERROR!`) เขียนผ่าน sanitize กลับมาครบทั้ง 7 ค่า |
| A | 2.3 เวลาอ่านกลับเป็นภาษาอังกฤษ | `timestamp:round_trip` — `sheets_converted_to_date: true` และแปลงกลับได้ตรงเป๊ะ |
| B | 1.4 ปฏิทินเข้าถึงได้แค่ 10 จาก 216 | บนของจริง: ค้น `ส.ค.` ได้ 21 รายการ · ดูเพิ่มจน 21/21 ปุ่มหายเอง · วันที่เป็น `จ. 3 พ.ย. 2568` · พิมพ์แล้ว focus ไม่หลุด |
| B | 1.2 แยกทิศกันแก้ปฏิทินย้อนหลัง | `calendar:guard_direction` 6 เคสผ่าน + ทิศ "เพิ่มเป็นวันเรียน" กดผ่านบนวันที่ยืนยันแล้วจริง |
| B | 1.1 เตือนเมื่อเช็คชื่อนอกปฏิทิน | แถบเตือนขึ้นบน ส. 15 ส.ค. · กดปุ่มแล้วแถบหายเอง · ปฏิทิน 216→217 · ขาด 119→120 สาย 114→115 · CSV รวม 2352→2391 (+39 = จำนวนคนของวันเสาร์พอดี) |
| B | 2.5 วันที่ในปฏิทินเป็น ISO | แสดงเป็นภาษาไทยแล้ว และตัดบรรทัดรายละเอียดที่ซ้ำกับป้ายสถานะออก |
| C | 1.5 วันร่างกับวันไม่มีข้อมูลแยกไม่ออก | บนของจริง: 10-14 ส.ค. เป็นแถบเหลืองต่อเนื่องมี `·` แยกจากวันยืนยันชัดเจน · คำอธิบายสัญลักษณ์ขึ้น "เช็คแล้วแต่ยังไม่ยืนยัน 5 วัน" ตรงกับข้อมูลจริง |
| C | 2.7 รายงานรายวันโหลดเอง + ช่วงเริ่มต้น | ขึ้น "กำลังโหลดรายงานรายวัน" แทน "ยังไม่มีรายงานรายวัน" · ช่วงเริ่มต้นเป็น 1-15 ส.ค. 2569 · คำเตือนเหลือ 5 วันแทน 156 วัน |
| C | 1.3 (ส่วนหน้าจอ) ระบุวันที่ตกหล่น | การ์ดขึ้น "วันเรียนที่ยังไม่ได้ยืนยัน 5 วัน" พร้อมวันที่ไทยครบทั้ง 5 วัน แทนที่จะบอกแค่จำนวน |
| C | ปุ่ม "นับเป็นวันเรียน" บนการ์ด | สร้างเคสจริงแล้วกดทดสอบ: 216→217 · ขาด 119→120 สาย 114→115 · รวม 2076→2113 · การ์ดหายไปเอง · ตัวเลขกลับเท่าเดิมทุกตัว ไม่มีเรคอร์ดสูญหาย |
| B | กันลบวันเรียนของวันที่ยืนยันแล้ว | ทดสอบซ้ำแล้วยังบล็อกถูกต้อง และพอยกเลิกการยืนยันก่อน ลบผ่านทันที — กติกาขึ้นกับสถานะจริง ไม่ใช่บล็อกตายตัว |
| D | 2.1 ปุ่มสถานะที่ 5 ตกบรรทัด | บนมือถือ: 5 ปุ่มอยู่แถวเดียว ปุ่มล้างสถานะกินแถวของตัวเอง |
| D | 2.4 กดทับสถานะได้เลย | แตะ "สาย" ทับ "มา" ได้จังหวะเดียว · ประวัติได้ "แก้ไขสถานะ มาเรียน → สาย" บรรทัดเดียว เทียบกับของเก่าในจอเดียวกันที่เป็น "ยกเลิกสถานะ" + "บันทึกสถานะ" |
| D | กันประวัติเปล่าจากการแตะสถานะเดิม | เจอ "แก้ไขสถานะ สาย → สาย" ตอนทดสอบ แก้ให้ `markStatus` ออกทันทีถ้าสถานะตรงกับของเดิม |
| D | 2.8 ข้อความปฏิเสธการลบปฏิทิน | เพิ่มวิธีแก้ต่อท้ายแล้ว ให้รูปแบบเดียวกับฝั่งบันทึก |
| D | 2.2 ช่องวันที่ต้องกด Enter | พิมพ์ 13082026 โดยไม่กด Enter → ช่องเก็บครบ 13/08/2026 · ไม่มี error แดง · เปลี่ยนไป พฤ. 13 ส.ค. 2569 พร้อมข้อมูลถูก (34/2/3) |
| E | 1.3 คำเตือนบนเอกสาร ปพ.6 | เอกสารไม่มีกรอบแดงแล้ว มีบรรทัดขอบเขตสีปกติ "คำนวณจากวันเรียนที่ยืนยันแล้ว 61 วัน จากทั้งหมด 217 วัน" · หัวเอกสารกับคอลัมน์วันเรียนตรงกันที่ 61 ทุกแถว |
| E | บล็อกพิมพ์เมื่อมีวันหลุดปฏิทิน | สร้างเคสจริงแล้วกดพิมพ์ → ไม่มีแท็บพิมพ์เปิด ขึ้นข้อความบอกทางแก้ · กด "นับเป็นวันเรียน" คืนสภาพ 216→217 แล้วพิมพ์ได้ตามปกติ |
| E | 2.6 ช่องว่างคั่นคอลัมน์ที่ตรึงไว้ | แก้ได้รอบที่ 4 · ต้นเหตุจริงคือ `table-layout: auto` ทำให้ `width` บนเซลล์เป็นแค่ข้อเสนอ ล็อกความกว้างไม่ได้ · เปลี่ยนเป็น `fixed` แล้วคุมด้วยตัวแปรเดียว · ของแถม: ชื่อนักเรียนไม่ถูกตัดด้วย `...` อีกแล้ว (ปิดส่วนหนึ่งของ 3.4) |
| F | 3.2 "ต้องติดตาม" มี 3 นิยามไม่ตรงกัน | บนของจริง: ชิปลดจาก **37 เหลือ 1** และแถวที่ติดป้ายแดงเหลือคนเดียวตรงกัน (เลขที่ 39 ด.ญ.ศิรดา 75.4%) ตรงกับที่ไฮไลต์บน ปพ.6 · ใต้แถบกรองขึ้นบอกเกณฑ์ตรงๆ ว่าคือต่ำกว่า 80% เกณฑ์เดียวกับ ปพ.6 · หน้าแรกไม่มีคำว่า "ต้องติดตาม" ซ้ำอีก เปลี่ยนเป็น "เริ่มขาด/สายบ่อยช่วงนี้" (5 คน คนละชุดกับชิป ตามที่ตั้งใจ) |
| F | 3.1 ตัวเลขใหญ่หน้าแรก | ทดสอบครบ 3 สถานะบนวันเดียวกัน: ยังไม่เช็ค → วงแหวน 0% (0/39) · เช็คมาครบ → "39 มาครบทุกคน" · ตั้งขาด 1 สาย 1 → **"1 คนขาดวันนี้"** สีแดง พร้อมบรรทัดรอง "สาย 1" · ล้างข้อมูลทดสอบคืนสภาพ 0/39 แล้ว |
| F | 3.4 ถ้อยคำอ่านได้สองแบบ | บนของจริง: **"ใช้อยู่ 1 จาก 5 เครื่อง"** (เพดานส่งมาจาก server ไม่ได้ hard-code ฝั่ง client) · **"การล็อกหน้าจอ: ปิดอยู่ · ตั้ง PIN แล้วแต่ยังไม่ได้เปิดล็อก"** อ่านได้แบบเดียว |
| F | 3.3 ช่วงเวลาที่กำลังดู | หน้าติดตามขึ้น **"ช่วงที่ใช้จริง: 1 ส.ค. 2569 – 31 ส.ค. 2569"** และกดถอยเดือนแล้วเปลี่ยนตามเป็น 1–31 ก.ค. 2569 (payload ส่ง `from_th`/`to_th` มาอยู่แล้วแต่ไม่เคยถูกใช้) |

## หมายเหตุการทำงาน

ทดสอบในเครื่องไม่ได้ ทุกรอบต้อง `clasp push` → สร้าง deployment version ใหม่ → ทดสอบบนของจริง
การมัดหลายข้อไว้ในรอบเดียวจึงคุ้มกว่าแก้ทีละข้อ

ข้อ 1.1 ต้องทดสอบด้วยการเช็คชื่อในวันที่ไม่อยู่ในปฏิทิน ซึ่ง seed ปัจจุบันไม่มีเคสนี้
(ปฏิทินชุดที่ใช้อยู่ไม่มีวันหยุดเลย) ต้องลบรายการปฏิทินบางวันออกก่อนทดสอบ
— และตอนนี้ลบผ่านหน้าจอได้แค่ 10 วันแรกเท่านั้นตามข้อ 1.4

### สิ่งที่ทดสอบแล้วผ่าน ไม่ต้องแตะ
- เอกสาร ปพ.6 หารวันเรียนตามช่วงที่เด็กอยู่ในห้องจริง (ย้ายเข้ากลางเทอม 17 วัน / ย้ายออก 55 วัน)
- รายงานรายวันทั้งภาคเรียน (40 คน × 217 วัน) โหลดใน **< 5 วินาที** (วัดตอน cache อุ่น)
- หน้าผู้ปกครองเมื่อลิงก์ไม่ถูกต้อง: ขึ้น "ลิงก์ไม่ถูกต้อง" พร้อมบอกวิธีแก้
  ไม่หลุดชื่อโรงเรียน ชื่อครู หรือข้อความ error ทางเทคนิค
- หน้านำเข้า CSV: มีแม่แบบ บอก 4 คอลัมน์ จำกัด 1 MB มี 2 โหมด และบังคับ "เริ่มอยู่ในห้องตั้งแต่"
- มือถือ: มีแถบเมนูล่าง 5 ปุ่ม ฟอร์มตั้งค่าเต็มความกว้าง ตั้งค่าขั้นสูงถูกซ่อนไว้หลังปุ่ม
- ตั้งค่าขั้นสูง > กู้คืน: บอกชัดว่าจะเขียนทับอะไรบ้าง และบอกว่า "ไม่รวมรหัสครู, PIN และ session"

### ยังไม่ได้ทดสอบ
- สั่งพิมพ์ PDF จริง (กล่องพิมพ์ของ macOS อยู่นอกเบราว์เซอร์) — ต้องตรวจเองว่าตาราง 40 แถวตัดหน้าตรงไหน
- หน้าผู้ปกครองแบบมีลิงก์จริง — ต้องออกลิงก์ใหม่ ซึ่งอยู่ในรายการห้ามกด
- ส่งอีเมล / นำเข้า CSV จริง / กู้คืนไฟล์สำรอง — อยู่ในรายการห้ามกดทั้งหมด
