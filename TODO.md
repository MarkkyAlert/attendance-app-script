# TODO

คิวงานที่ยังไม่ได้ทำ ลบรายการออกเมื่อทำเสร็จและทดสอบผ่านแล้ว
กติกาถาวรของ repo อยู่ที่ `CLAUDE.md` ไฟล์นี้เก็บเฉพาะงานค้าง

ที่มา: UAT บนมือถือ 390x844 ผ่าน Chrome จริง วันที่ 15 ส.ค. 2569
ข้อมูลทดสอบ 40 คน ย้อนหลัง 3 เดือน

---

## รอบที่ 1 — ความน่าเชื่อถือของตัวเลข (P0)

ข้อเดียวที่ทำให้ครูไม่กล้าเชื่อตัวเลข ที่เหลือคือความรำคาญ
ลำดับการแก้: 1.0 → 1.4 → 1.1 → 1.2 → 1.3 (1.4 ต้องเสร็จก่อน ไม่งั้น 1.1 แก้แล้วครูตรวจสอบไม่ได้)

### 1.0 ชื่อที่ขึ้นต้นด้วย = กลายเป็น #ERROR! บนเอกสาร ปพ.6
- **อาการ**: นักเรียนเลขที่ 28 ที่ชื่อขึ้นต้นด้วย `=` แสดงเป็น `#ERROR!`
  ทั้งในระบบและ**บนเอกสาร ปพ.6 ที่ส่งโรงเรียน** ชื่อเด็กหายไปทั้งชื่อ
- **ต้นตอ**: เขียนชื่อลงชีตด้วย `appendRow_` / `setValues` ตรงๆ
  Google Sheets ตีความค่าที่ขึ้นต้นด้วย `=` `+` `-` `@` เป็นสูตร
  `setNumberFormat('@')` ก่อนเขียนกันไม่อยู่ (ทดสอบแล้วยังเป็น #ERROR!)
- **เข้าถึงได้จากการใช้งานปกติ**: ฟอร์ม "เพิ่มนักเรียน" ปล่อยให้พิมพ์ `=` ได้
  `normalizeLimitedText_` กันแค่ `<` `>` backtick ไม่ได้กันอักขระนำสูตร
- **ขอบเขต**: ต้องกันทุกทางที่เขียนข้อความของผู้ใช้ลงชีต ไม่ใช่แค่ชื่อนักเรียน
  (ชื่อเล่น กลุ่ม หมายเหตุ ชื่อโรงเรียน ชื่อครู ชื่อภาคเรียน รายละเอียดปฏิทิน)
- **หมายเหตุ**: ฝั่ง export CSV มี `sanitizeCsvCell_` กันอยู่แล้ว แต่ฝั่งเขียนลงชีตไม่มี

### 1.4 ปฏิทินวันเรียนเข้าถึงได้แค่ 10 วันแรกจาก 216 วัน
- **อาการ**: หน้าตั้งค่า > ขั้นตอนที่ 2.5 แสดงรายการปฏิทินแล้วปิดท้ายว่า
  "แสดง 10 รายการแรกจากทั้งหมด 216 รายการ" ไม่มีปุ่มดูเพิ่ม ไม่มีช่องค้นหา ไม่มีการแบ่งหน้า
  รายการที่เห็นคือ 2025-11-03 ถึง 2025-11-14 ซึ่งเป็นวันที่เก่าที่สุดของภาคเรียน
- **ผลคือ**: วันที่ 11–216 **แก้ไขหรือลบผ่านหน้าจอไม่ได้เลย** ถ้าวันหยุดตกอยู่กลางเทอม
  ครูหาไม่เจอ ต้องไปแก้ในชีตเอง
- **ทำไมต้องอยู่รอบ 1**: ข้อ 1.1 จะให้ครูกด "เพิ่มเป็นวันเรียน" จากหน้าเช็คชื่อ
  แต่ถ้าครูตรวจสอบหรือย้อนกลับรายการนั้นไม่ได้ การแก้ก็ไม่จบ
- **ขั้นต่ำที่ต้องมี**: ช่องค้นหาตามวันที่ + กรองเฉพาะวันหยุด + ปุ่มดูเพิ่ม

### 1.5 วันที่ยังไม่ยืนยันกับวันที่ไม่มีข้อมูล แสดงเหมือนกันทุกประการ
- **อาการ**: รายงานรายวันแสดง `–` ทั้งสองกรณี แยกไม่ออก
- **พิสูจน์แล้ว**: ศ. 14 ส.ค. 2569 หน้าเช็คชื่อขึ้น "เช็คครบ 39 คน · 34 มา / 2 สาย / 3 ขาด"
  สถานะ "ยังไม่ยืนยัน" — แต่ในรายงานรายวันคอลัมน์นั้นเป็น `–` เหมือน 3 พ.ย. 2568 ที่ไม่มีข้อมูลเลย
- **ผลคือ**: ครูที่เช็คทุกวันแต่ลืมกดยืนยัน จะเห็นรายงานว่างเปล่าและคิดว่าระบบทำข้อมูลหาย
- **ทางแก้**: ใช้สัญลักษณ์คนละตัวสำหรับ "ร่าง" กับ "ไม่มีข้อมูล" + คำอธิบายสัญลักษณ์ใต้ตาราง
- ⚠️ ห้ามเปลี่ยนให้รายงานนับวันร่าง — การกรองถูกแล้ว ที่ผิดคือการแสดงผล

### 1.1 เตือนเมื่อเช็คชื่อในวันที่ไม่อยู่ในปฏิทินวันเรียน
- **อาการ**: เช็คชื่อ 39 คน กดยืนยันได้ตามปกติ แต่ข้อมูลไม่เข้ารายงาน
  ไม่มีอะไรเตือนเลยระหว่างทาง (15 ส.ค. 2569 เป็นวันเสาร์ ปฏิทินมีแต่จันทร์-ศุกร์)
- **ตัดสินใจแล้ว**: เตือน + ให้กดเพิ่มเข้าปฏิทินได้ทันทีจากหน้าเช็คชื่อ ไม่บล็อก
- **ต้องทำ**
  - หน้าเช็คชื่อ: แถบเตือนเหนือรายชื่อ + ปุ่ม "เพิ่มเป็นวันเรียน"
  - กล่องยืนยันวัน: เพิ่มบรรทัดเตือนถ้าวันนั้นยังไม่อยู่ในปฏิทิน
- **มีของให้ใช้ซ้ำแล้ว**: แท็บ "รายงานรายวัน" มีการ์ด "ตรวจพบข้อมูลที่ควรแก้จากปฏิทิน"
  ที่ระบุวันที่ชัดๆ ("ส. 15 ส.ค. 2569 · พบข้อมูลเช็คชื่อ 39 รายการในวันที่ตั้งเป็นวันหยุด")
  พร้อมปุ่มลงมือทำ — เอา pattern นี้ไปใช้ที่หน้าสรุปและหน้าเช็คชื่อได้เลย ไม่ต้องออกแบบใหม่
- **แต่ทางเลือกที่มีอยู่มีแต่ทางทำลาย**: ปุ่มเดียวที่ให้คือ "ล้างข้อมูลวันหยุด" = ลบงานที่ครูเช็คไปแล้ว
  ต้องเพิ่มปุ่ม "เพิ่มวันนี้เข้าปฏิทิน" คู่กัน และให้เป็นตัวเลือกแรก

### 1.2 แยกเงื่อนไขกันแก้ปฏิทินย้อนหลัง
- `saveSchoolCalendarEntry` [CalendarService.js:93] ปฏิเสธการแก้ปฏิทินย้อนหลัง
  ทุกกรณีถ้าวันนั้นมีข้อมูลเช็คชื่อที่ยืนยันแล้ว
- แต่สองกรณีอันตรายไม่เท่ากัน
  - ตั้งเป็น **วันหยุด** → ข้อมูลหลุดออกจากรายงาน ต้องกันต่อไป
  - เพิ่มเป็น **วันเรียน** → ข้อมูลถูกนับเข้ามา ปลอดภัย ควรอนุญาต
- ถ้าไม่แก้ข้อนี้ ปุ่มในข้อ 1.1 จะกดไม่ผ่านในเคสที่ต้องใช้จริงที่สุด

### 1.3 ตัวนับวันในรายงานต้องตรงกับตัวเลขที่นับจริง
- **อาการ**: หลังยืนยันวันเสาร์ บรรทัดสีฟ้าขึ้นจาก "ยืนยันข้อมูลแล้ว 60 จาก 216 วัน"
  เป็น 61 แต่ ขาด 119 / สาย 114 เท่าเดิม
- ให้แสดงจำนวนวันที่นับจริง และระบุวันที่ตกหล่นเป็นวันที่ชัดๆ
  ข้อมูลมีอยู่แล้วใน `holiday_conflicts` แค่ยังไม่ได้เอาขึ้นมาแสดง
- ⚠️ ห้ามแตะสูตรใน `buildAttendanceComputationContext_` แก้เฉพาะตัวเลขที่เอาไปแสดง
- **ปรากฏบนเอกสาร ปพ.6 ด้วย**: หัวเอกสารเขียน "วันที่มีข้อมูลยืนยันแล้ว 61 วัน"
  แต่คอลัมน์ "วันเรียน" ของนักเรียนทุกคนเป็น 60 — ตัวเลขขัดกันเองบนเอกสารทางการ
- **คำเตือนสีแดงถูกพิมพ์ลงบนเอกสารด้วย** ครูคงไม่อยากส่ง ปพ.6 ที่มีคำเตือนสีแดง
  ควรให้แก้ให้จบก่อนพิมพ์ ไม่ใช่พิมพ์คำเตือนติดไปกับเอกสาร

---

## รอบที่ 2 — แรงเสียดทานรายวัน (P1)

### 2.1 ปุ่มสถานะที่ 5 ตกบรรทัดบนมือถือ (CSS, เบามาก)
- จอ 390px: มา/สาย/ขาด/ป่วย อยู่บรรทัดแรก "กิจ" ตกไปบรรทัดสองตัวเดียว
- ทำให้การ์ดสูงขึ้น ~30% ต้องเลื่อนมากขึ้นทุกเช้าที่เช็ค 39 คน

### 2.2 ช่องเลือกวันที่ต้องกด Enter ถึงจะเปลี่ยนหน้า (client, เบามาก)
- พิมพ์ 05/08/2026 แล้วหัวข้อยังค้างวันเดิม ดูเหมือนระบบค้าง

### 2.3 เวลาในประวัติแก้ไขเป็นภาษาอังกฤษดิบ (server, เบา)
- แสดง `Sat Aug 15 2026 13:03:59 GMT+0700 (Indochina Time)`
- **ต้นตอ**: `nowString_()` เขียนเป็นข้อความ แต่ Sheets แปลงเป็น Date object ให้เอง
  พอ [AttendanceService.js:1239] อ่านกลับด้วย `String(row[...])` จึงได้ default ของ JS
- ⚠️ ไล่คอลัมน์เวลาอื่นทั้งชุดด้วย: `บันทึกเมื่อ` `แก้ไขเมื่อ` `ยืนยันเมื่อ`
  เป็นบั๊กตระกูลเดียวกับที่ `createTextFinder` ค้นวันที่แบบ text

### 2.4 แก้สถานะให้กดทับได้เลย (client, กลาง)
- ตอนนี้ต้องกด "ล้างสถานะ" ก่อนแล้วค่อยเลือกใหม่ = 2 จังหวะต่อการแก้ 1 คน
  และได้ประวัติ 2 บรรทัด (ยกเลิกสถานะ + บันทึกสถานะ)
- **ฝั่ง server รองรับการเขียนทับอยู่แล้ว** `recordAttendance` อัปเดตแถวเดิม
  และบันทึก log เป็น `update` เดิม→ใหม่ บรรทัดเดียว
- แก้ client จุดเดียว (แสดงปุ่มสถานะต่อไปแม้มีสถานะแล้ว) ได้ผล 2 เรื่องพร้อมกัน

### 2.5 วันที่ในรายการปฏิทินเป็นรูปแบบ ISO ไม่ใช่ภาษาไทย (client, เบามาก)
- หน้าตั้งค่า > ปฏิทินวันเรียน แสดง `2025-11-03` ทั้งที่ทั้งระบบใช้ `3 พ.ย. 2568`
- เป็น ค.ศ. ด้วย ครูต้องแปลงปีเองในหัวทุกบรรทัด
- บรรทัดคำอธิบายใต้วันที่เขียนว่า "วันเรียน" ซ้ำกับป้ายสถานะข้างบนพอดี ตัดออกได้

### 2.7 รายงานรายวันต้องโหลดเอง และต้องเริ่มที่ช่วงที่มีข้อมูล (client, กลาง)
- **ทำคู่กับข้อ 1.5 ในรอบ push เดียว** เพราะเป็นอาการเดียวกัน — ครูเปิดมาเจอจอว่างแล้วคิดว่าข้อมูลหาย
- **อาการที่ 1 — ไม่โหลดเอง**: แท็บ "สรุปภาพรวม" โหลดทันทีที่เข้า
  แต่แท็บ "รายงานรายวัน" ข้างๆ กันต้องกด "โหลดข้อมูล" ก่อน พฤติกรรมสองแท็บไม่ตรงกัน
  และหน้าติดตามที่ช้ากว่า (15–20 วิ) กลับโหลดเอง ส่วนหน้านี้ที่เร็วกว่ากลับต้องกด
- **อาการที่ 2 — ข้อความตอนว่างอ่านผิดได้**: ขึ้นว่า "ยังไม่มีรายงานรายวัน"
  อ่านเหมือน "ไม่มีข้อมูล" ไม่ใช่ "กดปุ่มเพื่อโหลด"
- **อาการที่ 3 — ช่วงเริ่มต้นชี้ไปที่ช่องว่าง**: ตั้งไว้ทั้งภาคเรียน (1 พ.ย. 2568 – 31 ส.ค. 2569)
  217 คอลัมน์ โดยคอลัมน์แรกๆ ไม่มีข้อมูล ครูต้องเลื่อนขวา ~200 คอลัมน์ถึงจะเจอของจริง
  **เปิด auto-load เฉยๆ โดยไม่แก้ข้อนี้ จะยิ่งแย่ลง** เพราะเจอกำแพง `–` เต็มจอทันทีที่เข้า
- **ทางแก้**: โหลดเองเมื่อเข้าแท็บ + ตั้งช่วงเริ่มต้นเป็นเดือนปัจจุบัน
  + ปุ่ม "ดูทั้งภาคเรียน" ไว้ให้ขยายเอง
- ⚠️ **วัดเวลาบน cache เย็นก่อนตัดสินใจ**: ที่วัดได้ < 5 วินาที (40 คน × 217 วัน)
  เป็นการวัดหลังเพิ่งสั่งพิมพ์ ปพ.6 ช่วงเดียวกัน derived cache จึงอุ่นอยู่แล้ว
  ถ้าโหลดเย็นแล้วช้ากว่านี้มาก ให้ auto-load เฉพาะช่วงเดือนปัจจุบัน
  ส่วนทั้งภาคเรียนคงปุ่มกดไว้เหมือนเดิม

### 2.6 หัวตารางรายงานรายวัน: ข้อความวันที่ทะลุใต้คอลัมน์ตรึง (CSS, เบามาก)
- เลื่อนตารางแนวนอนแล้วหัวคอลัมน์ "เลขที่ / ชื่อ" มีข้อความวันที่โผล่ทับ
  (เห็นเป็น `เลขที่ . 1` และมี `2569` ลอยอยู่ระหว่าง ชื่อ กับคอลัมน์วันแรก)
- เซลล์หัวตารางที่ตรึงไว้ยังพื้นหลังโปร่ง ใส่สีทึบให้เท่าแถวข้อมูลก็จบ

---

## รอบที่ 3 — ต้องตัดสินใจก่อนถึงจะแก้ได้ (P2)

### 3.1 หน้าแรกไม่ตอบคำถาม "วันนี้ขาดกี่คน"
- ตัวเลขใหญ่สุดคือ "เช็คชื่อแล้ววันนี้ 100%" ซึ่งบอกว่าครูทำงานเสร็จหรือยัง
  ไม่ได้บอกว่าเด็กมากี่คน ต้องเลื่อนลง 2 หน้าจอถึงเจอ 37/1/1
- **ต้องตัดสินใจ**: ตัวเลขใหญ่สุดบนหน้าแรกควรเป็นอะไร

### 3.2 "ต้องติดตาม 37 คน จาก 40 คน"
- เกณฑ์ปัจจุบันคือขาดครบ 3 ครั้งแบบนับสะสมไม่จำกัดช่วง
  ยิ่งเทอมยาวยิ่งเข้าเกณฑ์ทุกคน สัญญาณเตือนที่เตือนเกือบทั้งห้อง = ไม่ใช่สัญญาณ
- **ต้องตัดสินใจ**: นิยามใหม่ของ "เด็กที่ต้องติดตาม"
  (เชิงสัดส่วน เช่น ต่ำกว่า 80% / จำกัดหน้าต่างเวลา เช่น 30 วันล่าสุด / ขาดติดกัน)

### 3.3 ช่วงเวลาเริ่มต้นของแต่ละหน้าไม่ตรงกัน
- หน้ารายงานเปิดมาเป็น**ทั้งภาคเรียน** (1 พ.ย. 2568 – 31 ส.ค. 2569)
  หน้าติดตามเปิดมาเป็น**เดือนเดียว** (สิงหาคม 2569)
- ครูที่เอาสองหน้ามาเทียบกันจะสรุปว่าตัวเลขไม่ตรง ทั้งที่ทั้งคู่ถูก
- พ่วงกับข้อ 1.3: หน้ารายงานรายวันเขียน "ครอบคลุม 217 วัน" แต่การ์ดข้างล่างเขียน
  "216 วันเรียนตามปฏิทิน" ห่างกันไม่ถึงหนึ่งหน้าจอ (217 = 216 + วันเสาร์ที่หลุดเข้ามา)
- **ต้องตัดสินใจ**: ยึดช่วงเดียวกันทุกหน้า หรือแสดงช่วงให้เด่นจนไม่มีทางเข้าใจผิด

### 3.4 ถ้อยคำที่อ่านได้สองแบบ (เบา แต่ต้องเลือกคำ)
- ตั้งค่าขั้นสูง > "อุปกรณ์ที่อนุญาต: **ใช้งานได้ 1 อุปกรณ์**"
  อ่านได้ว่า "ตอนนี้มี 1 เครื่อง" หรือ "อนุญาตได้แค่ 1 เครื่อง" — ของจริงคือ 1 จาก 5
- PIN: "สถานะ PIN ตั้งค่าแล้ว" แต่บรรทัดถัดมา "การล็อกหน้าจอ **ปิดอยู่**"
  ครูที่ตั้ง PIN แล้วย่อมคิดว่าเครื่องถูกล็อกแล้ว ทั้งที่ยังต้องกด "เปิดการล็อกหน้าจอ" อีกที
- ตารางรายงานรายวัน: ชื่อถูกตัดด้วย `...` ตั้งแต่ตัวที่ ~14
  ("ด.ญ.ณิชาภัทร แก้..." / "ด.ญ.พิมพ์ชนก แซ่...") เด็กชื่อต้นเหมือนกันแยกไม่ออก

---

## งานค้างอื่น

- [ ] **ทดสอบ restore backup ทั้งเส้น** — P0 ข้อเดียวที่ยังไม่เคยทดสอบจริง
      และเป็นช่องทางเดียวที่ครูซึ่งซื้อไปแล้วจะได้รับโค้ดเวอร์ชันใหม่
      ต้องทำเอง เพราะต้องเลือกไฟล์จากเครื่อง
- [ ] **ทดสอบล็อกอินใหม่** ยืนยันว่าโควตาอุปกรณ์ 5 เครื่องไม่ได้ล็อกตัวเอง
- [ ] **merge `fix/p0-broken-features` เข้า main** ตอนนี้ main ยังเป็นโค้ดก่อนแก้ทั้งหมด
- [ ] **ลบไฟล์ CSV ค้างใน Drive 2 ไฟล์** จากการทดสอบก่อนแก้บั๊ก pop-up

### ก่อนส่งมอบให้ลูกค้าทุกครั้ง
- [ ] ลบ `Diagnostics.gs` + เมนู 🧪 ตรวจงานแก้ P0
- [ ] ลบ `SeedTestData.gs` + เมนู 🧪 [ทดสอบ] ทั้ง 4 รายการ
- [ ] ประกาศ `oauthScopes` ใน appsscript.json ให้ชัด ไม่ปล่อยให้ auto-derive
      หน้าจอขอสิทธิ์ที่ครูเห็นตอนติดตั้งจะได้ไม่กว้างเกินจำเป็น

---

## บันทึกการตัดสินใจ

| เรื่อง | เลือก | เหตุผล |
|---|---|---|
| วันนอกปฏิทิน | เตือน + เพิ่มเข้าปฏิทินได้ทันที | ไม่ขวางงานครู ไม่ทำข้อมูลสูญ ตรงกับกิจกรรมวันเสาร์ที่เกิดจริง |
| ผูกอุปกรณ์ครู | จำกัดโควตา 5 เครื่อง ปฏิเสธเครื่องที่ 6 | บังคับเครื่องเดียวขัดกับ `MAX_TRUSTED_TEACHER_DEVICES` ที่ตั้งไว้ 5 |
| `AttendanceSummaryService.js` | ลบทิ้ง | ไม่มีใครเรียก และเขียนตรรกะนับซ้ำกับของจริง เสี่ยงตัวเลขสองชุดไม่ตรงกัน |

## หมายเหตุการทำงาน

ทดสอบในเครื่องไม่ได้ ทุกรอบต้อง `clasp push` → สร้าง deployment version ใหม่ → ทดสอบบนของจริง
การมัดหลายข้อไว้ในรอบเดียวจึงคุ้มกว่าแก้ทีละข้อ

ข้อ 1.1 ต้องทดสอบด้วยการเช็คชื่อในวันที่ไม่อยู่ในปฏิทิน ซึ่ง seed ปัจจุบันไม่มีเคสนี้
(ปฏิทินชุดที่ใช้อยู่ไม่มีวันหยุดเลย) ต้องลบรายการปฏิทินบางวันออกก่อนทดสอบ
— และตอนนี้ลบผ่านหน้าจอได้แค่ 10 วันแรกเท่านั้นตามข้อ 1.4

### สิ่งที่ทดสอบแล้วผ่าน ไม่ต้องแตะ
- เอกสาร ปพ.6 หารวันเรียนตามช่วงที่เด็กอยู่ในห้องจริง (ย้ายเข้ากลางเทอม 17 วัน / ย้ายออก 55 วัน)
- รายงานรายวันทั้งภาคเรียน (40 คน × 217 วัน) โหลดใน **< 5 วินาที**
  (แต่การที่ต้องกดโหลดเองไม่ใช่ข้อดี ดูข้อ 2.7 — และตัวเลขนี้วัดตอน cache อุ่น)
- หน้าผู้ปกครองเมื่อลิงก์ไม่ถูกต้อง: ขึ้น "ลิงก์ไม่ถูกต้อง" พร้อมบอกวิธีแก้
  ไม่หลุดชื่อโรงเรียน ชื่อครู หรือข้อความ error ทางเทคนิค
- หน้านำเข้า CSV: มีแม่แบบ บอก 4 คอลัมน์ จำกัด 1 MB มี 2 โหมด และบังคับ "เริ่มอยู่ในห้องตั้งแต่"
- มือถือ: มีแถบเมนูล่าง 5 ปุ่ม ฟอร์มตั้งค่าเต็มความกว้าง ตั้งค่าขั้นสูงถูกซ่อนไว้หลังปุ่ม
- ตั้งค่าขั้นสูง > กู้คืน: บอกชัดว่าจะเขียนทับอะไรบ้าง และบอกว่า "ไม่รวมรหัสครู, PIN และ session"

### ยังไม่ได้ทดสอบ
- สั่งพิมพ์ PDF จริง (กล่องพิมพ์ของ macOS อยู่นอกเบราว์เซอร์) — ต้องตรวจเองว่าตาราง 40 แถวตัดหน้าตรงไหน
- หน้าผู้ปกครองแบบมีลิงก์จริง — ต้องออกลิงก์ใหม่ ซึ่งอยู่ในรายการห้ามกด
- ส่งอีเมล / นำเข้า CSV จริง / กู้คืนไฟล์สำรอง — อยู่ในรายการห้ามกดทั้งหมด
