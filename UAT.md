# UAT — สคริปต์ทดสอบก่อนปล่อย

สคริปต์สำหรับให้ AI เดินทดสอบระบบในบทบาทครูจริง ใช้ซ้ำได้ทุกรอบก่อนส่งมอบ
**ก่อนใช้ทุกครั้ง ให้เติมช่องว่างในหัวข้อ "ของที่ต้องเตรียม" ให้ครบก่อน ไม่งั้นเดินไม่จบ**

---

## ═══ ของที่ต้องเตรียม (คนสั่งงานกรอก) ═══

| รายการ | ค่า |
|---|---|
| **URL ของ deployment** | `___________________________` |
| **วันที่ระบบมองว่าเป็น "วันนี้"** | `_______` · เป็นวัน `____` (จ/อ/พ/พฤ/ศ/ส/อา) |
| **รัน seed ใหม่แล้วหรือยัง** | ☐ รันแล้ว ☐ ยังไม่รัน (ถ้ายัง ต้องเช็คสภาพข้อมูลก่อนเริ่ม) |
| **ลิงก์ผู้ปกครองเดิม (ถ้ามี)** | `___________________________` |

**★ กติกาที่คนสั่งงานต้องทำ**

1. **ล็อกอินค้างไว้ให้ก่อนเริ่ม** — ผู้ทดสอบพิมพ์รหัสครูไม่ได้ (เป็นรหัสผ่าน)
2. **ห้าม deploy version ใหม่ระหว่างที่ทดสอบอยู่** — session ครูเก็บใน CacheService
   พอ deploy ใหม่จะถูกล้างทันที แล้วผู้ทดสอบเข้าใหม่เองไม่ได้ ต้องเริ่มนับหนึ่งใหม่
3. **ถ้าเพิ่ง `clasp push` + deploy ให้ทิ้งไว้อย่างน้อย 15 นาทีก่อนเริ่ม** — เหตุผลอยู่ในกับดักข้อ 2
4. **ปิด PIN lock ไว้** — ถ้าเปิดไว้ ผู้ทดสอบจะติดตั้งแต่หน้าแรกเพราะใส่ PIN ไม่ได้

---

## ═══ บทบาท ═══

คุณคือครูประจำชั้นประถมในไทย อายุ 45 ปี ใช้เทคโนโลยีได้ระดับกลาง
เพิ่งซื้อระบบเช็คชื่อนี้มาใช้ กำลังลองเดินดูรอบแรกว่ามันเข้ากับชีวิตการทำงานจริงไหม

**ตั้ง viewport มือถือ 390x844** ครูใช้งานจริงบนมือถือ
⚠️ **ถ้าเครื่องมือตั้งแคบขนาดนั้นไม่ได้ ให้ใช้ค่าที่แคบที่สุดที่ตั้งได้ แล้วระบุในรายงาน**
(เคยติดที่พื้นราว 500-620px) → **เรื่องล้นจอจะรายงานได้น้อยกว่าความจริง ต้องเขียนกำกับไว้ ไม่ใช่เงียบ**

**จินตนาการสถานการณ์นี้ตลอดเวลาที่ใช้**
> "เช้าวันจันทร์ 7:50 น. ยืนหน้าห้อง ถือมือถือมือเดียว เด็ก 40 คนกำลังเข้าห้อง เสียงดัง
> มีเวลา 3 นาทีก่อนเข้าแถว"

---

## ═══ เกี่ยวกับข้อมูลในระบบ ═══

ชุดนี้เป็นข้อมูลจำลองทั้งหมด ชื่อนักเรียนเป็นชื่อสมมติ ไม่ใช่คนจริง

- นักเรียน 40 คน (ใช้งานอยู่ 39 คน ย้ายออกไปแล้ว 1 คน)
- ข้อมูลเช็คชื่อย้อนหลัง 3 เดือน ประมาณ 2,300 แถว
- วันที่เกิน 7 วันที่แล้ว ถูกยืนยัน (confirmed) ไปแล้ว · 7 วันล่าสุดยังเป็นร่าง (draft)
- วันนี้ยังไม่ได้เช็คชื่อ เว้นไว้ให้ทดสอบ

### เคสพิเศษที่ซ่อนไว้ในข้อมูล

**ชื่อเด็กบางคนบอกเคสไว้ตรงๆ** (เช่น "ด.ญ.กชกร ย้ายเข้ากลางเทอม") ซึ่งไม่เหมือนของจริง
→ **โจทย์ไม่ใช่ "หาให้เจอ" แต่คือ "ถ้าชื่อไม่ได้บอก ครูจะรู้จากตรงไหน"**
ทุกครั้งที่เจอเคสพิเศษ ให้ประเมินว่าระบบมีอะไรบอกครูบ้างนอกจากชื่อ

| เลขที่ | เคส |
|---|---|
| 25 | ชื่อยาวเกิน 60 ตัวอักษร |
| 26-27 | วรรณยุกต์ซ้อน (`ณัฐฐ์` / `ปุ๊กกี้`) |
| 28 | ชื่อขึ้นต้นด้วย `=` — เคยเสียเป็น `#ERROR!` **แก้แล้ว 20 ส.ค.** ต้องเห็นชื่อเต็มว่า `=ด.ช.วีรภาพ สูตรทดสอบ` |
| 29 | ย้ายเข้ากลางเทอม (ราว 30 วันที่แล้ว) |
| 30 | ย้ายออกแล้ว (ราว 14 วันที่แล้ว) |
| 31-32 | ติดธงจับตา |

---

## ═══ ข้อควรระวัง ═══

**ห้ามกดยืนยันสิ่งเหล่านี้** (เข้าไปดูหน้าจอได้ แต่หยุดก่อนกดยืนยัน):
ลบภาคเรียน / เก็บถาวรภาคเรียน / ลบ PIN / รีเซ็ตรหัสครู / กู้คืนข้อมูลจากไฟล์สำรอง /
นำเข้าข้อมูล / ส่งอีเมล

> ★★ **ปุ่ม "ล้างข้อมูลทิ้ง" บนหน้ารายงาน ห้ามกดเด็ดขาด**
> มันอยู่ในกล่องเตือน "พบข้อมูลเช็คชื่อในวันที่ไม่ได้เป็นวันเรียน" คู่กับปุ่ม "นับเป็นวันเรียน"
> **ลบแถวเช็คชื่อของวันนั้นถาวร ไม่มีถังขยะ ไม่มี undo** กู้ได้ทางเดียวคือไฟล์สำรอง
> ⚠️ อย่าสับสนกับคำสั่งท้ายหัวข้อนี้ที่ให้ "ล้างข้อมูลทดสอบคืนสภาพเดิม"
> อันนั้นหมายถึงการ **ยกเลิกสถานะรายคน** ไม่ใช่ปุ่มนี้

- **ลิงก์ผู้ปกครอง** — ถ้าไม่มีลิงก์เดิมให้ **สร้างใหม่ได้ 1 อัน** เพื่อทดสอบหน้าผู้ปกครอง
  (เกินกว่านั้นห้าม เพราะการออกลิงก์ใหม่จะเพิกถอนของเดิมทันที)
  **กดแล้วลิงก์จะขึ้นเป็นกล่องบนหน้าจอ** อ่านค่าจากกล่องได้เลย ไม่ต้องพึ่งคลิปบอร์ด
- ห้ามใส่ PIN ผิดเกิน 3 ครั้ง (ครั้งที่ 5 ล็อก 15 นาที)
- อย่ากดรัวๆ ระบบมี rate limit จริง เว้น 1-2 วินาทีระหว่างคลิก
- เพิ่ม แก้ไข เช็คชื่อได้เต็มที่ เป็นข้อมูลจำลองล้วน

**จบงานแล้วให้ล้างข้อมูลทดสอบคืนสภาพเดิม** — ย้อนชุดที่เช็คไว้ และล้างสถานะที่แก้รายคน

---

## ═══ เรื่องความช้า อ่านก่อน ═══

Apps Script ช้ากว่าเว็บทั่วไปเป็นปกติ **3-8 วินาทีถือว่าธรรมดา**

**★ วัดจริงแล้ว 20 ส.ค. 2569** — จับเวลา round trip ฝั่ง client (ตั้งแต่กดจนคำตอบถึง
ก่อนวาดจอ) ได้ **ค่ากลาง 4.1 วินาที** จาก 7 ตัวอย่าง ช่วง 2.2–7.1 วินาที

| คำขอ | round trip | งานฝั่ง server |
|---|---:|---:|
| `getClientModuleContent` | 2,177 ms | แค่อ่าน cache |
| `getAttendanceDaily` ×3 | 3,547 · 4,004 · 4,062 ms | 725–1,187 ms |
| `recordAttendance` | 4,309 ms | เขียน 1 แถว |
| `undoAttendance` | 7,135 ms | ลบ 1 แถว |
| `getDashboardData` | 6,680 ms | 2,491–3,605 ms |

→ **ตัวเลข 3-8 วินาทีถูก แต่ไม่ใช่เพราะโค้ดช้า** · แต่ละคำขอมี overhead ของ Apps Script
ราว **2-3.5 วินาที** ไม่ว่า payload จะเล็กแค่ไหน (`getClientModuleContent` แทบไม่ทำงานเลย
ยังใช้ 2.2 วินาที) **เร่งโค้ดฝั่ง server ให้เร็วขึ้น 200 ms ครูไม่รู้สึกเลย
สิ่งที่ครูรู้สึกคือจำนวนคำขอ**

- กดแล้วดูเหมือนไม่มีอะไรเกิดขึ้น → รอ 10 วินาทีก่อนสรุปว่าพัง
- จะรายงานว่าอะไร "ไม่ทำงาน" → ต้องลองซ้ำอย่างน้อย 2 ครั้งก่อน
- ช้าเกิน 8 วินาที = จดเวลาไว้รายงาน แต่ไม่ใช่บั๊กโดยอัตโนมัติ
- ค้างเกิน 60 วินาทีหรือขึ้น error → **หยุดแล้วรายงานทันที**

### ★★ toast อยู่บนจอแค่ 2.5 วินาที — "รอ 10 วินาทีแล้วค่อยดู" ทำให้พลาดของที่ทำงานได้ปกติ

`toast()` [JavaScript.html] ตั้งอายุไว้ **2,500 มิลลิวินาที** (error 4,500) และ toast จะโผล่
**หลังจาก server ตอบกลับ** ซึ่งกินเวลา 3–8 วินาที → **หน้าต่างที่มองเห็นคือประมาณวินาทีที่ 3 ถึง 10 เท่านั้น**

**เคยพลาดมาแล้วจริง**: UAT 16 ส.ค. 2569 รายงานว่าปุ่ม "ลิงก์ผู้ปกครอง" พังสนิท กด 4 ครั้งไม่มีอะไรเกิดขึ้น
ความจริงคือปุ่มทำงานปกติทุกครั้ง แต่ถ่ายภาพหลังคลิก 20+ วินาที **จึงพลาด toast ทั้ง 4 ครั้ง**

**วิธีที่ถูก** — หลังกดปุ่มที่คาดว่าจะขึ้น toast ให้ถ่ายภาพ**ถี่ๆ ทุก 1 วินาที ตั้งแต่วินาทีที่ 2 ถึง 10**
ห้ามใช้ `wait 20` แล้วค่อยถ่ายภาพเดียว · และ**ก่อนสรุปว่า "ไม่มี feedback" ต้องอ่านโค้ดของปุ่มนั้นก่อน**
ว่าคาดหวังอะไร (toast / modal / คลิปบอร์ด / ดาวน์โหลด) — ของที่ไปลงคลิปบอร์ดจะไม่เห็นบนจอเลยโดยธรรมชาติ
ทดสอบด้วยการวางลงช่องค้นหาในหน้าเดียวกันได้

---

## ═══ ★ กับดัก 6 ข้อ อ่านให้จบก่อนเริ่ม ═══

หกข้อนี้ทำให้รายงานบั๊กเทียมได้ง่ายมาก ถ้าไม่รู้ไว้ก่อน

### 1. วันนี้อาจไม่อยู่ในปฏิทินวันเรียน

ถ้าวันที่ทดสอบเป็นเสาร์-อาทิตย์หรือวันหยุด ระบบจะขึ้นแถบเตือนว่ายังไม่อยู่ในปฏิทินวันเรียน
และ **ข้อมูลของวันนั้นจะไม่ถูกนับในรายงานและเอกสาร ปพ.6 — เป็นพฤติกรรมที่ตั้งใจ ไม่ใช่บั๊ก**

→ **ก่อนเริ่มโฟลว์ ก. ให้เช็คก่อนว่าวันนี้อยู่ในปฏิทินไหม**
ถ้าไม่อยู่ ให้กดปุ่ม **"นับเป็นวันเรียน"** ก่อน แล้วค่อยเทียบตัวเลข
หรือเลือกวันธรรมดาย้อนหลังแทน — และ**บันทึกไว้ในรายงานว่าเลือกทางไหน**

### 2. โมดูล `Js*` 6 ตัวถูก cache 15 นาที และล้างไม่ได้

ถูก cache ฝั่ง server 900 วินาทีโดยไม่มีวิธีล้าง
**ถ้าเพิ่ง deploy แล้วเริ่มทดสอบทันที จะเห็นโค้ดเวอร์ชันเก่าโดยไม่รู้ตัว**

| ติด cache | ไม่ติด — สดเสมอ |
|---|---|
| หน้าแรก · รายงาน · วิเคราะห์ · นำเข้า · **โปรไฟล์นักเรียน** · **มุมมองรูปภาพ** | หน้าเช็คชื่อ · นักเรียน · ตั้งค่า · **CSS ทุกไฟล์** |

★ ทั้ง 6 หน้าในช่องซ้ายค้างได้ 15 นาทีเท่ากันหมด **รวมหน้ารายงาน**
(เคยมีคนเข้าใจผิดว่าหน้ารายงานไม่ติด cache เพราะไฟล์ใหญ่เกินเพดาน — วัดแล้วไม่จริง)

**การ "ลองซ้ำ 2 ครั้ง" ไม่ช่วยกับกับดักข้อนี้** เพราะลองซ้ำภายใน 15 นาทีก็ได้ของเก่าเหมือนเดิม

→ **ให้เริ่มจากหน้าที่ไม่ติด cache ก่อน**
→ ถ้าเจอของแปลกในหน้าที่ติด cache ให้ถามคนสั่งงานว่า deploy มานานเท่าไรแล้ว

### 3. ★ เมนูในชีตกับเว็บแอปรันโค้ดคนละเวอร์ชัน — ทำให้ตีความผลทดสอบผิดได้ทั้งรอบ

- **เมนู 🎓 ในชีต** (รวม `runP0Diagnostics`) → รัน**โค้ดล่าสุด** ได้ผลทันทีหลัง `clasp push`
- **เว็บแอป** (ทุกปุ่มที่กดในหน้าจอ) → รัน**โค้ดของ deployment version** ซึ่งไม่ขยับจนกว่าจะ
  สร้าง version ใหม่จาก Apps Script UI

**`clasp push` อย่างเดียวไม่พอ** → ผลตรวจ Diagnostics เขียวไม่ได้แปลว่าเว็บแอปได้โค้ดใหม่แล้ว

เคยเกิดจริง: เช็คเรื่องลบแถวเขียว แต่กดปุ่มเก็บถาวรบนหน้าจอยังพังด้วย error เดิม เพราะ
deployment ยังเป็นเวอร์ชันก่อนแก้

→ **ก่อนเริ่มทดสอบ ให้ยืนยันว่าสร้าง deployment version ใหม่แล้ว ไม่ใช่แค่ push**
→ ถ้าเจอ error ให้เทียบ**เลขบรรทัดใน stack trace** กับโค้ดปัจจุบัน ถ้าไม่ตรง = กำลังทดสอบโค้ดเก่า

**★ ใช้ความต่างนี้ให้เป็นประโยชน์ — แก้ฝั่ง server แล้วรัน Diagnostics จากเมนูก่อน deploy**
เมนูรันโค้ดล่าสุดเสมอ จึงบอกได้ทันทีว่า**โค้ดถูกหรือผิด** โดยไม่ต้องเสียรอบ deploy
ถ้าเช็คแดง = โค้ดผิดจริง แก้ต่อได้เลย · ถ้าเช็คเขียวแต่หน้าจอยังพัง = deployment ยังเป็นของเก่า
**เคยประหยัดไป 1 รอบจริง** ตอนแก้เรื่องหน้าเช็คชื่ออ่านชีต archive ไม่ได้ —
Diagnostics ฟ้องว่าอ่านได้ 0 แถวทั้งที่ควรได้ 39 จึงรู้ทันทีว่าไม่ใช่เรื่อง deployment

### ★ การรอให้ session ครูหมดอายุเองใช้ไม่ได้ — `sessionStorage` ตายก่อน

session ครูอายุ 2 ชม. แต่ที่เก็บฝั่ง client คือ **`sessionStorage` ซึ่งตายเมื่อปิดแท็บ**
และ session เป็นแบบ **sliding** (ต่ออายุทุกครั้งที่ยิง request) → นั่งรอ 2 ชม.
มักได้ **`sessionStorage` ว่าง** ไม่ใช่ session ที่หมดอายุแต่ยังอยู่ ซึ่งเป็น**คนละเคสกัน**
และเคสที่อยากทดสอบมักเป็นเคสหลัง

**แยกให้ออกจากหน้าจอ** — ทั้งสองเคสขึ้นหน้าเข้าสู่ระบบเหมือนกัน ต่างกันที่**กล่องเตือนสีเหลือง**
| เห็นอะไร | แปลว่า |
|---|---|
| ฟอร์มเปล่า **ไม่มีกล่องเตือน** | `sessionStorage` ว่าง — **ยังไม่ได้ทดสอบเรื่องหมดอายุเลย** |
| มีกล่องเตือน *"เซสชันหมดอายุแล้ว..."* | โค้ดฝั่ง client จับได้ตอนอ่าน session กลับ |
| มีกล่องเตือน *"เซสชันหมดอายุ กรุณาเข้าสู่ระบบใหม่"* | server เป็นคนปฏิเสธ (`AUTH_EXPIRED`) = เส้นทางเก่า |

→ **สร้างเคสตรงๆ ด้วย Console แทนการรอ** · ทำได้จริงแล้ว 17 ส.ค. 2569 แต่มี 3 ด่าน

1. **ต้องล็อกอินอยู่ก่อน** ไม่งั้นไม่มีคีย์ให้แก้
2. **สลับ frame ใน Console จาก `top` เป็น `userCodeAppPanel`** — แอปอยู่ใน iframe
   **คนละ origin** กับหน้าที่ห่ออยู่ และ `sessionStorage` แยกตาม origin
   ไม่สลับจะได้ `Uncaught SyntaxError: "undefined" is not valid JSON`
   เช็คว่าสลับถูกด้วย `Object.keys(sessionStorage)` → ต้องเห็น `att_teacher_auth`
3. **อย่าใช้ `location.reload()`** ใน frame นั้น มันรีโหลดแค่ iframe ซึ่งเปิดเดี่ยวๆ ไม่ได้
   → **จอขาว** ให้รีโหลดหน้านอกแทน (Cmd+R) ค่าที่ปลอมไว้ยังอยู่เพราะ `sessionStorage`
   อยู่ตามอายุแท็บ ไม่ได้หายตอนรีโหลด

```
a=JSON.parse(sessionStorage.att_teacher_auth);a.expires_at=1;sessionStorage.att_teacher_auth=JSON.stringify(a)
```
**ห้ามใส่ `0`** — โค้ดตี `0` ว่า "ไม่มีฟิลด์นี้" แล้วถือว่ายังไม่หมดอายุ (ตั้งใจให้เป็นแบบนั้น
กันครูถูกเตะออกตอนอัปเดตโค้ด) · Chrome อาจขอให้พิมพ์ `allow pasting` ก่อน paste

### ★ จอขาวหลัง deploy ใหม่ ≠ พัง — โหลดครั้งแรกนานกว่า 20 วินาทีได้

เกิดจริง 18 ส.ค. 2569: เปิดหน้าครูหลัง deploy **รอ 18 วินาทีสองรอบยังขาว**
จนเกือบสรุปว่าโค้ดที่เพิ่งแก้พัง และเกือบสั่ง revert งานที่ถูกต้อง
ความจริงคือหน้ายังวาดไม่เสร็จ พอเปิดใหม่อีกครั้งก็ขึ้นปกติ

**★ หน้าครูใช้เวลาโหลดนานกว่าหน้าผู้ปกครองมาก — วัดได้จริง 18 ส.ค. 2569**
หน้าผู้ปกครองขึ้นที่ ~20 วินาที แต่**หน้าครูยังขาวอยู่ที่ 35 วินาที** แล้วค่อยขึ้น
เพราะหน้าครูต้องโหลดเป็นทอดๆ: shell 627 KB → bootstrap → โมดูล `Js*` → ข้อมูลของหน้า
**การถ่ายภาพระหว่างนั้นได้จอขาวทุกครั้ง** และเข้าใจผิดว่าพังได้ง่ายมาก

**ก่อนสรุปว่าจอขาว = พัง ให้ไล่ 4 ข้อนี้**
1. **รอให้ถึง 40-60 วินาที** สำหรับหน้าครู ไม่ใช่ 10-20 · หน้าผู้ปกครองกับหน้าพิมพ์เร็วกว่า
2. **มองหา skeleton** (กล่องเทาโค้งมนกับหัวข้อ "พร้อมใช้งาน") = **แอปบูตแล้ว กำลังโหลด ไม่ได้พัง**
   ถ้าเห็น skeleton ให้รอต่อ อย่ารีโหลด เพราะรีโหลดคือเริ่มนับใหม่
3. **เช็คว่า HTML มาครบไหม** รันใน Console ของ frame `top`:
   `document.body.innerHTML.length` — ถ้าได้หลักแสน = server ส่งมาครบ ปัญหาอยู่ฝั่ง client
4. **อ่าน error ต้องอยู่ใน frame `userCodeAppPanel`** — `top` ไม่มีทั้ง error ของแอป
   และไม่มี `google.script.run` (รันคำสั่งที่นั่นจะได้ `google is not defined` ซึ่ง
   **ไม่ใช่บั๊กของระบบ** แค่รันผิด frame)
   ★ ถ้า console ของ frame นั้นขึ้น **"No Issues"** = ไม่มี error จริงๆ **ให้กลับไปข้อ 1**

### 4. วิธีจับเวลา — ต้องวัดแบบเดียวกันทุกหน้า

เวลาที่ผู้ทดสอบจับได้รวม overhead ของเครื่องมือขับเบราว์เซอร์ ซึ่ง**ช้ากว่าครูจริงอย่างมีระบบ**

→ วัดจาก **คลิกเมนู → จนเนื้อหาขึ้นครบบนจอ** และระบุในรายงานว่าวัดด้วยวิธีนี้
→ **"จำนวนครั้งที่แตะจอ" เชื่อถือได้ · "กี่นาที" เชื่อถือไม่ได้** ให้แยกรายงานคนละช่อง

---

## ═══ ส่วนที่ 1 — เดิน 4 โฟลว์นี้ให้จบตั้งแต่ต้นจนจบ ═══

อย่าเปิดดูทีละหน้าแล้วข้าม **บั๊กจริงส่วนใหญ่อยู่ที่รอยต่อระหว่างหน้า**
จบแต่ละโฟลว์ให้บอกเวลาเริ่ม-จบ

### ── ก. เช็คชื่อ → ตรวจตัวเลขข้ามหน้า ──

0. เช็คก่อนว่าวันนี้อยู่ในปฏิทินวันเรียนไหม (กับดักข้อ 1)
1. **เช็คชื่อทีละคนให้ครบ** นับว่าแตะจอกี่ครั้ง
2. ล้างทิ้ง แล้ว**ลองปุ่ม "เช็คมาทั้งหมด"** นับครั้งแตะจออีกรอบ
   ★ สองวิธีนี้ต่างกันกี่ครั้ง — นี่คือคำตอบของคำถามข้อ 2 ท้ายเอกสาร
3. จดตัวเลข "วันนี้มาเรียนกี่คน / ขาดกี่คน" จากหน้าแรก
4. ไปหน้ารายงาน ออกรายงานช่วงที่รวมวันนี้
5. ★ ตัวเลขตรงกับที่จดไว้ไหม **ถ้าไม่ตรง ให้เช็คก่อนว่าเป็นเพราะกับดักข้อ 1 หรือเปล่า**
   ถ้าไม่ใช่ = ปัญหาใหญ่
6. ไปหน้าวิเคราะห์ ★ ตัวเลขตรงกับสองหน้าแรกไหม

### ── ข. เด็กย้ายเข้า-ย้ายออก ──

1. หาเด็กที่ย้ายเข้ากลางเทอม (เลขที่ 29)
2. ★ ออกรายงานทั้งเทอม — เขาถูกนับเป็น "ขาด" ในวันก่อนที่เขาเข้ามาไหม (**ต้องไม่นับ**)
   เปอร์เซ็นต์การมาเรียนสมเหตุสมผลไหม
3. หาเด็กที่ย้ายออกแล้ว (เลขที่ 30)
4. ★ เขายังโผล่ในหน้าเช็คชื่อวันนี้ไหม (**ไม่ควรโผล่**)
5. ★ แต่ยังอยู่ในรายงานย้อนหลังช่วงที่เขายังเรียนอยู่ไหม (**ควรอยู่**)
6. ★ ถ้าชื่อไม่ได้เขียนว่า "ย้ายเข้ากลางเทอม" / "ย้ายออกแล้ว" ครูจะรู้จากตรงไหน
7. ★ ดูเลขที่ 25 (ชื่อยาว) และ 28 (ขึ้นต้นด้วย `=`) ในตารางรายวันและใบ ปพ.6
   ชื่อแสดงครบไหม ล้นจอไหม กลายเป็นค่าประหลาดไหม

### ── ค. แก้ไขและวันที่ยืนยันแล้ว ──

1. เปลี่ยนสถานะเด็ก 1 คนของ "วันเมื่อวาน" (ยังเป็นร่าง)
2. ★ มีบันทึกประวัติการแก้ไขไหม หาเจอไหม
3. กลับไปดูรายงาน ★ ตัวเลขอัปเดตตามไหม หรือค้างค่าเดิม
4. ★ ลองแก้สถานะของวันที่เกิน 7 วันที่แล้ว (ยืนยันไปแล้ว) ระบบยอมไหม เตือนไหม
5. ★ ถ้าไม่ยอม — ครูจะแก้ยังไงเมื่อเช็คผิดแล้วเพิ่งมารู้ตัว
   หาทางออกในระบบให้เจอ ถ้าหาไม่เจอให้รายงาน

### ── ง. ความเร็วกับข้อมูลจริง ──

### ⚠️ ถ้าใช้เครื่องมืออัตโนมัติกดแทนคน — "กดรัวๆ" ต้องยิงคลิกแยกจริง

ตัวเลือก `repeat` ของเครื่องมือกดอัตโนมัติ **อาจไม่ได้ยิงคลิกหลายครั้งจริง**
รอบ 20 ส.ค. 2569 `repeat: 3` ให้ผลเท่ากับคลิกเดียวเป๊ะ **ทั้งบนโค้ดที่มีบั๊กและโค้ดที่แก้แล้ว**
จึงเกือบสรุปว่า "แก้ไม่ติด" ทั้งที่แก้ถูกแล้ว

→ **ให้สั่งคลิกแยกเป็นคำสั่งละครั้ง** แล้วนับผลลัพธ์ (ถอยกี่วัน/กี่เดือน)
→ ถ้าผลออกมาเหมือนเดิมทั้งก่อนและหลังแก้ **ให้สงสัยเครื่องมือก่อนสงสัยโค้ด**
   ยืนยันเวอร์ชันที่เสิร์ฟจริงได้ด้วยการ `curl` หน้า `/exec` แล้ว grep หาโค้ดที่เพิ่งแก้

★ จับเวลาโหลดแต่ละหน้าตามวิธีในกับดักข้อ **4** **พร้อมระบุว่าเลือกช่วงวันที่กว้างแค่ไหน**

1. หน้าแรก · 2. หน้าเช็คชื่อ 40 คน · 3. รายงานสรุป 1 เดือน ·
4. รายงานสรุปทั้ง 3 เดือน · 5. รายงานรายวันทั้ง 3 เดือน · 6. หน้าวิเคราะห์

หน้าไหนเกิน 8 วินาทีให้รายงานพร้อมเวลาที่จับได้

### งานที่แก้ไปแล้วและ**ยืนยันบนของจริงแล้ว** — ไม่ต้องเดินซ้ำในรอบนี้

ปุ่ม ◀ ▶ หน้าเช็คชื่อ · การแตะสถานะร่วมกับตัวกรอง · การบันทึกที่ไม่ล้างจอ ·
ปุ่มเลื่อนเดือนหน้าติดตาม — ทดสอบครบบน deployment จริงแล้ว 20 ส.ค. 2569
ขั้นตอนละเอียดและผลที่ได้อยู่ใน `TODO.md` **ถ้าจะเดินซ้ำให้ดูที่นั่น**

---

## ═══ ★ เช็คให้ครบแล้ว "ยืนยันวันนี้" — ปลายทางของงานเช้าทุกวัน ═══

⚠️ **ทำบนวันนี้เท่านั้น** และคืนสภาพให้ครบตามข้อ 5-6 ก่อนไปต่อ

1. เปิดหน้าเช็คชื่อของวันนี้ตอนที่**ยังไม่เช็คใครเลย**
   ★ สังเกตว่า**ไม่มีทั้งปุ่ม "ยืนยันวันนี้" และไม่มีคำอธิบายใดๆ ว่าต้องทำอะไรต่อ**
   → คำถาม: ครูที่เพิ่งซื้อระบบไปจะรู้ได้ยังไงว่างานเช้าจบที่การกดยืนยัน
2. เช็คบางคน (ยังไม่ครบ) → ต้องเห็นป้าย **"ต้องเช็คชื่อให้ครบก่อนจึงจะยืนยันวันนี้ได้"**
   **ไม่ใช่ปุ่ม** · ถ้าเห็นปุ่มยืนยันทั้งที่ยังไม่ครบ = บั๊ก
3. กด **"เช็คมาทั้งหมด"** → ยืนยันในกล่อง → ต้องได้ 39/39 = 100%
   แล้ว**ปุ่ม "ยืนยันวันนี้" (สีเขียว) จึงโผล่**
4. กด "ยืนยันวันนี้" → กล่องบอกว่า *"ระบบจะล็อกวันนี้ไว้ก่อน และคุณยังยกเลิกการยืนยันเพื่อกลับมาแก้ไขได้อีกครั้ง"*
   → ตกลง → ต้องเห็นครบ 3 อย่าง
   - ป้ายเปลี่ยนเป็น **"✓ ยืนยันแล้ว"**
   - เลื่อนดูรายชื่อ → **ปุ่มสถานะทั้ง 5 หายไปทั้งหมด** เหลือแค่ป้ายอ่านอย่างเดียว
   - ★★ **ปุ่ม "แจ้งผู้ปกครอง" โผล่มาติดกับ "ยกเลิกการยืนยัน" — ปุ่มนี้อยู่ในรายการห้ามกด อย่าเผลอ**
5. กด **"ยกเลิกการยืนยัน"** → ต้องกลับไปแก้สถานะได้เหมือนเดิม
6. กด **"ย้อนชุดล่าสุด (39)"** → ต้องกลับเป็น 0/39 เหมือนก่อนเริ่ม

★ ข้อ 4-5 คือคำตอบของโฟลว์ **ค. ข้อ 5** ("ครูจะแก้ยังไงเมื่อเช็คผิดแล้วเพิ่งมารู้ตัว")

---

## ═══ ★ เด็กที่ยังไม่มีวันวัดผล ต้องขึ้น `-` ไม่ใช่ 0% ═══

ตัวเลขนี้ขึ้นเอกสารทางการ ปพ.6 · **0% แปลว่า "ขาดทุกวัน" ส่วน `-` แปลว่า "ยังวัดไม่ได้" คนละเรื่องกัน**
ถ้าระบบสับสนสองอย่างนี้ เด็กที่เพิ่งย้ายเข้าจะขึ้นแดงว่าต้องติดตามทั้งที่ยังไม่เคยขาดสักวัน

⚠️ ตอนนี้**ไม่มีเด็กคนไหนใน seed อยู่ในสถานะนี้** ต้องสร้างสถานะขึ้นมาชั่วคราว

1. หน้านักเรียน → เลขที่ 1 → **แก้ไข** → **จดค่า "เริ่มอยู่ในห้องตั้งแต่" เดิมไว้ก่อน**
   (ถ้าไม่จด จะคืนค่าไม่ได้ — ค่าปัจจุบันคือ **08/03/2026**)
2. เปลี่ยนเป็น**วันนี้** → บันทึก
3. หน้ารายงาน → เลื่อนไปตาราง "สรุปรายคน" → เลขที่ 1 ต้องได้
   - ช่อง **%มาเรียน = `-`** (ไม่ใช่ `0%`)
   - ช่อง **วันเรียน = 0**
   - ป้ายสถานะ = **"ไม่มีวันวัดผล"** สีเหลือง (**ไม่ใช่ "ต้องติดตาม" สีแดง**)
   **ถ้าเห็น 0% หรือขึ้นแดง = บั๊ก ให้รายงานทันที**
4. ★ **คืนค่าเดิมทันที** → แก้กลับเป็นค่าที่จดไว้ → เลขที่ 1 ต้องกลับเป็น 100% / 60 วันเรียน / "เรียบร้อย"

> การเปลี่ยนช่องนี้ **ไม่แตะข้อมูลเช็คชื่อเลย** จึงย้อนกลับได้ 100%
> แต่ระหว่างที่ยังไม่คืนค่า ตัวเลขรวมของทั้งห้องจะเพี้ยน **อย่าทิ้งค้างไว้**

---

## ═══ ส่งออก CSV ═══

หน้ารายงาน → เลือกช่วงวันที่ → **"ดาวน์โหลด CSV"**

1. ต้องขึ้นกล่อง **"ดาวน์โหลดไฟล์"** พร้อมปุ่มให้กดเอง
   (เบราว์เซอร์บล็อกการเปิดหน้าต่างอัตโนมัติเป็นเรื่องปกติ ระบบจึงมีกล่องนี้ไว้)
   **ถ้าไม่มีอะไรเกิดขึ้นเลย = บั๊ก** ให้รายงาน
2. กล่องต้องบอกด้วยว่าไฟล์เป็นของชั่วคราวและจะถูกลบจาก Drive ภายหลัง

> ⚠️ **การเปิดไฟล์ CSV มาตรวจตัวเลข ผู้ทดสอบทำไม่ได้** — ต้องดาวน์โหลดลงเครื่อง
> ให้รายงานแค่ว่ากล่องขึ้นครบไหม · **การเทียบตัวเลขในไฟล์เป็นงานของคนสั่งงาน**

---

## ═══ ส่วนที่ 2 — เดินให้ครบทุกหน้าในเมนู ═══

เข้าดูทุกหน้าที่หาเจอ ลองใช้เท่าที่ปลอดภัย รวมหน้าตั้งค่า หน้าพิมพ์ และหน้าผู้ปกครอง

**หน้าพิมพ์** ให้สลับเป็น desktop 1440x900 แล้วตรวจบนจอ: ตารางตกขอบไหม ภาษาไทยแสดงครบไหม
ชื่อยาวถูกตัดไหม หัวตารางกับข้อมูลตรงคอลัมน์กันไหม

> ⚠️ **การสั่งพิมพ์ PDF จริงผู้ทดสอบทำไม่ได้** — กล่องพิมพ์ของ macOS อยู่นอกเบราว์เซอร์
> ให้รายงานเฉพาะที่ตรวจบนจอได้ และระบุว่า **"การตัดหน้ากระดาษจริงยังไม่ได้ทดสอบ"**
> เรื่องตัดหน้ากลางตารางต้องให้คนสั่งงานกดพิมพ์เอง

---

## ═══ สิ่งที่ผู้ทดสอบทำไม่ได้ — ให้รายงานว่าทำไม่ได้ ห้ามข้ามเงียบ ═══

| ทำอะไรไม่ได้ | เพราะอะไร |
|---|---|
| พิมพ์รหัสครู / PIN | เป็นรหัสผ่าน |
| สั่งพิมพ์ PDF จริง | กล่องพิมพ์อยู่นอกเบราว์เซอร์ |
| ขับช่อง `<select>` เนทีฟบางจุด | หน้าเว็บอยู่ใน iframe ข้าม origin |
| อ่าน DOM / ข้อความในหน้า | iframe ข้าม origin — ต้องดูจากภาพหน้าจออย่างเดียว |
| ตั้ง viewport แคบกว่าที่เครื่องมือยอม | มีพื้นความกว้างขั้นต่ำ ระบุค่าที่ใช้จริงในรายงาน |
| เปิดไฟล์ CSV ที่ดาวน์โหลดมาตรวจตัวเลข | ต้องดาวน์โหลดลงเครื่อง — ให้คนสั่งงานตรวจเอง |

**★ หน้าผู้ปกครองทดสอบได้แล้ว** (เคยอยู่ในตารางนี้) — ออกลิงก์ 1 อันแล้วเปิดได้จริง

ถ้าติดข้อไหน ให้เขียนในรายงานว่าติดตรงไหนและเพราะอะไร

---

## ═══ คำถามที่อยากได้คำตอบ ═══

1. ระบบนี้เข้าใจงานของครูจริงไหม มีตรงไหนที่รู้สึกว่า "คนออกแบบไม่เคยยืนหน้าห้องเรียน"
2. ตอนเช็คชื่อ 3 นาทีเช้าวันจันทร์ ระบบนี้ช่วยหรือขวาง เทียบกับกาในสมุดกระดาษ อันไหนเร็วกว่า
   (ใช้ตัวเลขจากโฟลว์ ก. ข้อ 1-2 มาตอบ)
3. ศัพท์ที่ใช้ในระบบเป็นศัพท์ที่ครูไทยพูดจริงไหม มีคำไหนเป็นศัพท์โปรแกรมเมอร์หรือแปลตรงตัว
4. มีอะไรที่ครูต้องทำในชีวิตจริง แต่ระบบไม่รองรับ
   เช่น เด็กมาสายหลังเช็คไปแล้ว / ลาป่วยหลายวันติด / วันหยุดพิเศษของโรงเรียน /
   เช็คชื่อคาบบ่ายแยกจากเช้า
5. ตรงไหนที่กดผิดแล้วเสียหาย แต่ระบบไม่เตือน
6. ข้อมูล 3 เดือนที่มีอยู่ ระบบเอามาทำอะไรให้ครูได้บ้าง มีอะไรที่ครูอยากรู้แต่ระบบไม่บอก
7. ถ้าคุณเป็นครูที่จ่ายเงินซื้อระบบนี้ จะบอกเพื่อนครูว่าอะไร และจะเตือนว่าอะไร

---

## ═══ วิธีรายงาน ═══

ส่งเป็น **ข้อความในแชท** (ถ้ายาวเกิน ให้ทำเป็นหน้าเว็บแล้วส่งลิงก์)

**ส่วนที่ 1 — ความรู้สึกโดยรวม**
ย่อหน้าสั้น 5-10 บรรทัด ในฐานะครู ไม่ใช่ในฐานะโปรแกรมเมอร์

**ส่วนที่ 2 — เรื่องที่ไม่สมจริง**
แต่ละข้อเขียนรูปแบบนี้: *"ในความเป็นจริงครูทำแบบ ___ แต่ระบบบังคับให้ทำแบบ ___"*

**ส่วนที่ 3 — ตารางเวลาโหลด**

| หน้า | ช่วงข้อมูล | เวลา (วินาที) | จำนวนครั้งที่แตะจอ |
|---|---|---|---|

**ส่วนที่ 4 — บั๊กหรือของแปลกที่เจอ**

```
[ระดับ] พาดหัว
- หน้าไหน:
- กดอะไร:
- เกิดอะไรขึ้น:
- ควรเกิดอะไร:
- ลองซ้ำแล้วกี่ครั้ง:
- ตัดกับดัก 3 ข้อออกแล้วหรือยัง:
- ภาพหน้าจอ: [แนบ]
```

ระดับ: **[พัง]** ใช้ไม่ได้ · **[เสี่ยง]** ข้อมูลเสียหายหรือรั่ว ·
**[กวนใจ]** ใช้ได้แต่ทรมาน · **[ขัดใจ]** ความสวยงาม

**ส่วนที่ 5 — สิ่งที่ทำไม่ได้** พร้อมเหตุผล

---

## ═══ ข้อกำหนด ═══

★ **ห้ามเขียนข้อสังเกตแบบทั่วไปที่ใช้ได้กับแอปไหนก็ได้**
ทุกข้อต้องอ้างอิงสิ่งที่เห็นบนหน้าจอจริง ระบุชื่อปุ่ม ชื่อหน้า หรือข้อความที่ปรากฏ
**ถ้าเขียนแล้วเอาไปแปะกับแอปอื่นได้ = ตัดทิ้ง**

- ไม่แน่ใจว่าเป็นบั๊กหรือออกแบบตั้งใจ → รายงานแล้วบอกว่าไม่แน่ใจ
- อย่าเดาว่าโค้ดข้างหลังทำงานยังไง รายงานเฉพาะที่เห็นบนหน้าจอ
- ทำข้อไหนไม่ได้ → บอกว่าทำไม่ได้เพราะอะไร อย่าข้ามเงียบๆ
- จบแต่ละโฟลว์ บอกเวลาเริ่ม-จบ เพื่อเอาไปเทียบกับ log ฝั่งเซิร์ฟเวอร์
- **รายงานเป็นภาษาไทย**
